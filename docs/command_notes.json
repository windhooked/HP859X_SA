{
  "ABORT": {
    "behavior": "Type-0x10 softkey/state handler at 0x406EC: tests bit 12 of state word $b1e0.w and, if set with $b1fe.w==0x12, JSRs the low-mem A7-API routine $4d8.w to abort the current active-function/measurement state; otherwise returns.",
    "category": "",
    "verified": true
  },
  "ABS": {
    "behavior": "Shared handler 0x434E6 (also AC): copies calibration/trace RAM blocks out of cal RAM 0x2FC016 into low-RAM working buffer $9EB6 (length validated via fcn.61EC), conditionally copies 0x2FCB42->$A01E when $bfee.w>0x218f, and 0x2FC91A->$A096 \u2014 stages cal/trace data into RAM scratch.",
    "category": "",
    "verified": true
  },
  "AC": {
    "behavior": "Shares the exact handler at 0x434E6 with ABS: block-copies cal RAM 0x2FC016->$9EB6 and 0x2FC91A->$A096 (plus the conditional 0x2FCB42->$A01E), moving calibration/trace RAM regions into low-RAM working buffers.",
    "category": "",
    "verified": true
  },
  "ACP": {
    "behavior": "Handler 0x3E448: link A6, pushes D0, BSRs fcn.3E3E6 then JSRs low-mem A7-API routine $6802.w before returning \u2014 a thin dispatcher (adjacent-channel-power measurement setup) whose real work is in fcn.3E3E6/$6802.",
    "category": "",
    "verified": true
  },
  "ACPCONTM": {
    "behavior": "Handler 0x702E: thin wrapper that saves D0 and BSRs fcn.6F78 (the ACP continuous-measurement routine) then returns; actual behavior lives in fcn.6F78 which was not disassembled.",
    "category": "",
    "verified": false
  },
  "ACPE": {
    "behavior": "Handler 0xED78: parses the argument token bytes (checks for the 0x04,0x23('#'),0x41('A') sequence, else dereferences D0), extracts channel count into $b1f8/$b24c, then loops calling ADCXFR (fcn.B3F4) and ADCSHOW (fcn.B9D6) with $ba7c/$ba7e params \u2014 runs the extended adjacent-channel-power measurement.",
    "category": "",
    "verified": true
  },
  "ACPGRAPH": {
    "behavior": "Handler 0x3FBF8: on query (D0==0) reads state via $18a.w and derives a value from the $b0a6/$b0a7 ACP-graph state word (sign-extending the packed byte field), calling fcn.71E4/70D2; on set it merges the argument into $b0a6.w \u2014 toggles/sets the ACP graph display mode.",
    "category": "",
    "verified": true
  },
  "ACPSNGLM": {
    "behavior": "Handler entry 0x4063A: loads mode enum D1=-1 (moveq #-1) and JMPs the shared mode-setter fcn.3E702 \u2014 one arm of a jump table that selects the ACP single-measurement mode value and dispatches to the common setter.",
    "category": "",
    "verified": true
  },
  "ACTDEF": {
    "behavior": "Handler 0x28436: initializes ring/list pointers $b02e,$a996,$a992 to $bffe, then loops over a 12-entry table at 0x2842A computing A7-bus DAC port addresses 0xFFF700+2*idx, writing #$8000 to each port and reading them back into a $a706-area buffer \u2014 resets/initializes the 0xFFF700-series analog DAC ports (active-function default).",
    "category": "",
    "verified": true
  },
  "ACTVF": {
    "behavior": "Handler 0x5ACB2: calls the menu-partition loader fcn.5AA88 and fcn.5A552, builds a 4-entry menu vtable (clears/sets word fields near $9AA6, seeds $8000|masked $ab76), and initializes active-function menu state $956A=0,$956C,$956E,$9544 \u2014 sets up the active-function menu/display.",
    "category": "",
    "verified": true
  },
  "ADCPBKT": {
    "behavior": "Handler 0x184A2: writes #$2FFF to the A7-bus coarse-YTO DAC $f704.w, masks $b212.w, calls low-mem A7 routines $718/$766 and fcn.17546/fcn.5B0DA, then reads the POST self-test latches $f610/$f612 (masking 0xEC) and clears accumulators $b218/$b21c/$b220 \u2014 an ADC pass-band/bucket diagnostic sweep.",
    "category": "",
    "verified": true
  },
  "ADCSETUP": {
    "behavior": "Handler 0xABD0: writes command word #$0002 to the serial interface command port $f5fc.w (0xFFF5FC) and #$8100 to data port $f5fe.w (0xFFF5FE), then returns \u2014 configures the SCI/ADC serial interface (a sibling routine at 0xABEC also copies a 4-word default table into cal RAM 0x2FC004).",
    "category": "",
    "verified": true
  },
  "ADCSHOW": {
    "behavior": "Handler 0xB9D6: from byte coordinates $ba74/$ba75 it computes packed display-region words (ori #$9070 into $ba6c, plus $ba6e/$ba70) defining the on-screen rectangle where the ADC/measurement readout is drawn.",
    "category": "",
    "verified": true
  },
  "ADCXFR": {
    "behavior": "Handler 0xB3F4: clears SCI command port $f5fc.w, polls ready bit1 of $f5fd.w, then writes #$8000, D0, and the stack-arg word to data port $f5fe.w (0xFFF5FE) storing results to $ba80/$ba82 \u2014 performs a serial ADC/DAC value transfer over the SCI.",
    "category": "",
    "verified": true
  },
  "ADD": {
    "behavior": "Handler 0xD6B2: issues an HD63484/SCI command stream through $f5fc/$f5fd/$f5fe \u2014 WPR 0x080C=0x4003, WPR 0x080D=0xFC00, block-write 0x5800 followed by D0,0x3F,0xFF \u2014 then polls status bits 5/2 of $f5fd.w; a display-RAM graphics block operation, not arithmetic.",
    "category": "",
    "verified": true
  },
  "ADEFSCL": {
    "behavior": "Handler 0x317A0: dereferences structure pointer $a89a->A4, compares word fields (A4+0x14) vs (A4+0x16), and if they differ calls fcn.3177C with param 0x3F (returning its result), else returns 0 \u2014 a default-scale/range check on the addressed data structure.",
    "category": "",
    "verified": true
  },
  "AMB": {
    "behavior": "Unresolved (handler_pc=0, not reachable via the dispatch tables); by name and arg-type 0x17 this is the trace-math 'A minus B' operation (store trace A - trace B into trace A), but no disassembly confirms it.",
    "category": "",
    "verified": false
  },
  "AMBPL": {
    "behavior": "Unresolved (handler_pc=0, not resolved via dispatch tables); by name and arg-type 0xC1 this is the trace-math 'A minus B plus display line' operation (A - B + reference display line -> A), but no disassembly confirms it.",
    "category": "",
    "verified": false
  },
  "AMPCOR": {
    "behavior": "Amplitude-correction editing entry: gated on the active-function register $b1e4==0x34, it drives function id 0x1f through low-mem slots ($5fe query-active, $42a activate, $616, $610) and reads status registers $b1f8/$b1f9/$b073/$b212/$ad65 to pick the correction-table edit mode.",
    "category": "",
    "verified": true
  },
  "AMPLEN": {
    "behavior": "Parses a numeric argument (slot $6112, arg count in $b0c8) into 32-bit RAM var $ad5a, then clamps it into one of two ranges ([0xC350..0x53EC60] or [0x1388..0x1E8480]) depending on a mode/units bit0 returned by fcn.223a2.",
    "category": "",
    "verified": true
  },
  "AMSG": {
    "behavior": "Display/annotation redraw for the active trace: selects a graticule region index from $b070 bit0, pushes width 0x190, calls display slots $544/$5ce, and conditionally slot $562 when $b1e0 bit c is set and $b1e4 is in the range (6,0xB).",
    "category": "",
    "verified": true
  },
  "ANNOT": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0); by name and arg type 0x18 this is the screen-annotation on/off control. Best-effort meaning only.",
    "category": "",
    "verified": false
  },
  "ANNOTCTRL": {
    "behavior": "Parses a numeric argument (slot $7978) into $b0ec and sets redraw flag $b05a=1, then clamps the value into 0x1D..0x29 (special-casing 0x1D or 0x16 based on the $bfee model check) \u2014 an annotation position/mode control.",
    "category": "",
    "verified": true
  },
  "ANNOTCTRLA": {
    "behavior": "Reads the display-list/menu structure pointer at $956a and, if bit 7 of field (-0x61AE,A4) is set, calls subroutine 0x4f31c; the effect of that thin sub-dispatch is unresolved.",
    "category": "",
    "verified": false
  },
  "ANNOTCTRLS": {
    "behavior": "Stores the argument longword-pair to $b068 and sets $b0a1 bit0 (redraw); clears $ad65 bit3 and, unless $ad65 bit4 is set, activates functions 0x31/0x32 via slot $42a, then drives the display via slot $54a4.",
    "category": "",
    "verified": true
  },
  "APB": {
    "behavior": "Shares handler 0x28436 with AXB (arg 0xFF). Sets up buffer pointers ($b02e/$a996/$a992 -> $bffe) then loops 12 entries writing 0x8000 to A7-bus analog-DAC ports at 0xFFF700+2*i and reading back 0xFFF702+2*i (comparing vs -5). The disasm is an analog-DAC access, not the expected A+B trace math, and the shared PC with arg 0xFF makes the name mapping doubtful.",
    "category": "",
    "verified": false
  },
  "APNDT": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0); by name and arg type 0x02 this is most likely an append operation (trace or display-list append). Best-effort meaning only.",
    "category": "",
    "verified": false
  },
  "AT": {
    "behavior": "Copies calibration data blocks from cal-RAM (0x2FC016.. length 0x168, transferred via slot $61EC) into low working RAM $9EB6, and gated on the $bfee model check copies further blocks 0x2FCB42->$A01E and 0x2FCA1C->$A198 \u2014 loading cal/attenuator correction tables.",
    "category": "",
    "verified": true
  },
  "ATA": {
    "behavior": "Same handler as AT (0x434e6, arg 0x10): copies cal-RAM correction tables from 0x2FC0xx into working-RAM buffers $9EB6/$A01E/$A198 (the latter two gated on the $bfee model check).",
    "category": "",
    "verified": true
  },
  "ATAN": {
    "behavior": "Not arctangent: a measurement/state control bracketed by writes of 0x305 (entry) and 0x306 (exit) to sequencing reg $bffe; it manipulates flags $ab76/$ab77/$ab7f, calls sub 0x2bed8 with index 0 then 1 (gated on $a9ae/$a9b0), and sets $b1f8 bit a. The exact subsystem meaning is unresolved.",
    "category": "",
    "verified": false
  },
  "AUDSPLY": {
    "behavior": "Periodic display-state update: initializes counter $b0a8 from ($b0aa>>12)&3+1, resets $b0ac to 0xC9 (copying $b060->$b176 marker) when it reaches 0x3FF, and gated on $b1e4==0x45 / $b070 bit d / $b213 bit 6 calls redraw sub 0x409a6. Purpose inferred as a display refresh; name-to-function link is uncertain.",
    "category": "",
    "verified": false
  },
  "AUTO": {
    "behavior": "Queries whether active-function 0 is engaged (slot $5FE with D0=0) and, if so, invokes slot $616 (id 0xD) and executes slot $100 with the saved argument \u2014 an auto/coupling dispatch (takes no argument, arg_type 0x00).",
    "category": "",
    "verified": true
  },
  "AVG": {
    "behavior": "Video-averaging/detector hardware control: read-modify-writes shadow register $ada4 into distinct bit patterns (0xC600 then 0x2000 when arg==1; otherwise 0x4600 or 0x8600 selected by the sign of $adae) and writes the result to MMIO register 0xFFF740.",
    "category": "",
    "verified": true
  },
  "AXB": {
    "behavior": "Same handler as APB (0x28436, arg 0xFF): loops writing 0x8000 to A7-bus analog-DAC ports 0xFFF700+2*i and reading back 0xFFF702+2*i using a 12-entry byte offset table at 0x2842a. The disasm shows an analog-DAC access, not an 'A exchange B' trace op; shared PC and arg 0xFF make the mapping ambiguous.",
    "category": "",
    "verified": false
  },
  "BADDR": {
    "behavior": "Entry immediately branches to main body at 0x1FF94 (outside the window); the visible path stores the arg-pair to $b068, then subtracts an arg-derived scaled value from amplitude registers $b1a8 and $b1a6 (tracking sign at frame -0x38). Full behavior unresolved because the main body is out of range.",
    "category": "",
    "verified": false
  },
  "BITF": {
    "behavior": "Bit-field hardware-port output: gated on $ad4d bit0, writes control word $ad3c to the port pointed to by $ad48 and 0 to the port at $ad44. (The linked entry at 0x256DA additionally writes $ad3a, reads the port back, masks bits 0xC, and toggles $b0e4 bit9 via slots 0x2a/0x2e.)",
    "category": "",
    "verified": true
  },
  "BLANK": {
    "behavior": "Real handler at 0x30768: reads $9fbc/$9fbe and $b0d0 from RAM, runs fixed-point scaling (jsr $611c/$61ec), then clamps the result to the 0..0xFFF 12-bit range and stores it, and separately compares a value against 0x186a0 (100000). Computes a clamped display/coordinate value rather than obviously erasing anything, so the 'blank' name is not clearly supported by the code.",
    "category": "display",
    "verified": false
  },
  "BML": {
    "behavior": "Shares a table-dispatched handler at 0x28436 (with BTC/BXC/CALFLTDATAX) that seeds scratch pointers $b02e/$a996/$a992=$bffe then loops ~12 entries indexing a byte table at 0x2842a to form A7 analog-bus port addresses 0xFFF700+2n (direct YTO/DAC word ports), writing 0x8000 and reading back into a $bffe buffer, comparing against 0xFFFB(-5). An A7-bus register walk/margin self-test; per-command semantics (arg 0xFF) are not resolvable from this shared code.",
    "category": "cal-selftest",
    "verified": false
  },
  "BRD": {
    "behavior": "Stub at 0x5ED70: 'move.w #$7d00,D0; rts' \u2014 returns the constant 0x7D00 with no side effects.",
    "category": "stub",
    "verified": true
  },
  "BTC": {
    "behavior": "Same shared handler as BML at 0x28436: walks A7 analog-bus ports at 0xFFF700+2n (writes 0x8000, reads back into the $bffe scratch buffer, compares to 0xFFFB) using an index table at 0x2842a. Which specific action BTC selects (arg 0xFF) cannot be determined from the shared code.",
    "category": "cal-selftest",
    "verified": false
  },
  "BUFCLR": {
    "behavior": "Handler at 0x101C6 jumps over an inline byte table (0x101c8), tests $b204.w bit15 (returns 0 if set) else masks $b078.w with 0x7f and indexes the 0x101c8 table, returning the looked-up byte in D0. Behaves as a state/table-lookup query on $b204/$b078, not a buffer clear.",
    "category": "query",
    "verified": true
  },
  "BUFSTAT": {
    "behavior": "Handler at 0x40816 masks $b072.w with 0x24 and tests $b1f9.w bit3, then per the arg either returns $b094/$b096 or indexes a param table (base ~$b0aa via the -$4f56 offset), also consulting $b0e6.w, to produce a trace/buffer status word.",
    "category": "query",
    "verified": true
  },
  "BWR": {
    "behavior": "handler_pc 0x25FD2 falls inside the ROM boot-menu default-state template (165 words of data, per rom_annotations/MEASURE_MODE_HANDOFF), not executable code \u2014 the dispatch table mis-resolved this entry, so disassembly is garbage. By name/kind BWR is a memory/board write, but the actual handler is unverified.",
    "category": "read-write",
    "verified": false
  },
  "BXC": {
    "behavior": "Same shared A7-bus walker at 0x28436 as BML/BTC: writes 0x8000 to and reads back the direct A7 DAC/register ports 0xFFF700+2n into the $bffe scratch buffer, comparing to 0xFFFB. Per-command (arg 0xFF) meaning is not recoverable from the shared handler.",
    "category": "cal-selftest",
    "verified": false
  },
  "CAL": {
    "behavior": "Handler at 0x2FFF4 writes analog-control select/data word pairs to the 0xFFF750(select)/0xFFF752(data) register port: select 0x7D60 then data 0x12C, then select 0x5FE0 then a variable data word from the local frame \u2014 programming analog/DAC control registers as part of the calibration routine.",
    "category": "cal-analog",
    "verified": true
  },
  "CALATN": {
    "behavior": "handler_pc 0x315AA lands in a ROM data/string-table region (frequency-unit and dB-unit suffix strings; real code such as a strcmp begins at 0x315F8), so the dispatch mis-resolved this entry and the disassembly is not the handler. By name a calibrate-attenuator command, but behavior is unverified.",
    "category": "cal-atten",
    "verified": false
  },
  "CALATNDATA": {
    "behavior": "Leaf helper at 0x3D4C2 computes a cal-attenuator data block offset/size from the buffer pointers $bb4a.w and $bb4e.w minus 0x780(*2=0xF00) entries, loads index 0x211 (*2) and calls the buffer/address routine jsr $61FE \u2014 i.e. it locates the attenuator (flatness) cal-data array (arg_type 0x05, block data).",
    "category": "cal-atten",
    "verified": true
  },
  "CALFLTDATA": {
    "behavior": "Handler at 0x40DAA reads $b0aa.w, extracts (>>8)&0xF, divides by 5 and sets D0 = (that == 2) \u2014 returns a boolean state query on the $b0aa mode/filter word (related nearby code also checks $b0aa low byte and can emit error 0x4d).",
    "category": "cal-query",
    "verified": true
  },
  "CALFLTDATAX": {
    "behavior": "Routed to the shared A7-bus walker at 0x28436 (with BML/BTC/BXC): writes 0x8000 to and reads back the 0xFFF700+2n A7 DAC/register ports into the $bffe scratch buffer, comparing against 0xFFFB. The specific filter-data-X action (arg 0xFF) is not resolvable from the shared code.",
    "category": "cal-selftest",
    "verified": false
  },
  "CALMXRBIAS": {
    "behavior": "Handler at 0xC084 copies a 10-word template from ROM (PC-rel $bff4) into a stack param block, computes a span from the $c(A6)-$8(A6) args, then derives a mixer-bias cal-data address as $a914 + (arg<<10) masked to a 16-byte (0xFFFFFFF0) boundary \u2014 sets up the mixer-bias calibration data region.",
    "category": "cal-mixer",
    "verified": true
  },
  "CALMXRDATA": {
    "behavior": "Handler at 0x2E77C calls setup routines ($2bc44/$2ba64), sets $ab7f.w bit7, and gated by $ab77.w mode bits stores the arg byte into a RAM buffer indexed by the $ab82.w / $ab80.w pointers at base offset -0x564e, then clears $ab7f.7 \u2014 writes mixer calibration data bytes into the cal buffer.",
    "category": "cal-mixer",
    "verified": true
  },
  "CALTGX": {
    "behavior": "Handler at 0x20F06 dispatches on the arg (0..10) via a jump-offset table at 0x20F0E (bounds-checked through jsr $862), each case calling $20EA0 with a different flag mask in D0 (0x000, 0x800, 0x100, ...) plus the $8(A6) argument \u2014 a tracking-generator calibration subcommand selector.",
    "category": "cal-trackgen",
    "verified": true
  },
  "CAMPCOR": {
    "behavior": "Accessor family at 0xAB76 uses an indexed register port: writes a select byte (e.g. 0x84/0x88, or the arg) to 0xFFF5FC and reads/writes the data word at 0xFFF5FE (returning the high byte, or merging arg<<8 into the low byte on write) \u2014 reads/writes the amplitude-correction register(s) via the select/data port pair.",
    "category": "cal-analog",
    "verified": true
  },
  "CAT": {
    "behavior": "Handler at 0x30536 normalizes trace length $b0e6.w, sets the display-array pointer $9552.w into $b1d8/A5 (clearing the first word), processes 0x191=401 trace points via jsr $6bb0, honors $b070.w bit8 and $ab77/$ab76 flags, updates via jsr $5d4/$43c/$41e, and checks $b1e4.w (menu register) against 0x70 \u2014 redraws/refreshes the trace display array.",
    "category": "display",
    "verified": true
  },
  "CATONEV": {
    "behavior": "Boolean predicate (tag 0x10 softkey/state): returns TRUE (0xFFFF) only if bit 13 of $bc64.w is set AND the classic-command letter register $b1e4.w == 0x34 AND the hardkey-menu register $b1ee.w is 0x60 or 0x61; i.e. it tests whether the catalog display is active on a specific menu. No writes.",
    "category": "memory-card/catalog",
    "verified": true
  },
  "CATPOS4": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0, tag 0x30, arg 0x05); undocumented. From the name and the referencing DLP macro ('CATPOS' in an IF/ELSE block), best-effort meaning is memory-card catalog cursor/position handling (variant 4) \u2014 exact behavior unconfirmed.",
    "category": "memory-card/catalog",
    "verified": false
  },
  "CATREG": {
    "behavior": "Catalog/save-register select: stores D0, bounds-checks $ab7c.w (<=20) and $a9b0.w (>=0x28), clears bit0 of state byte $ab77.w, clamps the register value via helper 0x2dbea (limits to 0x270f..0xd121), writes the result to $ab8a.w and conditionally into $ab24.w \u2014 manipulating the memory-card catalog register RAM block.",
    "category": "memory-card/catalog",
    "verified": true
  },
  "CENTROID": {
    "behavior": "Reads scratch/cal-RAM word $2fc00c.l, transforms it via helper 0x1955c and writes it back, then walks the table at ROM 0x19544 to find the index whose entry first meets/exceeds the value, adds the arg offset, clamps to [1, table[0]] and returns the interpolated table value \u2014 a marker/graph centroid computation.",
    "category": "marker/measurement",
    "verified": true
  },
  "CHP": {
    "behavior": "Channel Power measurement: validates the floating-point argument (helpers 0x5af4/0x54bc plus an F-line FP op), then runs a timed measurement loop (0x6882/0x5be8/0x47fc) polling the 0x4824 time-window until bit0 is set \u2014 integrating power over the ACPBW channel bandwidth.",
    "category": "measurement",
    "verified": true
  },
  "CHPGRAPH": {
    "behavior": "Jump-table entry: loads selector D1=3, at 0x40644 pops the return frame and sets A0-A2 (param table at 0x4068a), then tail-jumps to the shared graph/measurement dispatch routine at ROM 0x3e702 which branches on the D1 selector \u2014 the channel-power graph member of that command family.",
    "category": "measurement/graph",
    "verified": true
  },
  "CLIPTITLE": {
    "behavior": "Converts the arg's low nibble to an ASCII digit ('0'+n) and writes it into the title/CLIP text buffer at $acca.w via char-output helper 0x69c8, then calls 0x26282 \u2014 building the plotter CLIP title string in RAM.",
    "category": "display/plot",
    "verified": true
  },
  "CLRAVG": {
    "behavior": "Clear-Average: resets the average/sweep counter $b0ac.w to 0x3ff, reads $b0ab.w and calls 0x19fb2, fires redraw/event notifications via 0x42a with codes 0x11/0x12 (and 0x37 if $9e50 set), and clears averaging status bits (mask 0x220e/0x2208) in status word $b1e0.w \u2014 resetting the video-averaging accumulator.",
    "category": "trace/average",
    "verified": true
  },
  "CLRBOX": {
    "behavior": "Drives the HD63484 ACRTC (command port 0xFFF5FC, data port 0xFFF5FE, status-poll bit1 of 0xFFF5FD): writes command words 0x0800/0x0801 with the arg as a coordinate, then sends the parameter block at ROM 0xb922 via 0xa91c \u2014 draws/clears a rectangular box region on the CRT.",
    "category": "display",
    "verified": true
  },
  "CLRDSP": {
    "behavior": "Clear-Display: resets the display-list pointers $b02e.w/$a996.w/$a992.w all to $bffe, then loops 12 times over the offset table at ROM 0x2842a computing MMIO addresses 0xFFF700+2*offset and writing 0x8000 to each while reading back the paired port \u2014 blanking/reinitialising the 0xFFF700-block display registers.",
    "category": "display",
    "verified": true
  },
  "CLRW": {
    "behavior": "Registered handler at 0x223a2 is a predicate returning bit4 of trace-mode word $b213.w (clear-write mode active?) as a boolean; the adjacent helper at 0x223b6 (not the entry) programs the A7 analog bus via select port $f728.w and data port $f72a.w.",
    "category": "trace",
    "verified": true
  },
  "CLS": {
    "behavior": "Gated on display-enable bit15 of $b072.w: copies a ~0x4b-word ACRTC command template from ROM 0xd78c onto the stack, patches it with graticule position/scale from $ba64.w/$ba66.w (with a /8 divide), and sends the built display command block via 0xa91c \u2014 drawing/clearing a graticule region.",
    "category": "display",
    "verified": true
  },
  "CMDERRQ": {
    "behavior": "Command-Error Query: reads the pending-error flag via syscall 0x5fe with code 0x26; if an error is pending it formats the message with 0x27972, re-reads via 0x616, and outputs the string with the jsr $100 output routine \u2014 returning the last command/syntax error.",
    "category": "error/status query",
    "verified": true
  },
  "CNF": {
    "behavior": "Confidence Test: reads the long at $b068.w/$b06c.w; if zero it patches display-status word $b1f8.w (clears mask 0x80, sets 0x180) and calls 0x20442; otherwise indexes a table by $b0c8.w and runs floating-point math (0x5ad2/0x555a plus F-line ops) on the $b060/$b068 values.",
    "category": "self-test",
    "verified": true
  },
  "CNTLA": {
    "behavior": "Documented as 'Auxiliary Interface Control Line A' (rear-panel aux interface output control line A; arg-type 0x1C). Handler did not resolve via the dispatch tables (pc=0), so the exact register write is unconfirmed.",
    "category": "io-control",
    "verified": false
  },
  "CNTLB": {
    "behavior": "Documented as 'Auxiliary Interface Control Line B' (rear-panel aux interface output control line B; arg-type 0x1D). Handler unresolved (pc=0); exact register write unconfirmed.",
    "category": "io-control",
    "verified": false
  },
  "CNTLC": {
    "behavior": "Documented as 'Auxiliary Interface Control Line C' (rear-panel aux interface output control line C; arg-type 0x1E). Handler unresolved (pc=0); exact register write unconfirmed.",
    "category": "io-control",
    "verified": false
  },
  "CNTLD": {
    "behavior": "Documented as 'Auxiliary Interface Control Line D' (rear-panel aux interface output control line D; arg-type 0x1F). Handler unresolved (pc=0); exact register write unconfirmed.",
    "category": "io-control",
    "verified": false
  },
  "CNTLI": {
    "behavior": "Sweep-mode/redraw handler: clears/sets sweep-control bits in $ab76/$ab77, dispatches to the continuous-sweep code ($2d2ee CONTS) or single-shot variant ($2d320->$2d2c2) based on $ab7f bit0, then repaints two trace vector-lists via message #$191 through the display vtables at $9556/$955e using the negated offset from $a9ac.",
    "category": "",
    "verified": true
  },
  "COMPRESS": {
    "behavior": "DLP trace-math op: loads the 64-bit operand pair from $b068 and works it through the float library ($5ad2/$5556/$5568), comparing against a per-record entry in the trace table indexed by $956a (<<3 = 8-byte records) and the operand at $b084. Concrete register/table touches are clear; exact numeric semantics are not fully pinned.",
    "category": "",
    "verified": false
  },
  "CONCAT": {
    "behavior": "DLP data/trace op: loads operand $b068 via the float lib ($5ad2/$5532/$5556), indexes a table at -0x50ca(A4) by $b0c8 ((x*2+1)<<3), and if the $b068 operand is nonzero emits display message #$190 ($6688). Registers/tables are clear; precise concat semantics not confirmed.",
    "category": "",
    "verified": false
  },
  "CONTS": {
    "behavior": "Continuous-sweep command: clears the single-sweep flag $ab77 bit4, clears sweep-mode bits in $ab76 (andi #$87ef when bit15 set), issues internal command 0x2c via jsr $42a, optionally re-triggers a sweep ($2d2c2), and sets status/annunciator bit $b1f8 bit10.",
    "category": "",
    "verified": true
  },
  "CORREK": {
    "behavior": "Correction on/off/type command: masks the correction-flag word $ab76 (andi #$1fff), and depending on the arg (correction type 1..4, gated on $b0e6>0) either dispatches via jsr $2bc or sets $ab76 bit15/bit13 after querying correction-availability helpers $2b8f8/$2b916.",
    "category": "",
    "verified": true
  },
  "CORREKFLT": {
    "behavior": "Per-channel correction-table operation on the active measurement state $9ea6 (skipped when $9ea6==-1): tests the per-channel enable flag bit6 at (-0x61ae,A4), and applies float-library correction ($5ad2, msg $6688, $79cc/$61ec) keyed on operand $b068 and index $956a. Mechanism is clear; the specific correction type is inferred from the name.",
    "category": "",
    "verified": true
  },
  "CORREKRLV": {
    "behavior": "Ref-level correction command: if $b116 bit15 is clear and helper $2f498 reports bit1, strobes analog-control MMIO 0xFFF76E with 0x0007 then 0x000F, then clamps the ref-level correction longwords $b0fe and $b102 to the range [1, 0xFFFF].",
    "category": "",
    "verified": true
  },
  "COS": {
    "behavior": "Writes the real-time-clock: sets RTC control registers 0xEF401D=0x07 and 0xEF401F=0x05, clamps the arg to <=0x17 (23), splits it to BCD tens/units via divs #10, and writes the tens digit to RTC BCD register 0xEF400B (hours field). Despite the name it drives the OKI RTC at 0xEF40xx.",
    "category": "",
    "verified": true
  },
  "COUPLEmD": {
    "behavior": "Unresolved (handler pc=0, not reachable via the dispatch tables). From the name, an auto-coupling command that couples a measurement parameter to its default/mode-dependent value; behavior not confirmed from disassembly.",
    "category": "",
    "verified": false
  },
  "CSIZE": {
    "behavior": "Toggles state-flag $b0a1 bit3 (XOR'd with the arg, then bchg), and when the operand $b068 is zero and the longword $b0a2 lies within frequency-like thresholds (0x4E20/0xC3500/0x30D40) it reprograms via short dispatches ($1ea/$178/$5d4) and checks active-command register $b1e4==3. Numeric semantics unresolved.",
    "category": "",
    "verified": false
  },
  "CTA": {
    "behavior": "Short trace dispatch: tests arg bit0 at (9,A6) and the trace-state count $b0e6 (>=1), then branches to trace routines at $d622 or $d05a. Targets fall in a table-dispatched region not resolved here.",
    "category": "",
    "verified": false
  },
  "CTM": {
    "behavior": "Bare leaf accessor at the dispatch target: writes command 0x0088 to the HD63484 ACRTC command port 0xFFF5FC and returns (0xFFF5FE>>8), i.e. reads back an ACRTC/CRT register. No link frame (unlike normal command handlers), so this may be a mis-resolved helper rather than the true CTM body.",
    "category": "",
    "verified": false
  },
  "CTMADC": {
    "behavior": "Per-channel ADC/correction-table operation on active state $9ea6 (skipped when ==-1): reads/writes the per-channel flags at (-0x61ae,A4) (sets bit7, clears bit6), and computes corrections via helpers $5dec/$609c/$4eb72/$5af4 keyed on operand $b068. Mechanism clear; ADC specifics inferred from name.",
    "category": "",
    "verified": true
  },
  "CTY": {
    "behavior": "Cal/state restore: block-copies calibration/trace tables out of cal-scratch RAM into working RAM \u2014 $2FC016->$9eb6 (length-computed via $61ec), and conditionally (gated on $bfee<=0x218f) $2FCB42->$a01e (0x3C words), $2FC91A->$a096 (0x81 words), and $2FCA1C->$a198 (0x81 words).",
    "category": "",
    "verified": true
  },
  "CTYA": {
    "behavior": "Bare leaf accessor: the dispatch target is 'move.w $94de.w,D0; rts', simply returning the measurement/ADC-cal state word $94de. Surrounding helpers clear/set $94de/$94e0/$94d6/$948a, so this looks like a mis-resolved getter rather than the full CTYA command body.",
    "category": "",
    "verified": false
  },
  "CTYB": {
    "behavior": "Shares the identical handler with CTY at 0x434e6: block-copies cal/state tables from cal-scratch RAM ($2FC016/$2FCB42/$2FC91A/$2FCA1C) into working RAM ($9eb6/$a01e/$a096/$a198), gated on $bfee<=0x218f.",
    "category": "",
    "verified": true
  },
  "DATASTAT": {
    "behavior": "Data/limit status: the entry sets mode flag $befb bit1, and the associated routine walks trace samples via the trace-buffer pointer $9552 and tests each point against the limit-line values $bb24/$bb26/$bb28/$bb2a, returning -1 (in-limit) or 0 (out).",
    "category": "",
    "verified": true
  },
  "DATEMODEVE": {
    "behavior": "Unresolved (handler pc=0, arg_type 0xFF query/set). From the name, a set/query for the date display-format mode (e.g. MDY vs DMY); behavior not confirmed from disassembly.",
    "category": "",
    "verified": false
  },
  "DDCMD": {
    "behavior": "Sets bit 9 of the mode/busy flag word $b072.w then calls the subroutine at $1a6e2 to dispatch the DD-context command; the same $b072.9 flag is later cleared by DISPOSE.",
    "category": "",
    "verified": true
  },
  "DDGET": {
    "behavior": "Reads the long at $b0a2.w and clamps it to hardware limits (ceiling 0x5F5E100 = 100e6; floored/clamped to 0x4E20 = 20000 or 0xC3500 = 800000 depending on the $b0e4.w mask 0x1100 and the $b0ec.w==0x31 model/mode check), returning the limited value.",
    "category": "",
    "verified": true
  },
  "DDMEAS": {
    "behavior": "Configures the measurement-mode bit fields in $ab76.w / $ab77.w according to the numeric arg in D0 (bit3/bit0 of $ab77 and the 0x2000/0x4004 field of $ab76), sets $ab84.w=$ab7c.w=1, then triggers a measurement via $2ba64.",
    "category": "",
    "verified": true
  },
  "DDREAD": {
    "behavior": "Sets $b214.w=7, stores the incoming address/length long-pairs into the pointer registers $b068.w (from 8(A6)) and $b060.w (from D0-D1), then calls the block-read routine $5556 and $1ee28 to read data through those pointers.",
    "category": "",
    "verified": true
  },
  "DDRECS": {
    "behavior": "Entry bra's to $407d0, which extracts the top 2 bits of $b0aa.w (lsr #6) and calls $407ca to return a small mode/record index (1..N); DEMOD uses this return value as an index into a demod-handler pointer table at $40740-0x6aba.",
    "category": "",
    "verified": false
  },
  "DDRST": {
    "behavior": "Writes the reset/enable flag $ac02.w = 0xFFFF when the arg's bit0 is set, else clears $ac02.w to 0.",
    "category": "",
    "verified": true
  },
  "DDSEND": {
    "behavior": "Gated on the active-function register $b1e4.w != 0x34, assembles an output message (params 0xAAAA / $ba82-derived offsets / vtable [$9562]+0x4e) via $ea92/$f288 and emits it through $c084, then outputs a trailing char via $316/$be22.",
    "category": "",
    "verified": true
  },
  "DDSTAT": {
    "behavior": "Reads two status getters $101c6 and $102a4, scales the result (x10, x8, and 0x1F40=8000 minus value) into screen coordinates, calls DDWRT ($c58a) when $ba6b.w bit1 is set, and tests the sweep/mode mask 0x28 of $b07a.w against $b07e.w.",
    "category": "",
    "verified": true
  },
  "DDWRT": {
    "behavior": "Reads the two getters $101c6 and $102a4 and computes scaled display coordinates (x10, 0x1F40=8000 minus product) using the calibration scale factors $ba56.w/$ba58.w and the display-mode field 0xE000 of $b0a0.w (special path when ==0xA000, divides by 7).",
    "category": "",
    "verified": true
  },
  "DEMOD": {
    "behavior": "Demodulator control: after $40dc2/$2c2 it tunes using the offset $b0ae.w through the DSP/math routines $64f0/$643e/$5080/$62a0/$651c, then calls DDRECS ($40740) to get a mode index (subq #1, lsl #2) that selects a handler pointer from the table at (-0x6aba,A4).",
    "category": "",
    "verified": false
  },
  "DERASE": {
    "behavior": "Erases the DLP user-memory object referenced by the handle in $956a.w by calling the memory-manager $5ae5c (and re-storing the returned handle); when $956e.w bit0 is clear it also posts a status message via $39a with params 0xE9/0x1D7/0xFFEA and writes ~$bef6 to $b034.w.",
    "category": "",
    "verified": true
  },
  "DISPOSE": {
    "behavior": "Entry bra's to $3824c, which frees the allocation handle $a89a.w via $316fa (if $a896.w is nonzero, then clears $a896), clears bit 9 of $b072.w (the DD-active flag set by DDCMD), and disposes each of the $a4e6.w sub-objects by calling $381e8 in a loop.",
    "category": "",
    "verified": true
  },
  "DIV": {
    "behavior": "DLP arithmetic DIV operator on user variables: looks up the operand variables in DLP variable segment 0x2000 via $57a/$aa8 (name pointers $bb54 / $a02), guarded by the operand-check $3cd6a, performing the division on the referenced DLP variables.",
    "category": "",
    "verified": true
  },
  "DLPINIT": {
    "behavior": "Menu-state predicate: compares the hardkey-menu register $b1ee.w against the menu-id set {0x1a,0x24,0x25,0x5c,0x5e,0x60,0x61} and returns (in D0) a boolean that is true only when $b1ee is NOT any of those menus (name suggests init but the handler is a menu-context test).",
    "category": "",
    "verified": true
  },
  "DLPINITWR": {
    "behavior": "DLP init-write loop: after $43670 setup it repeatedly loads the ROM template pointer $499f4 into $4330c, calls $8a4, and advances the $b068.w write-pointer pair comparing via $54ac until done (the fff8/1111 words are branch-displacement decode noise, so exact loop bounds are unresolved).",
    "category": "",
    "verified": false
  },
  "DLPTERM": {
    "behavior": "Entry bra's to $3646a, a DLP token-stream parser/terminator: reads the token byte at offset 1 of the source pointer (-4,A6), classifies it via $439a and the bitmasks $5fe056 / $48000080, and dispatches to $31f88/$5dc2/$5dec/$31a14 accordingly; clears $b20f.w bit2.",
    "category": "",
    "verified": false
  },
  "DOCATVMEAS": {
    "behavior": "CATV measurement watchdog: when $9a6 returns true and $b213.w bit0 is clear, compares the elapsed-time counter $bf12 against 0x493E0 (300000) \u2014 if exceeded it sets annunciator 0x1B via $42a and marks $b213.0, otherwise (with $9e50.w==0) it clears annunciator 0x1B via $610.",
    "category": "",
    "verified": true
  },
  "DONE": {
    "behavior": "Reads the battery-backed BCD real-time clock registers $FFEF4017/$FFEF4015/$FFEF4013, combines the nibbles and converts BCD to binary (x10 + units), comparing against 0x57 (87) / 0x63 (99) to derive a date/time field (after the $59998 setup call).",
    "category": "",
    "verified": true
  },
  "DRAWBOX": {
    "behavior": "Draws a rectangular box on the CRT: saves the graphics cursor ($b08c X-pair, $b090 Y-pair), repeatedly calls the graphics-primitive emitter $4330c with parameter block $499cc (looping up to 0x32 segments while comparing position $b0ac against 0xC9), then restores the cursor from the stack.",
    "category": "display",
    "verified": true
  },
  "DRTSETUP": {
    "behavior": "handler_pc 0x80ac4 lands in a non-code packed-data region of the 0x80000 image (raw bytes 00/11/10/04/25..., not valid M68K and not ASCII), so it cannot be confirmed as a native handler; name suggests some trace/'DRT' setup but the disassembly is meaningless.",
    "category": "data-region",
    "verified": false
  },
  "DRTYMK": {
    "behavior": "Reads the DLP/trace state-structure pointer from RAM $a7d6, tests bit0 of $956e; if the structure's state field ($16,A4) doesn't match it runs the DLP source at $5be8e via scheduler $cdc, then stamps that field = 0xFFFF (a trace Y-marker refresh/setup).",
    "category": "dlp-display",
    "verified": true
  },
  "DRTYMKCTRLtH": {
    "behavior": "Unresolved (handler_pc=0) \u2014 did not resolve through the dispatch tables; appears to be a control-suffixed variant of DRTYMK (trace Y-marker), meaning inferred only from the mangled name.",
    "category": "unresolved",
    "verified": false
  },
  "DSPBIAS": {
    "behavior": "Packs hardware-status flag bits from $b1f0/$b1f6/$b1f8/$b1fa/$b1e0 into the display/DAC control words $b084/$b08c/$b098/$b060 (and sets $b0c2 bit31 when $b1e0 bit13 is set) \u2014 the documented status/annunciator aggregation routine fcn.11B9A.",
    "category": "status-display",
    "verified": true
  },
  "DSPLY": {
    "behavior": "Clamps the incoming value to 1..0x190 (400) via $7978 and stores it to display-position register $b0ac, sets bit6 of display flag $b1f9, then triggers a display refresh (jsr $214, $9ab0, $19c).",
    "category": "display",
    "verified": true
  },
  "DSPLYA": {
    "behavior": "Formats a 32-bit value using a field/cursor index ($b234/$b235) into a local buffer at (-$30,A6) and publishes the buffer address to the display-string pointer $b224 (via formatter at $f41c); handles sign by negating and selecting the $b235 vs $b234 field.",
    "category": "display",
    "verified": true
  },
  "DSPLYPSD": {
    "behavior": "Table-dispatch entry that loads format-selector D1=3 plus descriptor table $4068a, pops the arg longword pair, and jumps to the common display/print routine $3e702 to render one formatted parameter.",
    "category": "display",
    "verified": true
  },
  "DSPYTF": {
    "behavior": "Uses the argument (D0>>4, *8) to index two low-memory longword pointer tables at $342 and $32a, runs each low-nibble selection through conversion routine $69b4, and gates output on display flag $b073 bit6 \u2014 computes/displays a tuned-filter (YTF) parameter (semantic label inferred; table-indexing behavior is from disasm).",
    "category": "analog-display",
    "verified": true
  },
  "DSPYTFI": {
    "behavior": "Reads a packed word at $41b6 (low 9 bits masked, upper via >>9), searches the ROM breakpoint table at $3fb44 with a cmp/dble loop and applies /4 (divs #4) arithmetic to derive a band/segment index for an internal YTF display computation (YTF label inferred; arithmetic/table-search is from disasm).",
    "category": "analog-display",
    "verified": true
  },
  "EDITAMPCOR": {
    "behavior": "4-way editor (selector = arg & 3) for the amplitude-correction (AMPCOR) table: indexes the active field via $b234 with lookup table $a5ec, then calls the shared edit dispatcher $1034a (EDITDONE) plus $33eba to format/commit the edited value.",
    "category": "edit",
    "verified": true
  },
  "EDITDLP": {
    "behavior": "DLP (downloadable-program) editor command taking a pointer arg: if the second byte of the string is '?' it queries via $6ef8 using DLP edit state $b0aa, otherwise it matches the token against keyword table $40694 (via $39762) and dispatches the corresponding DLP-edit sub-command.",
    "category": "dlp-edit",
    "verified": true
  },
  "EDITDONE": {
    "behavior": "Shared editor-commit dispatcher keyed on a mode value on the stack (cases 3/4/5): reads/writes the edited value pair via $6736/$6770 and formats it via $7864/$7942/$5f22; called by the other EDIT* handlers to finalize a field.",
    "category": "edit",
    "verified": true
  },
  "EDITLIML": {
    "behavior": "4-way editor (selector = arg & 3) for the lower limit-line table: indexes the active field via $b234 (table $a5ec), fetches the value from the caller's table pointer ($a,A6), and commits through the shared editor $1034a + $33eba, updating the display via $70ca/$7108.",
    "category": "edit",
    "verified": true
  },
  "EDITNAME": {
    "behavior": "Name/label editor keyed on menu-item index $956a: tests mode bits $b072 bit15 and $956e bit0, then indexes per-item name structures in RAM (base offsets -0x6604 / -0x65ec off the computed pointer) to edit the selected save-register/trace name.",
    "category": "edit",
    "verified": true
  },
  "ELSE": {
    "behavior": "DLP IF/ELSE flow-control token (shares handler 0x406ec with ENDIF): tests the DLP conditional-nesting flag $b1e0 bit12 and $b1fe == 0x12, and branches/skips the enclosed block via $4d8 accordingly.",
    "category": "dlp",
    "verified": true
  },
  "EN": {
    "behavior": "Type-0x10 state command that validates (checksum call $61ec) and block-copies calibration/state tables out of CalRAM 0x2fc016/0x2fcb42/0x2fc91a/0x2fca1c into working RAM $9eb6/$a01e/$a096/$a198 (word-copy dbra loops).",
    "category": "cal-state",
    "verified": true
  },
  "ENDIF": {
    "behavior": "DLP ENDIF flow-control token sharing handler 0x406ec with ELSE: closes an IF block by testing the conditional-nesting flag $b1e0 bit12 and $b1fe == 0x12 and resuming or skipping execution via $4d8.",
    "category": "dlp",
    "verified": true
  },
  "ENTER": {
    "behavior": "Handler at 0x4F5A4 masks two argument fields (stack (-2,A6) and (8,A6)) to 0-7, bounds-checks both <=3 (fail branch 0x4F62C), calls helper 0x4E8AE with pointer args, then computes an indexed offset D6=((2*arg+1)<<3) into A4 and dispatches via jsr 0x5AF4.w. A data-entry terminator that validates two 2-bit selectors and vectors through an indexed table.",
    "category": "",
    "verified": true
  },
  "EP": {
    "behavior": "Handler at 0x406EC tests menu-active bit 12 of $b1e0.w and checks current-menu id $b1fe.w==0x12; only when both hold does it jsr 0x4D8.w. Conditionally invokes the parameter-edit routine when a specific menu context is active.",
    "category": "",
    "verified": true
  },
  "ERASE": {
    "behavior": "Handler at 0x28436 (shared verbatim with EXP) initializes RAM pointer cells $b02e/$a996/$a992 to $bffe, then loops 12 times over a PC-relative byte table at 0x2842A building 0xFFF700-block addresses (0xFFF700 + 2*table[i]) \u2014 the A7 analog-I/O register bank. Sets up a table of analog-port register pointers; note it does NOT touch the display, and ERASE and EXP resolve to the same code.",
    "category": "",
    "verified": true
  },
  "ERR": {
    "behavior": "Query handler at 0x3122E calls 0x306DA then inspects error-state RAM: bit 10 and bit 13 of $b0d6.w, $b0ec.w==0x2d, and the OR of longs $b068/$b06c, returning an error code in D0 (0x2d, 1, or 0). Reports the current error/status number.",
    "category": "",
    "verified": true
  },
  "EVNTCNT": {
    "behavior": "Handler at 0x401C0 takes abs(arg), computes 0x191(401)-arg, and copies words through the display-list pointer $9552.w over the 401-point (0x190/0x191) trace array \u2014 a shift/roll of the trace buffer by an argument-derived offset. Operates on the 401-sample display array via the $9552 buffer pointer.",
    "category": "",
    "verified": true
  },
  "EVNTSTRT": {
    "behavior": "Handler at 0x17346 clears bit13 of $befa.w, copies $bf3c->$bf3e, then sets the low nibble of sweep-control word $b20c.w to 4 and writes it to the sweep MMIO register 0xFFF300.w before calling 0x640/0x5A4 and testing $a9a0.w. Arms/triggers a sweep (event start) by writing mode 4 to the 0xFFF300 sweep register.",
    "category": "",
    "verified": true
  },
  "EXITCAT": {
    "behavior": "Handler at 0x42ED2 calls 0x42EA2 then loads D0=8 and jsr 0x702E.w (a display/menu-state routine). Restores the display state on leaving the catalog (selector 8).",
    "category": "",
    "verified": true
  },
  "EXITDLPEDIT": {
    "behavior": "Handler at 0x5DF48 read-modify-writes the mode word $b0f0.w \u2014 clears the 3-bit field (mask 0x1c, via AND 0xFFE3) and inserts (arg<<2 & 0x1c) \u2014 then calls redraw routine 0x5DEA8. Sets a display/mode field in $b0f0 and refreshes, exiting the DLP editor.",
    "category": "",
    "verified": true
  },
  "EXP": {
    "behavior": "Handler at 0x28436 \u2014 identical shared code with ERASE: seeds RAM pointer cells $b02e/$a996/$a992 to $bffe and builds a 12-entry table of 0xFFF700-block A7 analog-I/O register addresses (0xFFF700 + 2*table[i]) from the PC-relative table at 0x2842A. Not an exponent math op; it programs the analog-port register-pointer table.",
    "category": "",
    "verified": true
  },
  "EXTOED": {
    "behavior": "Query handler at 0x11DA8 computes a boolean in D0 by combining state flags: bit13 of $befa.w, whether $bb6c.w != $bb6e.w, whether $bb64.w < 0, and bit9 of $b1e0.w. Returns a composite true/false status derived from those RAM cells.",
    "category": "",
    "verified": true
  },
  "FACTSET": {
    "behavior": "Handler at 0x3E3E6 fetches an argument array via jsr 0x2F2.w (count at -0xA, pointer at -0x8), computes a reference value via 0x3E276, then loops accumulating sum of squared deviations (D5=elem-ref; D5*=D5; D6+=D5) and divides by count-1 \u2014 a variance/standard-deviation computation over the array. The disassembled arithmetic is a statistics primitive and does not match a 'factory preset' name.",
    "category": "",
    "verified": true
  },
  "FDSPLY": {
    "behavior": "Handler at 0x59EF0 (after a bra past a mis-disassembled jump table) takes a buffer pointer arg in A4, extracts the high nibble of a byte (lsr #4), ORs 0x30 to make an ASCII digit, and stores it into the buffer at (1,A4). Formats numeric bytes into an ASCII display string.",
    "category": "",
    "verified": true
  },
  "FFT": {
    "behavior": "Entry realigns to 0x331CC (link A6,#-4); it indexes an array pointer arg (8,A6) by (n-1)*2 to read a word offset, extracts a 5-bit field (lsr #8, and 0x1f) into the (0x10,A6) output, and forms a pointer at DLP-record base $a50.w + offset stored to (0xC,A6), then iterates count 3. This is a table/record-decode against the $a50 DLP region; the higher 'FFT' meaning is not confirmed by the code.",
    "category": "",
    "verified": false
  },
  "FFTAUTO": {
    "behavior": "Handler at 0x3C4C0 loads D0=0, pushes the original arg, and tail-calls shared routine 0x3A374 (the sibling handler at 0x3C4D2 calls the same routine with a menu-string pointer). Dispatches to 0x3A374 with selector 0 \u2014 an auto/on-off style setter.",
    "category": "",
    "verified": true
  },
  "FFTCONTS": {
    "behavior": "Handler at 0x2DD3C calls 0x2DC60 with selector 0, then pushes two word args (8,A6)/(0xA,A6) and calls 0x2DCAE (the sibling at 0x2DD64 is identical but selector 1). Dispatches to the shared 0x2DC60/0x2DCAE pair with mode 0 \u2014 the continuous variant.",
    "category": "",
    "verified": true
  },
  "FFTKNL": {
    "behavior": "Handler at 0x5CC0E does link A6,#-4, stashes A0, and immediately branches to shared routine 0x5DD76 (not in the disassembled window); the fall-through code at 0x5CC18 is a separate DLP-text scan loop reached only by other callers. The actual FFTKNL behavior lives in the unresolved 0x5DD76 routine.",
    "category": "",
    "verified": false
  },
  "FFTMKR": {
    "behavior": "Handler at 0x3E1D0 fetches an argument array via jsr 0x2F2.w (count at -0xA, pointer at -0x8), and if count>1 pushes count/pointer and calls 0x6C26.w, returning that result+1 (else 0). A query that searches the array (via 0x6C26) and returns a 1-based index/marker position.",
    "category": "",
    "verified": true
  },
  "FFTMM": {
    "behavior": "Handler at 0x391D0 does link A6, saves D0, and branches to shared routine 0x3926E (outside the window); the fall-through at 0x391F0 is a separate 0x7F-entry table scan (comparing bytes at A4-0x5AB4 against $a4e6.w) belonging to another caller. The FFTMM behavior itself is in the unresolved 0x3926E routine.",
    "category": "",
    "verified": false
  },
  "FFTMS": {
    "behavior": "Handler at 0x40D86 checks measurement-state var $b1fe (==0x12 -> no-op), then tests FFT flag byte $b0ab and emits marker char 'M' (0x4D) via syscall $184 when clear, or outputs the $b0ab value via syscall $76C otherwise. Reads FFT/marker state; no DAC/ADC touched.",
    "category": "",
    "verified": true
  },
  "FFTOFF": {
    "behavior": "Handler at 0x4067C loads selector D1=0x0D and tail-jumps to the shared FFT engine routine 0x3E702, which stores the selector at (-0x2E,A6), calls $2F2 and branches on FFT sub-mode; D1=0x0D selects the FFT-disable path. Mechanical dispatch confirmed; the 'off' semantic is name-inferred.",
    "category": "",
    "verified": true
  },
  "FFTSNGLS": {
    "behavior": "Handler at 0x2BA16 parses its arg via 0x32C22, calls the selector-0x193 routine $7978, then block-copies words into the ACRTC display list pointed to by [$9556]; if $ab76 bit 14 is set it sets bit 10 of status word $b1f8. FFT single-sweep result blit.",
    "category": "",
    "verified": true
  },
  "FLTERRE": {
    "behavior": "handler_pc did not resolve via the dispatch tables (pc=0); tag 0x40 / arg_type 0x10 marks it a softkey/state-type command. Name suggests a filter or flatness error state query/report, but no disassembly supports this.",
    "category": "",
    "verified": false
  },
  "FLTINIT": {
    "behavior": "Handler at 0x128D0 reads and clamps parameter $b0e6 (takes abs value, clamps to <=0x28, defaults to 0xFFF1 = -15 when out of range); when positive it pushes selector 0x191 and updates the display list at [$9552] via $6BB0, clears bit 8 of $b212, and masks the arg to its low nibble. Filter-parameter initialization.",
    "category": "",
    "verified": true
  },
  "FMGAIN8": {
    "behavior": "handler_pc did not resolve via the dispatch tables (pc=0); tag 0x30 / arg_type 0x07. Name suggests an FM-gain calibration setting, but no disassembly is available to confirm.",
    "category": "",
    "verified": false
  },
  "FMSPAN": {
    "behavior": "Handler at 0x320FE reads a struct via the arg pointer at (0xC,A6) (skips when the long at +0x80 is <=0), derives an index D3 from D0 (clamped >= -10), and computed-jumps (jmp (0xC,PC,D3.w)) into an unrolled word-summation over samples at A2 \u2014 accumulates a span-dependent number of points (FM-span data averaging).",
    "category": "",
    "verified": true
  },
  "FORCECNT": {
    "behavior": "Handler at 0x32062 calls the selector-0x2A routine $6A98 to obtain a count, then loops comparing longwords from args (0xC,A6) vs (0x10,A6), calling subroutine 0x32038 per element \u2014 a bounded count/force loop over a value range (frequency-counter forcing).",
    "category": "",
    "verified": true
  },
  "FORMAT": {
    "behavior": "Handler at 0x5DE94 is a predicate returning SNE of bit 24 of NOT($bf2a) \u2014 reports a format/status flag from the $bf2a event longword. The adjacent setter at 0x5DEA8 loads params $b0f0/$b0f2/$b0f4 into MMIO DAC data port $F768 using register-selects 0xC6/0x4F/0x8F/0xCF written to $F76A.",
    "category": "",
    "verified": true
  },
  "FPKEY": {
    "behavior": "Handler at 0x306C6 is a predicate returning SNE of bit 18 of NOT($bf2a) \u2014 reports a front-panel-key event flag from the $bf2a status longword (adjacent variants at 0x306DA/0x306EE test bits 18/19 and OR them).",
    "category": "",
    "verified": true
  },
  "FUNCDEF": {
    "behavior": "Handler at 0x11DD4 returns bit 0 of CalRAM $2FC013 as a boolean. The actual user-function definition logic is the adjacent routine at 0x11DF4, which clamps the index to 0-9, multiplies by 0x65 and indexes the per-function record table at CalRAM 0x2FC136.",
    "category": "",
    "verified": true
  },
  "GAINDISP": {
    "behavior": "Handler at 0x5F150 calls predicate $5ECA2; when its bit 0 is set it runs gain processing ($5E88C, then selector-2 $5E84A), and on that bit 0 writes display-enable word $94E0=0xFFFF and calls $5E384 with 0x92, else $5E364 \u2014 updates the gain/reference-level display.",
    "category": "",
    "verified": true
  },
  "GAINTWEAK": {
    "behavior": "Handler at 0x4282E parses two longword args via 0x32C22; when they are equal it computes a log2 shift-count (repeated lsr.w counting shifts) and calls syscall $178 passing the A5 trace pointer \u2014 an IF-gain calibration tweak.",
    "category": "",
    "verified": true
  },
  "GATE": {
    "behavior": "Handler at 0x3F362 tests the arg char at (0xB,A6) for '?' (0x3F) query, otherwise searches a 4-entry char table at 0x3F36A ('A','I','B','M') against state byte $b039 to select a gate mode, emitting mnemonic tokens via $6EF8/$7108. Time-gating mode set/query.",
    "category": "",
    "verified": true
  },
  "GATESEL": {
    "behavior": "handler_pc 0x800E4 lands inside a data/token table in the 0x80xxx region that disassembles as ASCII HP-IB mnemonic pairs ('O1 M3 MS O2 M4 MT GZ ...'), not executable code, so the true gate-source-select handler is not at this address and cannot be confirmed.",
    "category": "",
    "verified": false
  },
  "GC": {
    "behavior": "handler_pc 0x801A6 lands in the same 0x80xxx ASCII/token-table region and disassembles as garbage/data rather than a code handler, so its behavior cannot be resolved from the mapped address.",
    "category": "",
    "verified": false
  },
  "GDRVCLPAR": {
    "behavior": "DLP trampoline at 0x6B93A pushes source id 0x2BE and source ptr 0x6B67C and calls the DLP scheduler $D18; the macro computes the gate-drive clock period __T from gate pulse width __GGTPWID (__T=1/__GGTPWID, switching to 10/__GGTPWID when __T<=5E6) inside a REPEAT loop \u2014 gate-drive clock parameter.",
    "category": "",
    "verified": true
  },
  "GDRVSWAP": {
    "behavior": "DLP trampoline at 0x6B1EC pushes source id 0x177 and source ptr 0x6B074 and calls scheduler $D18; the macro sets __K=__G-5 and __L=__G+5, bounds-checks both to the trace-point range [1,400], then plots \u2014 positions/swaps gate-drive markers +-5 points around __G.",
    "category": "",
    "verified": true
  },
  "GDg": {
    "behavior": "Handler did not resolve (pc=0); the mixed-case name plus slot 4098 (duplicated with GLh) marks this as a spurious/garbled parser-name-table extraction artifact rather than a real command. tag=0x30, arg=0x09.",
    "category": "unresolved",
    "verified": false
  },
  "GETPLOT": {
    "behavior": "Native handler (tag 0x10): copies the plot/output argument token from the arg string into a stack buffer at (-$2c,A6), tests for empty/'?' (0x3f), calls a token lookup (jsr $6090), range-checks the result to 0..0x31, then pushes 0xffff and dispatches into $17a64 to drive the HP-GL plotter output.",
    "category": "other",
    "verified": true
  },
  "GETPRNT": {
    "behavior": "handler_pc=0x0809cc lands in the 0x08xxxx pointer/data-table region (bytes decode as $0008xxxx longwords, not executable code), so the native disassembly is not valid and behavior cannot be confirmed; by name/tag (0x20) it configures/queries printer output.",
    "category": "other",
    "verified": false
  },
  "GETT": {
    "behavior": "Handler did not resolve (pc=0). tag=0x30, arg=0x00; name suggests a 'get trace/time' query but no code or DLP source backs it, so meaning is a best-effort guess.",
    "category": "unresolved",
    "verified": false
  },
  "GLh": {
    "behavior": "Handler did not resolve (pc=0); mixed-case name and slot 4098 (shared with GDg) indicate a garbled/duplicate parser-name-table entry, not a genuine command. tag=0x30, arg=0x09.",
    "category": "unresolved",
    "verified": false
  },
  "GPkP": {
    "behavior": "Handler did not resolve (pc=0); mixed-case name and out-of-range slot 8194 mark it as a spurious parser-name-table extraction artifact. tag=0x30, arg=0x00.",
    "category": "unresolved",
    "verified": false
  },
  "GRAT": {
    "behavior": "Handler unresolved via the dispatch tables (pc=0) though the command is documented (tag 0x10, arg 0x19); by name it is the graticule on/off display control, but this cannot be confirmed from disassembly.",
    "category": "other",
    "verified": false
  },
  "GRC": {
    "behavior": "handler_pc=0x08029e falls inside the 0x08xxxx pointer-table region (contents are $00080b00.. longwords back-referencing the same segment, not code), so the 'native' disassembly is garbage and the behavior is unverifiable. tag=0x30, undocumented.",
    "category": "other",
    "verified": false
  },
  "GRDLYNRM": {
    "behavior": "handler_pc=0x08075e resolves into the 0x08xxxx non-code data/pointer region, so no valid handler disassembly exists; the name suggests a group-delay-normalize operation but it cannot be confirmed. tag=0x30, undocumented.",
    "category": "read/write",
    "verified": false
  },
  "GSTAT": {
    "behavior": "Native status query: if the arg (D0) is 5 or '?' (0x3f) it returns the byte at RAM $b235.w, otherwise it returns the byte at $b234.w in D0 \u2014 a simple read of an instrument status byte.",
    "category": "other",
    "verified": true
  },
  "HAVE": {
    "behavior": "Native state-capture handler: compares queue pointers $bb96/$bb98, clears $b216, copies the 8-byte state block at $b03e into the $bb82 save buffer, and stores the active-function context registers $b1e4\u2192$bb8a, $b1fe\u2192$bb8c, $b1b8\u2192$bb8e (then conditionally post-processes via $165a8/$12b10 on $b1e1/$b1e0 flags).",
    "category": "other",
    "verified": true
  },
  "HN": {
    "behavior": "Native handler (real code at 0xa5f6): masks the arg to arg&3 and uses it to index a small offset table at 0xa5ec, reading the status byte $b234.w and dispatching to one of several number/HP-IB output routines ($1034a, $70d2, $7068, $708a) to format/emit that value.",
    "category": "other",
    "verified": true
  },
  "HNLOCK": {
    "behavior": "Native handler: clears bits 3-5 of $bc64.w (andi #$ffc7), sets up a softkey/menu context via jsr $6bb0 with A0=$bc2c and index 0x1c, then stores menu pointer $be02\u2192$bc3c and 0xf4\u2192$bc3a \u2014 installing the harmonic-number-lock menu/state.",
    "category": "other",
    "verified": true
  },
  "HNUNLK": {
    "behavior": "Native handler: copies DLP include-stack pointer $a90c\u2192$a90a and calls $32298 with A0=$bb54 and $bf66 pushed, manipulating the DLP interpreter's saved include/context stack to undo the lock state.",
    "category": "other",
    "verified": true
  },
  "HP": {
    "behavior": "Native handler: pushes the current DLP record ((A4) from $a68), the name-table base $a02, and the arg word, then calls $32b70 and tail-jumps through the return address \u2014 a DLP dispatch wrapper invoking a name-table routine with the current record context.",
    "category": "other",
    "verified": true
  },
  "ICOMCMD": {
    "behavior": "Native handler: parses the arg (pea (8,A6); bsr $31bfc) into a D0-D1 pair, range-checks the arg against 0xf, and conditionally emits one of two command constants (loaded from $37b36 / $37b3e) via jsr $5414 \u2014 sending a command word to an internal-communication/IF path.",
    "category": "other",
    "verified": true
  },
  "ICOMFRQ": {
    "behavior": "Native handler: sets bit0 of flag $b20f (saving prior $b224), stores the arg pointer into $b224, reads the active-function class $b1e4 and formats a value (jsr $316, $7a8), then divides $b1e4 by 6 (divs #6) and compares to 3 \u2014 configuring an internal-comm frequency parameter.",
    "category": "read/write",
    "verified": true
  },
  "ID": {
    "behavior": "handler_pc=0x0807f0 points into the 0x08xxxx pointer-table region (contents are $00080f5c.. longwords, not code), so the disassembly is not a valid handler; by name/tag (0x30, documented) this is the standard instrument identification query, but that cannot be confirmed here.",
    "category": "other",
    "verified": false
  },
  "IDLET": {
    "behavior": "Circular-decrements a 16-bit index at struct-offset +0x14 (pointed to by A0) by the command's word arg: if index>=arg it just subtracts arg, otherwise it subtracts arg and wraps by adding the field length at +0x0e, then stores back to +0x14. Advances/wraps a character position within an ID/label field.",
    "category": "buffer-index",
    "verified": true
  },
  "IDNUM": {
    "behavior": "Parses a numeric token via jsr $2f2 (result ptr in D0, element count in local -$0e,A6), then iterates over its 16-bit words (negating negatives) to build/format an ID number. A number-token accessor.",
    "category": "token-parse",
    "verified": true
  },
  "IDSET": {
    "behavior": "Computes (($b0aa.w & 0x0f)/5); on the zero case tests $b071.w bit3, calls the menu/table lookup $60a, indexes a pointer table at (-$6aba,A4), and uses $b0ac (offset by 0x11) via $7978 to select/set an ID/menu state entry. Table-driven state setter.",
    "category": "menu-state",
    "verified": false
  },
  "IEKMODE": {
    "behavior": "Clears bit0 of the command's arg byte (local -1,A6) then writes the 16-bit arg word through the pointer arg into *(-$4,A6)->A4. A generic store-word-through-pointer accessor (sets an internal mode word).",
    "category": "accessor",
    "verified": true
  },
  "IF": {
    "behavior": "Six-instruction conditional: tests bit12 of the mode-flags word $b1e0.w and whether $b1fe.w == 0x12, and only if both hold calls subroutine $4d8; otherwise returns. A state-gated conditional primitive.",
    "category": "conditional",
    "verified": true
  },
  "IFTKNL": {
    "behavior": "IF-token lookup: if the token index $b0c8.w is zero it loads default longwords from PC table $20d7e, else uses A4=$b0c8*4 to index tables at (-$5fba/-$5fa6/-$5f92,A4) and runs fixed-point math routines $6736/$62b6/$62f4 to compute an IF (intermediate-frequency) value.",
    "category": "freq-math",
    "verified": true
  },
  "INITMXRDATA": {
    "behavior": "Init mixer data: checks the mode $b1e4.w>>1 == 0x0c, then via the measurement/menu struct at [$9562.w] reads offsets +0x50/+0x4e and calls $316 and the routine at $b3f4 to (re)initialize the mixer-parameter block.",
    "category": "init",
    "verified": true
  },
  "INLJTPLT": {
    "behavior": "After jsr $6ef8 (setup) it repeatedly loads stored longword parameters ($9ebc, $9f5c, $9ec0, $9ec4, ...) and emits each with the fixed 3-call sequence jsr $668a / $2c8 / $70ca (size 0x18), serializing a block of state longwords to an output/plot template.",
    "category": "serialize",
    "verified": true
  },
  "INT": {
    "behavior": "Reads the A7 analog-bus status (bsr $2267c, tests bit0) and $b212 bit8 to gate execution, then sets $b1e0 bit11, invokes fixed-point op $610c with 0x0a, builds a value into local -$2a,A6, and dispatches through the vector table at $22d90 indexed by $b0ee.w. An integrate/measurement function.",
    "category": "measurement",
    "verified": true
  },
  "INTVLSTRT": {
    "behavior": "Interval-start dump: after jsr $6ef8 it streams a long sequence of interval/gated-sweep config bytes and words ($9f64, $9fd6/$9fd7, $9f68-$9f6b, $9eda, $9edc, $9ede, ...) each through the emit helper $4d89c, serializing the interval-sweep parameter set.",
    "category": "serialize",
    "verified": true
  },
  "INTVLTIME": {
    "behavior": "Interval-time setter with a small 4-entry jump-offset table (0x12/0x18/0x1e/0x26 at $21132) dispatched via jsr $862; sets up via $21068 and calls the value routines $210f4/$21030 with masks 0x40/0x20 to configure the time-interval (gate/sweep-time) mode.",
    "category": "setter",
    "verified": false
  },
  "KB": {
    "behavior": "Calls the keyboard-routing/select helper $224c0 with 0 (which masks $ad7c.w), then transforms the arg through $22b56 (fixed-point via $22afe/$5842) and $22b8c. Touches the keyboard-input routing register $ad7c but the exact numeric transform is only partially resolved.",
    "category": "keyboard-num",
    "verified": false
  },
  "KBDTOCMD": {
    "behavior": "Real code starts at 0x331cc (a data prologue precedes it). Decodes a keyboard-table entry (base=arg ptr (8,A6)) into a 5-bit command token (word at index, >>8 & 0x1f, stored to *(0x10,A6)) and a DLP record pointer computed from the record-region base $a50.w plus the entry offset (stored to *(0xc,A6)). Maps a keypress to a DLP command record.",
    "category": "keyboard-route",
    "verified": true
  },
  "KBDTOPRFX": {
    "behavior": "Copies the two pointer args ((8,A6) and (0xc,A6)+4) through the pointer-store helper $3d32c, routing a keyboard entry's prefix pointer pair. A keyboard-to-prefix routing setter.",
    "category": "keyboard-route",
    "verified": true
  },
  "KBDTOSCRN": {
    "behavior": "Saves $ad7c->$ada2, sets or clears bit5 of the keyboard-routing register $ad7d and loads the input vector $ad76 from $9f48 or $9f4c per the arg's bit0, calls $224c0($ad7c), and on a bit5 change clears $b212 bit8 and refreshes $b0c8. Routes front-panel/keyboard entry to the screen.",
    "category": "keyboard-route",
    "verified": true
  },
  "KBDTOTITLE": {
    "behavior": "Real code from ~0x1a256 (a short data table precedes it): checks the title-buffer state ($bff0 vs 'G'/0x47, $bfee threshold 0x2192), sets flag $afce, copies $b0ec->$b008, clamps the sweep time $b0a2 to 0x61a80, and calls display routines at $17346 (x2) \u2014 routes keyboard entry into the title with associated sweep-time/display setup.",
    "category": "keyboard-route",
    "verified": true
  },
  "KC": {
    "behavior": "handler_pc 0xc470 is SHARED by KC, KD and KE all with sentinel arg_type 0xFF, indicating the dispatch tables did not resolve these to distinct handlers. The code at 0xc470 is a 7-entry param-table copy loop (from $b30e) followed by an unrelated marker/graph routine at 0xc49c, so it is not a reliable KC-specific handler.",
    "category": "unresolved",
    "verified": false
  },
  "KD": {
    "behavior": "Aliased to the same fallback handler_pc 0xc470 as KC and KE (all arg_type 0xFF), so its true handler is unresolved. The 0xc470 code (7-word table copy from $b30e, then marker/graph code at 0xc49c) is not KD-specific and should not be taken as its behavior.",
    "category": "unresolved",
    "verified": false
  },
  "KE": {
    "behavior": "Shares handler 0xc470 with KEYCLR/KLI/KLIF/LIMIBOT (arg_type 0xFF, a sentinel); the routine is a 7-iteration init loop that copies word entries from ROM table $b30e into a RAM state array near $ba96 and clears the companion cells near $baa4. The 5-name-to-1-PC collision plus 0xFF arg means the name-to-handler mapping is a fallback, not a distinct command.",
    "category": "unresolved-alias",
    "verified": false
  },
  "KEYCLR": {
    "behavior": "Same handler as KE (0xc470): the code re-initializes a 7-entry RAM word array (loads from ROM table $b30e near $ba96, clears near $baa4), consistent with a clear/reset of key or limit state, but five command names collapse onto this one PC with sentinel arg 0xFF so the specific attribution is unreliable.",
    "category": "unresolved-alias",
    "verified": false
  },
  "KEYCMD": {
    "behavior": "Writes control word #$c600 to output cell $ada4, checks readiness via $20dd6, then reads five key-definition bytes from $9f64 and $9f68-$9f6b and emits each through helper $20ea0 with field codes ($300/$0/$800/$100/$200) \u2014 it assembles and transmits a multi-field key command packet.",
    "category": "keyboard",
    "verified": true
  },
  "KEYDEF": {
    "behavior": "Clears longwords $9ffa/$9ffe, fetches the arg, then based on model/mode cell $bfee (vs #$2190) selects one of two ROM tables ($42e1a or $42d36) and loads their longwords into key-definition parameters $9f48/$9f4c; for $bff0>0x43 it substitutes the float constant #$4ccccd01 \u2014 it programs model-dependent key-definition scaling constants.",
    "category": "keyboard",
    "verified": true
  },
  "KEYENH": {
    "behavior": "Handler_pc 0x3afb6 pushes constant 0 and calls $3adac, which selects key-label buffer $a360 (arg 0) or $a370, validates a per-key flag at (-$5ca2,A4), and parses/copies the argument string (via length routine $7978) into the buffer+2 \u2014 it stores an enhanced key label/definition string.",
    "category": "keyboard",
    "verified": true
  },
  "KEYEXC": {
    "behavior": "Fetches the arg, sets flag byte $9544=0xFF, calls $5aa1c with the arg, stores the menu/key id into DLP user-menu register $956a, then indexes descriptor tables at (-$6604,A4) and (-$65ec,A4) using arg*3-derived offsets \u2014 it selects/executes a user-defined softkey/menu entry.",
    "category": "softkey-menu",
    "verified": true
  },
  "KEYLBL": {
    "behavior": "Reads menu/key coordinates $ba7c/$ba7e, calls the table-search routine $37e8e (KL) to get an index, and if found indexes the label-pointer table via $a5e0 and the label-byte table via $a5e8 to extract the key's label substring (testing for byte 0x63 'c') \u2014 it retrieves a key's on-screen label text.",
    "category": "key-label",
    "verified": true
  },
  "KEYNUM": {
    "behavior": "Handler_pc 0x21b80 disassembles to a data table, not code \u2014 a monotonically increasing byte sequence with stride 6 (0x0c,0x12,0x18,...), i.e. a 6-byte-stride index/offset table. The dispatch did not resolve to executable code, so the behavior is unresolved.",
    "category": "unresolved-data",
    "verified": false
  },
  "KFACT": {
    "behavior": "For arg>0 computes (arg-1), divides by 6, and uses the remainder*2 to index ROM table $10152, passing the value to $5b00 and then (arg-1)/6-1 to $687a \u2014 an engineering-unit/scale-factor selector that divides the index by 6 to pick a units table entry.",
    "category": "units-scaling",
    "verified": true
  },
  "KH": {
    "behavior": "Handler_pc 0x0b606 disassembles to ROM constant data (runs of dc.w $fffc, $000a, etc.), not executable code \u2014 the dispatch landed in a data table, so no handler behavior can be attributed.",
    "category": "unresolved-data",
    "verified": false
  },
  "KL": {
    "behavior": "Linear-searches the word table pointed to by $a5dc for an entry equal to the arg, incrementing the index until a match or until it reaches the count in $a5d8, and returns the index in D0 (0xffff if not found) \u2014 a key/label lookup-by-value helper (also called by KEYLBL).",
    "category": "key-label",
    "verified": true
  },
  "KLI": {
    "behavior": "Same shared handler 0xc470 as KE/KEYCLR (arg_type 0xFF sentinel): re-initializes the 7-entry RAM word array from ROM table $b30e (near $ba96, clearing $baa4). Five names collide on this one PC, so the specific KLI attribution is a fallback and not verified.",
    "category": "unresolved-alias",
    "verified": false
  },
  "KLIF": {
    "behavior": "Same shared handler 0xc470 as KE/KEYCLR/KLI (arg_type 0xFF sentinel): the routine is the 7-entry RAM state-array init (ROM table $b30e -> ~$ba96, clears ~$baa4). The many-to-one PC mapping makes the KLIF-specific behavior unverified.",
    "category": "unresolved-alias",
    "verified": false
  },
  "KRUNCH": {
    "behavior": "Drives the HD63484 ACRTC display controller directly through the command port 0xFFF5FC and data port 0xFFF5FE: writes register selects $82/$88 and reads back pointer bytes, tests flag $ba87.0, then writes drawing parameters (#$e8 with #$8900 data, then $98) \u2014 a display-controller register read/program sequence.",
    "category": "display",
    "verified": true
  },
  "LBU": {
    "behavior": "After validating the arg via $306c6, builds a control word from (arg<<12) masked with $880/$3c80, ORs it into the hardware-control shadow $b0ea, and writes $b0ea to MMIO port 0xFFF754 with strobe #$fffe to 0xFFF756; for arg==3 it also clears bit7 of $b0eb \u2014 it sets band/attenuator-style analog control bits.",
    "category": "analog-control",
    "verified": true
  },
  "LIMDK": {
    "behavior": "Unresolved (handler_pc=0): the dispatch tables did not yield a handler. Name and the LIM* family suggest a limit-line command (plausibly limit-line delete/key) with arg_type 0x0E, but nothing in the disassembly confirms it.",
    "category": "unresolved",
    "verified": false
  },
  "LIMIBEEP": {
    "behavior": "Handler 0x3cd6a is a predicate that returns true when the arg falls within one of four value bands (1-0x0f9c, 0x1f9f-0x270c, 0x36af-0x464a, 0x55ed-0x6588) via Sge/Sle tests; the caller at 0x3cdb6 uses that result to emit a #$2000 tone through $57a \u2014 an in-range check gating a limit-line beep.",
    "category": "limit-line",
    "verified": true
  },
  "LIMIBOT": {
    "behavior": "Same shared handler 0xc470 as the KE/KEYCLR/KLI/KLIF group (arg_type 0xFF sentinel): the 7-entry RAM state-array init from ROM table $b30e (~$ba96, clears ~$baa4). The five-name-to-one-PC collision makes the LIMIBOT-specific behavior a fallback and unverified.",
    "category": "unresolved-alias",
    "verified": false
  },
  "LIMIDEL": {
    "behavior": "Native limit-line-editor handler: calls status routine $346de and, if bit0 set (editor active), writes a 3-word ASCII label ($4b45/$5945/$5843) into buffer $a964.w then dispatches jsr $6688 with the argument word from (-2,A6) \u2014 deletes/updates the current limit segment and refreshes its softkey label.",
    "category": "",
    "verified": true
  },
  "LIMIDISPlR": {
    "behavior": "Unresolved (handler_pc=0, not reached via the dispatch tables); by name a limit-line DISPLAY on/off toggle for the limit-line trace, but no handler disassembly confirms it.",
    "category": "",
    "verified": false
  },
  "LIMIDONE": {
    "behavior": "Native handler shared at $c470 with LIMIEDIT/LIMINEXT/LIMIPURGE/LIMISDEL: a 7-iteration loop (D6=6, dbra) that copies 7 words from ROM table $b30e into RAM array at $BA96 and zeroes the parallel 7-word array at $BAA4 \u2014 a generic limit-editor softkey/menu-state init; this address does not distinguish the individual commands.",
    "category": "",
    "verified": true
  },
  "LIMIEDIT": {
    "behavior": "Native handler, same $c470 shared routine as LIMIDONE \u2014 copies 7 init words from ROM $b30e into RAM $BA96 and zeroes $BAA4 (limit-editor menu/state init); the per-command distinction is not encoded at this PC.",
    "category": "",
    "verified": true
  },
  "LIMIFAIL": {
    "behavior": "Native handler at $c056: selects a limit-fail test config by the argument \u2014 if D0==0 copies 8 bytes from ROM $bfec into RAM $bad8, if D0==1 copies 8 bytes from ROM $bfe4 into $bad8 (installs one of two limit-test-mode parameter blocks).",
    "category": "",
    "verified": true
  },
  "LIMIFTsS": {
    "behavior": "Unresolved (handler_pc=0); by name a limit-line segment flat/type selector, but no resolved handler so the behavior is a guess.",
    "category": "",
    "verified": false
  },
  "LIMIHALF_U": {
    "behavior": "Unresolved (handler_pc=0); name suggests a 'half up' limit-editor operation, but no handler disassembly is available to confirm.",
    "category": "",
    "verified": false
  },
  "LIMIHI": {
    "behavior": "Native handler at $348b4: calls segment-add routine $34644 (increments limit-segment counter $a634.w, max 0x28) and, on success (D0 bit0), copies a 10-byte high-limit parameter block (2 longs + 1 word) from a PC-relative ROM constant into RAM $a62a.w \u2014 appends/sets the upper limit-line segment.",
    "category": "",
    "verified": true
  },
  "LIMILINE": {
    "behavior": "Trampoline running a DLP macro that ABS-normalizes value __H into a [1,1000) mantissa while tracking exponent __E in a FOR loop (__E bounded -18..12) \u2014 engineering-notation scaling used to format/enter a limit-line value.",
    "category": "",
    "verified": true
  },
  "LIMILO": {
    "behavior": "Native handler at $321be: derives an index D6=((D0>>8)+D0)&0x1f from the argument, indexes the word table pointed to by low-mem pointer $a32.w, and computes the difference between entry[idx] and entry[idx+1] \u2014 a parameterized table lookup selecting the lower limit-line segment value.",
    "category": "",
    "verified": true
  },
  "LIMIMIRROR": {
    "behavior": "Trampoline running DLP macro 'IF(__MN[23]==2);MKA;ELSE;MKRACTV;ENDIF;' \u2014 branches on menu-state variable __MN[23] to select marker action MKA versus MKRACTV when mirroring the limit line.",
    "category": "",
    "verified": true
  },
  "LIMINEXT": {
    "behavior": "Native handler, same $c470 shared routine \u2014 copies 7 init words from ROM $b30e into RAM $BA96 and zeroes $BAA4 (limit-editor menu/state init); this shared PC does not encode a distinct 'next segment' action.",
    "category": "",
    "verified": true
  },
  "LIMIPURGE": {
    "behavior": "Native handler, same $c470 shared routine \u2014 reloads the 7-word init table from ROM $b30e into RAM $BA96 and zeroes the parallel $BAA4 array, consistent with purging/re-initializing the limit-line editor state; per-command distinction not visible at this PC.",
    "category": "",
    "verified": true
  },
  "LIMISAV": {
    "behavior": "Trampoline running a DLP macro that saves limit/graphics state: on __V==__X it calls __GTMAP/HD, sets the graphics-save flag __GTSPF=1, and registers an ONCMD GL on-command handler \u2014 persists the current limit-line/graphics setup.",
    "category": "",
    "verified": true
  },
  "LIMISDEL": {
    "behavior": "Native handler, same $c470 shared routine \u2014 copies 7 init words from ROM $b30e into RAM $BA96 and zeroes $BAA4 (limit-editor state init); the specific 'segment delete' action is not distinguishable at this shared address.",
    "category": "",
    "verified": true
  },
  "LIMISEG": {
    "behavior": "Trampoline running a DLP macro: computes __GGTRE=ABS(__GTTTM) and, when nonzero, issues CLRBOX 1,211,300,221,0 to clear the on-screen limit-line segment-editor box region before redraw.",
    "category": "",
    "verified": true
  },
  "LIMISEGT": {
    "behavior": "handler_pc=0x2611c falls inside the boot-menu per-slot init template data region (disassembles as a nonsensical 'ori.b' stream), so the dispatch resolves to a data table, not executable code \u2014 the real limit-segment-table handler is not established here.",
    "category": "",
    "verified": false
  },
  "LIMISIZE": {
    "behavior": "Native handler at $3b384: a thin wrapper that pushes the argument and calls common limit-parameter setter $3b27e with selector D0=3; $3b27e supports a '?' query (first char 0x3f) and otherwise parses the numeric value via $3a642 \u2014 sets the limit-line 'size' parameter.",
    "category": "",
    "verified": true
  },
  "LIMLI": {
    "behavior": "Unresolved (handler_pc=0): did not resolve through the master/secondary dispatch tables and is not in the documented 859x command set. Name prefix suggests the limit-line editing family (cf. LIMILINE); arg_type 0x0E, handler not recovered.",
    "category": "unresolved",
    "verified": false
  },
  "LIMMJ": {
    "behavior": "Unresolved (handler_pc=0): no handler recovered via the dispatch tables and no documented mnemonic match. Likely a limit-line-family parser-table entry (LIM* prefix) or a garbled name-table artifact; arg_type 0x0E.",
    "category": "unresolved",
    "verified": false
  },
  "LIMTb": {
    "behavior": "Unresolved (handler_pc=0): not resolvable through the dispatch tables and absent from the documented command set. Name hints at the limit-test/limit-line family (LIMT*, cf. LIMITEST); arg_type 0x09, handler unknown.",
    "category": "unresolved",
    "verified": false
  },
  "LIMUH": {
    "behavior": "Unresolved (handler_pc=0): no handler found via dispatch, not documented. Name suggests a limit-line 'upper' variant (LIMU*, arg_type 0x0E); best-effort only, behavior unverified.",
    "category": "unresolved",
    "verified": false
  },
  "LINEPTN": {
    "behavior": "Reads a string argument at (-4,A6), string-matches it against ROM table $41d5c via jsr $6a52, then ORs limit/graph state words $b0aa/$b0b0/$b0b6/$b0bc (and tests $b0a8); when set and $bef8 bit10 is on it sets redraw-request bit1 of $a5d5 and issues display subcommand 0x4c via jsr $184.",
    "category": "",
    "verified": true
  },
  "LINFILL": {
    "behavior": "Thin wrapper (link/bsr $2d2c2/unlk/rts) that calls $2d2c2, which block-fills the graph trace-plane with HD63484 raster word 0xAAAA (subcmd 8 via jsr $39a, region params 0xAF/0x18F), then clears flag $ab7e and refreshes the display via $76c/$62e.",
    "category": "",
    "verified": true
  },
  "LOAD": {
    "behavior": "Trampoline whose DLP source is 'TEXT~ Inactive ~;NV' \u2014 it merely draws the text 'Inactive', i.e. a disabled/placeholder macro rather than an active state-load routine.",
    "category": "stub",
    "verified": true
  },
  "LOG": {
    "behavior": "7-iteration loop (D6=6..0) that copies a 7-word table from ROM $b30e into RAM at $FFBA96 (2*i offsets) and zeroes the companion RAM array at $FFBAA4 (2*i) \u2014 reinitializes a 7-entry scaling/parameter table (log-amplitude/graticule scale state).",
    "category": "",
    "verified": true
  },
  "LOGCOR": {
    "behavior": "Trampoline running DLP 'MOV __GGTMN,242;__GTWINTRK;' \u2014 assigns 242 to DLP variable __GGTMN and invokes __GTWINTRK (gate-window tracking).",
    "category": "",
    "verified": true
  },
  "LOGGRAT": {
    "behavior": "Trampoline that installs ONCMD command hooks (DLP): ST->__GTUNCS and GD/GL/GATE->__GTPRGT, so those commands re-trigger the graticule/gate redraw routines (log-graticule setup).",
    "category": "",
    "verified": true
  },
  "LSPAN": {
    "behavior": "Trampoline running DLP 'IF(__SOENBL);IF(NDBPNT);__NDBRUN;ELSE;DLPINIT;DLPTERM;ENDIF;ENDIF;NV' \u2014 if the source is enabled and an N-dB-point is set it runs __NDBRUN, otherwise it re-inits/terminates the DLP.",
    "category": "",
    "verified": true
  },
  "MBRD": {
    "behavior": "Dispatch stub: loads selector D1=-1 (moveq #-1,D1) and jumps to the shared block-access routine $3e702, which fetches the argument array via jsr $2f2 and branches on the selector. The specific -1 (read) branch is not traced here.",
    "category": "memory",
    "verified": false
  },
  "MBWR": {
    "behavior": "Table-dispatch handler: after skipping an inline 7-entry longword table at $17fbc it indexes that table by a sub-index (A4=4*idx), jumps via jsr $100, and feeds DLP-source pointers 0x193b0/0x193b6 into the interpreter ($100/$6d0). Exact operation not resolved.",
    "category": "memory",
    "verified": false
  },
  "MDS": {
    "behavior": "Unresolved (handler_pc=0): not resolvable via the dispatch tables and not present in the documented command set (arg_type 0x1A). Best-effort meaning unknown; behavior unverified.",
    "category": "unresolved",
    "verified": false
  },
  "MDU": {
    "behavior": "Trampoline running a DLP window-layout macro: WINOPEN __X,0,120,400,90 opens a 400x90 window and WINITM/WINANNOT lines lay out annotation items \u2014 builds a display/menu panel.",
    "category": "",
    "verified": true
  },
  "MEAN": {
    "behavior": "Trampoline with DLP 'IF(__GTPFLG==1);__GTWID;ELSE;IF(__GTPFLG==-1);__GTPRIZ;ENDIF;ENDIF;' \u2014 dispatches on the gate-parameter flag __GTPFLG, calling __GTWID when 1 or __GTPRIZ when -1.",
    "category": "",
    "verified": true
  },
  "MEANTH": {
    "behavior": "Trampoline DLP trace-math macro: computes N=K-J+1, copies trace A segment TRA[__J,__K] into scratch TRD, finds the peak index via PKPOS, and computes amplitude __Y=9000-TRD[peak] (mean/threshold measurement over a marker-bounded trace region).",
    "category": "trace-math",
    "verified": true
  },
  "MEASOFF": {
    "behavior": "Native handler at 0x3ec9a: if arg count>1 it fetches an array descriptor (ptr in (-8,A6), count in (-a,A6)) via jsr $2f2, and \u2014 guarded by a $aea bit-test \u2014 stores the 0xFFFF blank sentinel into element index (count+1) before finalizing with jsr $9ee, blanking a measurement/trace array entry.",
    "category": "",
    "verified": true
  },
  "MEM": {
    "behavior": "Native handler links, then stores the command argument (D0) into RAM word $9b22 (0xFF9B22) and returns \u2014 a one-line setter for a memory/mode selector variable.",
    "category": "",
    "verified": true
  },
  "MENU": {
    "behavior": "Unresolved (pc=0, arg 0x12): did not resolve through the dispatch tables; by name it selects/activates a softkey menu by ID. Cannot confirm from disassembly.",
    "category": "unresolved",
    "verified": false
  },
  "MENULBS": {
    "behavior": "Native; calls shared menu-state handler 0x3b27e with selector 5, which sets/queries bit 5 of the menu-label enable bitmask at RAM $bef8 (0xFFBEF8), and on the '?' query path returns the computed menu-label state to $bf1a.",
    "category": "",
    "verified": true
  },
  "MERGE": {
    "behavior": "Native; parses a secondary keyword byte (accepts 'H'=0x48 or 'N'=0x4e, else raises syntax error via 0x184), then resolves a trace designator through 0x40740, indexes the trace-pointer table (RAM base -0x6aba), sets up display op 0x8300 and reads sweep length $b0ac to merge trace arrays.",
    "category": "",
    "verified": true
  },
  "MIN": {
    "behavior": "Trampoline running a DLP macro that computes a temp __T from the pulse-width variable __GGTPWID (e.g. __T=1/__GGTPWID or 10/__GGTPWID with conditional branches) followed by a REPEAT loop \u2014 a pulse-width-derived scaling calculation in a measurement personality.",
    "category": "trampoline",
    "verified": true
  },
  "MINH": {
    "behavior": "Trampoline running the adjacent-channel-power (ACP) measurement macro chain: __ACPSUD, __M3, __ACPSUM, __ACPICALC, __ACPANTIC, __ACPANLBL, __ACPERR plus a KEYCMD \u2014 computes and annotates ACP results (name suggests min-hold but the DLP source is the ACP sequence).",
    "category": "trampoline",
    "verified": true
  },
  "MINPOS": {
    "behavior": "Trampoline running an FFT/measurement DLP macro gated on the FFT state flag __FFTSTF and __S, which inspects measurement-state fields via MSFIELD(37,15,2)/MSFIELD(...) and conditionally issues MKTRACE TRB.",
    "category": "trampoline",
    "verified": true
  },
  "MIRROR": {
    "behavior": "Trampoline that DLPINITs the __NDBTERM (N-dB-down termination) routine, sets flag __NDBRFLG=1, re-enters MENU 50 if already there, runs __SOONRUN, and registers ONCMD ANNOT/handlers \u2014 sets up an N-dB-down bandwidth/mirror-marker measurement.",
    "category": "trampoline",
    "verified": true
  },
  "MKA": {
    "behavior": "Unresolved (pc=0, arg 0x11): not resolvable via the dispatch tables; by name a marker-A / marker-amplitude accessor. Cannot confirm from disassembly.",
    "category": "unresolved",
    "verified": false
  },
  "MKACT": {
    "behavior": "Native; copies stored state blocks out of cal-scratch RAM (~0x168 bytes from 0x2FC016 to $9eb6, plus 0x3c words from 0x2FCB42 to $a01e and 0x81-word blocks from 0x2FCA1C/0x2FC91A into $a096/$a198) \u2014 restores the active instrument/trace state from the 0x2FCxxx CalRAM buffer.",
    "category": "",
    "verified": true
  },
  "MKACTV": {
    "behavior": "Native; parses two operands via the arg-parser 0x2f2, calls marker/trace op 0x6bb8, and on a returned flag writes 0xFFFF into the trace/marker array (pointer at frame -0x8) at element index (arg1+1)*2 \u2014 marks an entry active.",
    "category": "",
    "verified": true
  },
  "MKBW": {
    "behavior": "Native thin dispatcher: pushes the argument and tail-calls the shared marker-bandwidth engine 0x3a374 with selector D0=0 (N-dB-down / marker-bandwidth mode 0); the real work is inside 0x3a374.",
    "category": "",
    "verified": true
  },
  "MKCF": {
    "behavior": "Native (arg 0xFF, no operand); runs a 7-iteration loop copying default words from ROM table 0xb30e into the marker/center-freq register block at RAM 0xFFBA96 and zeroing the companion words at 0xFFBAA4 \u2014 resets the marker-to-CF register set (the same $ba96.. registers the CF-set logic at 0xc49c writes).",
    "category": "",
    "verified": true
  },
  "MKCONT": {
    "behavior": "Trampoline running the display/window re-init macro: EM; ANNOT0 (annotation off); DISPOSE8049 (dispose window 8049); WININIT; ANNOT1 (annotation on); then registers ONCMD ANNOT and ONCMD WINZOOM handlers.",
    "category": "trampoline",
    "verified": true
  },
  "MKF": {
    "behavior": "Unresolved (pc=0, arg 0x15): not resolvable via the dispatch tables; by name a marker-frequency set/query. Cannot confirm from disassembly.",
    "category": "unresolved",
    "verified": false
  },
  "MKFC": {
    "behavior": "Native; gated on flag $bc36 (and checks $bc67 bit1), it emits a marker/frequency-counter readout string via char-output routine 0x712 \u2014 the inner loop outputs each byte from the RAM buffer at base -0x41fe, wrapping bytes with control codes 0x0e/0x40/0x0f when needed.",
    "category": "",
    "verified": true
  },
  "MKFCR": {
    "behavior": "Unresolved (pc=0, arg 0x1F): not resolvable via the dispatch tables; by name it sets the marker/frequency-counter resolution. Cannot confirm from disassembly.",
    "category": "unresolved",
    "verified": false
  },
  "MKMIN": {
    "behavior": "Trampoline running a short DLP macro '__GTCLPAR;NV' \u2014 invokes the __GTCLPAR macro (get/clear marker parameter) then NV; likely a marker-to-minimum action (source is short and its trailing token may be truncated).",
    "category": "trampoline",
    "verified": true
  },
  "MKNOISE": {
    "behavior": "Unresolved (handler_pc=0, slot 0xFFFC); did not resolve through the dispatch tables. By the conventional HP 8590 name this is the Marker Noise command \u2014 switches the active marker to noise-density readout (per-Hz normalized noise measurement).",
    "category": "",
    "verified": false
  },
  "MKOFF": {
    "behavior": "DLP trampoline (0x69C26: push src #$23, jsr $d18 scheduler); resolved macro snippet 'KH /CPL VBW|ON' is a softkey-label/handler ('KH') definition for a CPL-VBW ON/OFF key driving the __GTCPLV video-bandwidth-coupling flag. Snippet is truncated and its VBW-coupling content does not match the conventional 'marker off' meaning.",
    "category": "",
    "verified": false
  },
  "MKP": {
    "behavior": "Unresolved (handler_pc=0, slot 0xFFFD, arg 0x16). By the HP 8590 name this is Marker Position \u2014 places the active marker at a specified horizontal trace point/position.",
    "category": "",
    "verified": false
  },
  "MKPK": {
    "behavior": "DLP trampoline (0x6750A: push src #$0E, jsr $d18); resolved macro snippet '__ACPS;__ACPM;NV' chains ACP (adjacent-channel-power) DLP subroutines (__ACPS/__ACPM...). The macro content is ACP-measurement setup, not the classic marker-peak search implied by the name, and is truncated.",
    "category": "",
    "verified": false
  },
  "MKRACTV": {
    "behavior": "Native but resolves to the shared slot-3 fallback at 0x00C470 (arg=0xFF, one of six tag-0x10/0x30/0x40 commands landing here) \u2014 a 7-iteration loop copying words from ROM table ~$B30E into a RAM state block (dest A4-0x456A) and clearing (-$455C,A4), i.e. a generic softkey/state init, not a distinct handler. By name: make the active/reference marker current (delta reference).",
    "category": "",
    "verified": false
  },
  "MKREAD": {
    "behavior": "DLP trampoline (0x6438A: push src #$64, jsr $d18); resolved macro reads marker amplitude MKA and writes reference level RL with an amplitude-units correction: AUNITS==3 -> MPY RL,MKA,1.059; AUNITS==4 -> MPY RL,MKA,1.122; else ADD RL \u2014 a coherent, complete unit-dependent marker-amplitude-to-RL computation.",
    "category": "",
    "verified": true
  },
  "MKRL": {
    "behavior": "Native but resolves to the shared slot-3 fallback handler at 0x00C470 (arg=0xFF; generic 7-word ROM->RAM table-copy/clear init loop, not a distinct handler). By the HP 8590 name: Marker -> Reference Level (set ref level to the active marker's amplitude).",
    "category": "",
    "verified": false
  },
  "MKSP": {
    "behavior": "Native (tag 0x10) but resolves to the shared slot-3 fallback at 0x00C470 (arg=0xFF; generic table-copy/clear softkey-state init loop, not a per-command handler). By name: Marker -> Span.",
    "category": "",
    "verified": false
  },
  "MKSS": {
    "behavior": "Native (tag 0x10) but resolves to the shared slot-3 fallback at 0x00C470 (arg=0xFF; the generic 7-word table-init loop shared with 5 other commands). By name: Marker -> Step Size (set CF step size from the marker).",
    "category": "",
    "verified": false
  },
  "MKSTOP": {
    "behavior": "Native (tag 0x40) but resolves to the shared slot-3 fallback at 0x00C470 (arg=0xFF; the generic softkey-state table-copy init loop, not a distinct handler). By name: Marker -> Stop frequency.",
    "category": "",
    "verified": false
  },
  "MKTRACE": {
    "behavior": "Native handler at 0x3E160: fetches an operand vector via jsr $2f2 (count in (-$a,A6), ptr in D0), then computes the 64-bit sum-of-squares of its N words (muls.w D3,D3; add.l/addx.l into D4:D5) and returns it in D0/D1 \u2014 a power/RMS-style reduction. This does not match the conventional marker-to-trace-select meaning, so the table resolution is suspect.",
    "category": "",
    "verified": false
  },
  "MKTRACK": {
    "behavior": "Unresolved (handler_pc=0, slot 0xFFFB, arg 0x13). By the HP 8590 name this is Marker Track / signal-track ON/OFF \u2014 re-centers the signal at the marker each sweep by adjusting center frequency.",
    "category": "",
    "verified": false
  },
  "MKTYPE": {
    "behavior": "Native handler at 0x5A126: fetches an operand vector via jsr $2f2 into A4, bounds-checks index args at (8,A6)/(a,A6)/(c,A6) against the count, then interpolates \u2014 (c,A6) - vector[(8,A6)-1] left-shifted 16 and divided via jsr $6200 by (a,A6) \u2014 a fractional/scaled lookup into the fetched array. This array-interpolation behavior does not match the conventional 'marker type' meaning, so the resolution is suspect.",
    "category": "",
    "verified": false
  },
  "MN": {
    "behavior": "Unresolved (handler_pc=0, slot 0xFFF4, arg 0x12). Per the repo's KEYBOARD_MAP work (e.g. hardkey macro '...CF;MN25;') MN is the DLP menu-select command MN<n> that switches the softkey menu to menu number n.",
    "category": "",
    "verified": false
  },
  "MNSV": {
    "behavior": "Native but resolves to the shared slot-3 fallback at 0x00C470 (arg=0xFF; the generic 7-word ROM->RAM table-copy/clear softkey-state init loop shared with the marker commands, not a distinct handler). Undocumented; likely a state save/select command (menu- or min-hold-save) but its true handler is unresolved.",
    "category": "",
    "verified": false
  },
  "MOD": {
    "behavior": "Native getter at 0x209D4: btst #15,$ADC4.w \u2014 if the invalid/overflow bit is set returns D0=-1 (0xFFFF), otherwise returns the 16-bit value at RAM $B0C8.w. Mechanically a conditional accessor of $B0C8; the command's higher-level purpose is not evident from this stub.",
    "category": "",
    "verified": true
  },
  "MODEC": {
    "behavior": "Unresolved (handler_pc=0, slot 0x1004, arg 0x16); did not resolve through the dispatch tables and has no conventional HP 8590 documentation. Undocumented mode-related command; meaning unknown.",
    "category": "",
    "verified": false
  },
  "MOV": {
    "behavior": "DLP trampoline (0x69BEE: push src, jsr $d18); resolved macro 'IF(__GTCPLV==0);MOV GDRVVBW,1;ELSE;MOV GDRVVBW,0;ENDIF;' sets the GDRVVBW (video-bandwidth coupling drive) flag to the inverse of the __GTCPLV coupled-VBW state \u2014 a complete, self-consistent assignment macro.",
    "category": "",
    "verified": true
  },
  "MPRST": {
    "behavior": "Loops 7 times copying default words from the ROM table at $b30e into measurement-state RAM around $ba96 and clearing the companion state words around $baa4 (indices computed as idx*2 off signed displacements -$456a/-$455c); a measurement-preset/reset of 7 state entries.",
    "category": "native",
    "verified": true
  },
  "MPY": {
    "behavior": "Runs DLP macro KEYCMD39 with a %IF(LG) guard: if LOG scale is active for the measurement it holds (HD) and posts the message 'LIN Scale can not be used for this meas.' \u2014 a measurement key-command guard, not an arithmetic op (name misleading).",
    "category": "trampoline",
    "verified": true
  },
  "MRD": {
    "behavior": "Gated on $b218; sets bit 3 of $b20f as a request flag, then validates the current window params $a62e/$a62c/$a630 against the ROM template at 0x348ac and, if they match, restores $a630 from $a632 and calls 0x34690.",
    "category": "native",
    "verified": true
  },
  "MRDB": {
    "behavior": "Pops an integer count arg via the DLP arg-fetch jsr $2f2; if >0 it sets up via bsr 0x3e160 (using the pointer at (-4,A6)) and emits a formatted numeric result through the output-helper chain jsr $5df0/$64f0/$6016/$5e36/$6060/$6802; returns 0 otherwise.",
    "category": "native",
    "verified": true
  },
  "MSBIT": {
    "behavior": "Loads D1 = 0x0C (a measurement field/bit index) and jumps to the shared handler 0x3e702, which pops a DLP arg via $2f2 and reads/writes measurement-parameter state keyed by D1; the exact effect of field 0x0C is not resolved from this stub.",
    "category": "native",
    "verified": false
  },
  "MSFIELD": {
    "behavior": "Initializes a 0x20 counter and scans a measurement-field/marker list via helpers 26ef4/26a2a/26b5a, reading the pointer $ac78 and accumulating $ac7c into a running total (-$10,A6) while tracking the flags $ac76 and bit 4 of $acc9; a field/band accumulate.",
    "category": "native",
    "verified": true
  },
  "MWR": {
    "behavior": "When gated-trace-control (__GGTRCTL==1) it computes the gated time marker (__GTTTMKR) and moves the marker frequency MKF into the DLP variable GDRVGTIM, then re-defines that variable (UDEFPV).",
    "category": "trampoline",
    "verified": true
  },
  "MWRB": {
    "behavior": "Adjacent-channel-power macro: runs __ACPSUD setup, blanks the trace (TRC-8000), does VIEW TRC and marker M1, then invokes __ACPCG to compute/graph the ACP result.",
    "category": "trampoline",
    "verified": true
  },
  "MXM": {
    "behavior": "Annotation/marker cleanup macro: disposes the one-shot handler (DISPOSE ONEOS), restores the ANNOT on-command, clears __AMRFLG, sets PCTAMR to -100, and clears the display (CLRDSP).",
    "category": "trampoline",
    "verified": true
  },
  "MXMH": {
    "behavior": "Thin wrapper that pushes a mode flag of 1 and the command's 16-bit word arg (8,A6) and calls the shared routine bsr 0xeedc (a value/percentage handler), returning via the jmp (A0) tail-return trick.",
    "category": "native",
    "verified": true
  },
  "NBWDAC": {
    "behavior": "Gated-trace window macro (name misleading, not a DAC write): under __GGTRCTL==1, if window vars __V==__Y it swaps windows (__GTWINSWAP), then moves the gate scale __GTGSCL into the DLP variable GDRVSWP and re-defines it.",
    "category": "trampoline",
    "verified": true
  },
  "NITS": {
    "behavior": "Copies calibration/state tables out of the 0x2FC000 scratch cal-RAM into low working RAM: block $2fc016\u2192$9eb6, and (guarded by a build-rev check on $bfee) $2fcb42\u2192$a01e (60 words), $2fc91a\u2192$a096 and $2fca1c\u2192$a198 (129 words each); a cal-table state restore.",
    "category": "native",
    "verified": true
  },
  "NNSTLV": {
    "behavior": "ACP results readout macro: issues VRD (variable-read/query) on a series of adjacent-channel-power result variables \u2014 ACPPWRTX, ACPMAX, ACPLOWER, ACPUPPER, ACPMKA, ACPERR, CHPW... \u2014 to report them over the bus.",
    "category": "trampoline",
    "verified": true
  },
  "NPHASE": {
    "behavior": "Computes a boolean predicate (returns D0=0/1): guarded on $9fb4>1, gets a value via bsr 0x104ba, tests bit 15 of $adc4, indexes a table via $b0c8, compares against $b0ac, and gates the result on bit 13 of $b1e0, bit 10 of $b072 and $b0c2 \u2014 a trace/phase-condition flag.",
    "category": "native",
    "verified": true
  },
  "OBW": {
    "behavior": "Occupied-bandwidth config setter: validates the numeric arg (0..0xFFF) then selects a target RAM word by the 2nd char of the parameter string \u2014 'C'\u2192$b1ac, 'F'\u2192$b1aa, 'S'\u2192$b1b0 \u2014 and writes the value into it.",
    "category": "native",
    "verified": true
  },
  "OFF": {
    "behavior": "Resolves to the SAME dispatch slot 3 / handler 0xc470 as MPRST (a name collision, arg 0xFF); that code loops 7 times loading defaults from ROM $b30e into state RAM around $ba96 and clearing companions around $baa4, so OFF's true dedicated handler is unresolved.",
    "category": "native",
    "verified": false
  },
  "OFSTTWEAK": {
    "behavior": "Active-window offset macro: reads WINACTIVE? into __WN_VAR_4 and, if it is not the ID-only window (__WN_IDO), moves the video-average VAVG \u2014 adjusts the active window's video-average/offset (source truncated).",
    "category": "trampoline",
    "verified": true
  },
  "ON": {
    "behavior": "Menu/softkey enable: gets the arg count via bsr 0x5b16a, matches the arg against the DLP user-menu register $956a, then walks the menu-state table at ~(-$6604,A4) and sets the matching entry's state word to 3 (on).",
    "category": "native",
    "verified": true
  },
  "ONCMD": {
    "behavior": "Native handler: the query form (2nd char '?') emits a token via the output routines (jsr $6ef8/$7108/$708a/$70d2); the command form parses one argument and stores a 2-bit code in bits 14-15 (mask 0xC000) of config word $b0aa.w, arming which trigger the ON-event fires on.",
    "category": "",
    "verified": true
  },
  "ONCYCLE": {
    "behavior": "Trampoline running a DLP macro that clamps measurement-menu variable __MN[18] to the range 1..4, calls __PKFMB, and conditionally updates FFT/marker state when FFTSTAT is active.",
    "category": "",
    "verified": true
  },
  "ONDELAY": {
    "behavior": "Trampoline DLP macro that validates the adjacent-channel-power bandwidth against span: resets ACPERR, then increments it when ACPBW exceeds SP or falls below SP/100.",
    "category": "",
    "verified": true
  },
  "ONEOS": {
    "behavior": "Trampoline end-of-sweep DLP macro that installs __FFTKEYS, disables ANLGPLUS when measurement field MSFIELD(67,15,1) is clear, and forces the sample detector (DET SMP).",
    "category": "",
    "verified": true
  },
  "ONIP": {
    "behavior": "Trampoline DLP macro that declares and initializes window-manager state variables (__WN_ACT=0, __WN_IDO=-1, __WN_IDA=-1, __WN_VAR_1=0, ...).",
    "category": "",
    "verified": true
  },
  "ONMKR": {
    "behavior": "Trampoline on-marker DLP macro managing the gate/graph-drive priority variable: copies GDRVPRI into __GGTPRI when __GGTRCTL==1, takes its absolute value, and redefines GDRVPRI.",
    "category": "",
    "verified": true
  },
  "ONMKRU": {
    "behavior": "Trampoline marker-update DLP macro scaling marker position by the 401-point trace length (__T=401*__GGTPRI) and choosing a plot coordinate (100 or 130) from __Z before plotting (PU).",
    "category": "",
    "verified": true
  },
  "ONMODEX": {
    "behavior": "Native menu/mode-execute handler: loads the active-window struct pointer at $a7d6, sets bit 13 of $bc64.w, and dispatches on the hardkey register $b1e4 (==0x34) and mode index $b056.w through the $bb54 vtable (fcn.58f42).",
    "category": "",
    "verified": true
  },
  "ONPWRUP": {
    "behavior": "Trampoline on-power-up DLP macro that sets ACP menu variable __ACPMN=215 and invokes ACP single-measure mode (ACPSNGLM).",
    "category": "",
    "verified": true
  },
  "ONSRQ": {
    "behavior": "Trampoline on-SRQ DLP macro: when __SOENBL is set, it runs __AMRUN if PCTAM is true, otherwise re-initializes the DLP (DLPINIT/DLPTERM).",
    "category": "",
    "verified": true
  },
  "ONSWP": {
    "behavior": "Native sweep-state handler: copies sweep-mode byte $b05d->$b05c, snapshots sweep counter $bbbc.w, then jump-table dispatches sweep subroutines (fcn.544b0/fcn.543d8) that update $bbbc/$bbba and clear bit 5 of sweep-flag word $befd.w.",
    "category": "",
    "verified": true
  },
  "ONTIME": {
    "behavior": "Trampoline on-timer DLP macro that sets gate menu variable __GGTMN=245, holds (HD), and runs the gate-window-tracking routine __GTWINTRK.",
    "category": "",
    "verified": true
  },
  "OP": {
    "behavior": "Trampoline TOI (third-order-intercept) init DLP macro: DLPINIT with __TOITERM, sets __TOIRFLG=1, refreshes MENU 50, runs __SOONRUN, and arms an ONCMD annotation command.",
    "category": "",
    "verified": true
  },
  "OR": {
    "behavior": "Native handler shared with PAN at 0x434e6; the code is a state/recall routine that copies saved cal-state blocks from cal-RAM (0x2fc016/0x2fcb42/0x2fc91a/0x2fca1c/0x2fcb1e/0x2fc010/0x2fc037) into working RAM ($9eb6/$a01e/$a096/$a198/$a29a) and status words $b1fa/$b1f6/$b1e0 \u2014 the OR-specific meaning is not distinguishable from the shared block-copy code.",
    "category": "",
    "verified": false
  },
  "OUTDAC": {
    "behavior": "Trampoline whose DLP source is window-manager marker handling, not DAC output: clears MENU when MN==202 and, when __WN_ACT is set, copies the active marker (ZMKCP -1,__WN_IDA) and reads its value into __A \u2014 the OUTDAC name is misleading.",
    "category": "",
    "verified": true
  },
  "OUTPUT": {
    "behavior": "Native handler that reads a status/config word from the $b060.w-based table at [$b060 + reg_arg*2], extracts a bitfield defined by two arguments (start bit and width via a 0xFFFF>> mask and shifts), and returns the extracted field through the output routine (jsr $77e).",
    "category": "",
    "verified": true
  },
  "PAN": {
    "behavior": "Native handler with the same PC as OR (0x434e6) \u2014 the shared cal/state-recall routine that restores saved blocks from cal-RAM 0x2fc0xx into working RAM and status words $b1fa/$b1f6/$b1e0; arg_type 0x10 (vs OR's 0x11) selects the variant, but PAN-specific behavior is not visible in the shared code.",
    "category": "",
    "verified": false
  },
  "PARSTAT": {
    "behavior": "Native parser-status query (PARSTAT?): copies the trailing command-name string into a local buffer, space-terminates it, looks it up via fcn.95e against the $bb54 parser vtable, and on success outputs a status string (ROM constant $3d144) through fcn.6ef8/7154.",
    "category": "",
    "verified": true
  },
  "PAVGGRAPH": {
    "behavior": "Trace-averaging-graph reference helper (DLP trampoline, src @0x6e422): when the graph-control variable __GGTRCTL==1 it copies the drive reference GDRVREFE into __GGTRE, applies ABS, and redefines the processed-value variable GDRVREFE via UDEFPV \u2014 maintains the reference used by the power/averaging graph.",
    "category": "",
    "verified": true
  },
  "PDA": {
    "behavior": "Native handler at 0x3b3fe: reads window/measure-mode state $b0e6.w (>0) and $b0ec.w (==0x31 test) and dispatches with a selector (1/2/6) either through the low-mem vector jsr $2bc.w or helper bsr $3b27e. The actual action is behind the unresolved $2bc.w table.",
    "category": "",
    "verified": false
  },
  "PDF": {
    "behavior": "Engineering/SI number formatter (DLP trampoline, src @0x71294): normalizes value __T with exponent __E in steps of 3, DSPLYs the mantissa, then appends the SI prefix char (p/n/'.'=micro/m/?) and the 'S' seconds unit \u2014 formats a time value for on-screen display.",
    "category": "",
    "verified": true
  },
  "PEAKS": {
    "behavior": "Short DLP trampoline (src @0x6d2e0): if __V==__Y it calls __GTWINSWAP to swap the active graphics window; a helper step in the peak-search flow (name does not reflect a full peak-list op here).",
    "category": "",
    "verified": true
  },
  "PHASELOCK": {
    "behavior": "Display-refresh macro (DLP trampoline, src @0x627ac; name is misleading): if annotation (ANNOT) is on it calls __AMDISP to repaint the amplitude/annotation display, otherwise CLRDSP clears the screen and re-enables the graticule (GRAT ON) when GRAT is set.",
    "category": "",
    "verified": true
  },
  "PKPOS": {
    "behavior": "Native handler at 0x4264e: computes a peak/marker trace-point position via jsr $32c22, clamps it to max 0x400 (1024 points), reads counter longword $421a8, and runs marker positioning math through jsr $64f0/$6444 \u2014 positions/returns the marker at a trace index.",
    "category": "",
    "verified": true
  },
  "PLOT": {
    "behavior": "Native HP-GL plotter-output dispatcher at 0x4062e: an entry-point table sets a plot sub-mode code in D1 (values -6..+6) then jmps to the shared plot routine $3e702, popping A0/A1 (pen/coord args) and pushing return callback $4068a.",
    "category": "",
    "verified": true
  },
  "PP": {
    "behavior": "Softkey-definition macro (DLP trampoline, src @0x6989ca): defines the 'TRIG MKR' (trigger-marker) ON/OFF toggle softkey (KH .../|ON .OFF.) bound to handler __GTTMF.",
    "category": "",
    "verified": true
  },
  "PREAMPG": {
    "behavior": "Unresolved (handler pc=0, not reachable via the dispatch tables); by name and arg-type 0x1D a preamplifier-gain command \u2014 sets/queries the RF preamp gain offset (dB) applied as an amplitude correction. Best-effort, unverified.",
    "category": "",
    "verified": false
  },
  "PREFX": {
    "behavior": "FFT-mode menu setup (DLP trampoline, src @0x63790): calls __FFTPAMK then KEYDEFs the FFT softkeys \u2014 key 131\u2192__FFAPC 'ANALOG+ ON/OFF', keys 61/67\u2192FFTMM 'MARKER MID SCRN', 8044\u2192__FFSSHKC, 8041\u2192__FFMUHKC.",
    "category": "",
    "verified": true
  },
  "PRINT": {
    "behavior": "Native hardcopy-PRINT setup at 0x4f634: validates the output-device selector (both the arg in D0 and the 2nd arg at (8,A6) must be 0..3, else forced to the 0xffff sentinel) and reads the per-device config byte table at (-$61ae,A4), masking the low 3 bits \u2014 selects/configures the print output device.",
    "category": "",
    "verified": true
  },
  "PRNTPPG": {
    "behavior": "Native handler at 0x2e2ae: reads plot/print state word $ab7c.w and, based on bit 9 of mode flag $ab76.w, dispatches to one of two output routines ($2e216 or $2e0d2) \u2014 drives a print/plot page output.",
    "category": "",
    "verified": true
  },
  "PRSDAC2": {
    "behavior": "Unresolved (handler pc=0, not reachable via the dispatch tables); by name and arg-type 0x05 a service/diagnostic 'preset DAC 2' \u2014 forces a preset code onto analog DAC #2 on the A7 analog-control bus. Best-effort, unverified.",
    "category": "",
    "verified": false
  },
  "PSTATE": {
    "behavior": "Unresolved (handler pc=0, not reachable via the dispatch tables); by name and arg-type 0x0F a state-protection command \u2014 most likely protects the saved instrument-state registers from being overwritten. Best-effort, unverified.",
    "category": "",
    "verified": false
  },
  "PURGE": {
    "behavior": "Native catalog-delete at 0x3c4d2: deletes a user file from the save/recall catalog by indexing 0x22-byte records based at (-$5c54,A4) with file index $a35c.w, iterating via $3a374 with callback $3d108 and clearing the selected entry.",
    "category": "",
    "verified": true
  },
  "PURGES": {
    "behavior": "Native handler at 0x5f1cc in the DLP-runtime region: bsr $5eca2 yields a status bit0 that selects among management routines $5e84a/$5e6bc/$5e364; the exact object purged is not resolvable from the flag-gated dispatch alone.",
    "category": "",
    "verified": false
  },
  "PURGET": {
    "behavior": "Marker/window housekeeping macro (DLP trampoline, src @0x6fdbe): reads WINSTAT/WINZOOM into __Q, and when annotation is on it walks marker-state fields (MSFIELD 37..48) and redraws peak markers via __PKBMD/__PKMBD/__PKDMD (with __PKNSP/__PKNAU unit fix-ups).",
    "category": "",
    "verified": true
  },
  "PWRBW": {
    "behavior": "FFT power-bandwidth measurement (DLP trampoline, src @0x654b4): runs PEAKS on the trace, computes the dB offset of the found peak vs PKPOS ((TRB[__L]-TRB[PKPOS])/100) into the percent-amplitude marker FFTPCTAMR, scales it x200, and calls __FFTAMD to display it.",
    "category": "",
    "verified": true
  },
  "PWRSD": {
    "behavior": "DLP power-up macro: runs DLPINIT and __AMTERM, sets __AMRFLG=1, forces MENU 50 when not already active, schedules __SOONRUN, and installs an ONCMD ANNOT handler.",
    "category": "",
    "verified": true
  },
  "PWRUPOB": {
    "behavior": "Power-up option-board/cal load: validates a cal-RAM block via jsr $61ec (checksum) then copies calibration/config words from cal RAM $2fc016/$2fcb42/$2fc91a/$2fca1c into low-RAM working buffers $9eb6/$a01e/$a096/$a198, gated by a version compare (#$218f vs $bfee).",
    "category": "",
    "verified": true
  },
  "PWRUPTIME": {
    "behavior": "DLP macro that clamps the graph time-marker variable __GTDMKR into the range [1E-6, 65E-3] at power-up.",
    "category": "",
    "verified": true
  },
  "QPGAIN": {
    "behavior": "Sets an analog gain: clamps the arg via jsr $7978 (max 0xff), stores it to $9ecd and a 4-entry byte table, then merges the low byte into shadow $b204 and writes it to MMIO control/gain port 0xFFF712.",
    "category": "",
    "verified": true
  },
  "QPOFFSETL": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0); by name it appears to set/query a quasi-peak-detector offset (low band) for EMI measurements.",
    "category": "",
    "verified": false
  },
  "QPRST": {
    "behavior": "Reset routine: if $b0e6>0 it aborts via jsr $2bc, else saves $b05e->$b05c and $bf02, clears the local frame and $bf0a, and (when b05c==4 && $9b20==3) drives HP-IB/serial MMIO $f100/$f130/$f148/$f160 before reading status via jsr $54d78.",
    "category": "",
    "verified": true
  },
  "RCLT": {
    "behavior": "Short getter that reads and returns the 16-bit RAM word at $b20a.",
    "category": "",
    "verified": true
  },
  "RCTITLE": {
    "behavior": "DLP macro: if __Z==1 clears the box (CLRBOX 1,1,399,199,0), activates window __X, sets __GTSPF=0, then HD;NV to redraw the title/window.",
    "category": "",
    "verified": true
  },
  "RDBCDL": {
    "behavior": "DLP macro: sets __GTPFLG=1, sets marker __GGTMN=248, runs __GTWINTRK window tracking, and (if __Z==1) sets coordinate vars __A/__B/__C to 180/170/160.",
    "category": "",
    "verified": true
  },
  "RDDBL": {
    "behavior": "DLP macro that runs the object-window setup macros __OBWS and __OBWM then NV.",
    "category": "",
    "verified": true
  },
  "RDREAL": {
    "behavior": "DLP macro that displays the alarm message \"You must press FFT OFF to use this feature\" (HD;AMSG...), rejecting the command while FFT mode is active.",
    "category": "",
    "verified": true
  },
  "REFF": {
    "behavior": "Resolves to fcn.34EE8, the DLP command-interpreter/parser step: reads the parser control block (A0 at -4,A6), tests $a89f bit5, walks the in/out pointers at (14,A4)/(16,A4), fetches characters via jsr $4258 and handles '?' (query) and space tokens.",
    "category": "",
    "verified": true
  },
  "RELHPIB": {
    "behavior": "Short handler: stores the arg to RAM $ba58, loads $b076, and calls fcn.80a0 (the reference-level/amplitude-scaling recompute); despite the name the disasm shows a $ba58 write plus a scaling recompute, not an HP-IB bus release.",
    "category": "",
    "verified": true
  },
  "REPEAT": {
    "behavior": "Small guard at 0x406ec (shared with RETURN): tests $b1e0 bit 12 and $b1fe==0x12 and, only if both hold, does jsr $4d8, else rts; two command names map to this one PC with different arg types so which name it truly serves is unresolved.",
    "category": "",
    "verified": false
  },
  "RESETRL": {
    "behavior": "Handler at 0x5ecb6 reads/returns the RAM word $94de (reference-level/ADC-cal state); the adjacent routine at 0x5ecbc performs the actual reset (clears $94de/$94e0/$94d6 and polls sweep-status $f300 bit 11).",
    "category": "",
    "verified": true
  },
  "RETURN": {
    "behavior": "Same handler PC as REPEAT (0x406ec): tests $b1e0 bit 12 / $b1fe==0x12 then conditionally jsr $4d8; the shared PC means the RETURN name-to-behavior mapping is ambiguous.",
    "category": "",
    "verified": false
  },
  "REV": {
    "behavior": "NOT a firmware-revision query: dispatches on bsr $274d4 and outputs memory-card/file status strings from ROM ($2b482 \"CARD:\"/\"NO CARD\"/\"BAD MEDIA\", $2b4ba \"Purge complete\", $2b4ca \"TYPE\"/\"NOT FOUND\"/\"FILENAME:\") via jsr $22c/$2da.",
    "category": "",
    "verified": true
  },
  "REVW": {
    "behavior": "Packs a scaled value: divides the arg by 10 up to 3x (while >1) to form a 2-bit exponent, ORs it into $b0f0 low bits, then clamps two more params via jsr $7978 into $b0f2 and $b0f4 and calls fcn.5dea8.",
    "category": "",
    "verified": true
  },
  "RFCAT": {
    "behavior": "Trampoline running DLP macro `KH /SWEEP|DELAY/,ACTVF __GTSWDE,0;NV`: installs a key-hook that matches the SWEEP/DELAY keys and sets the active-function display via the __GTSWDE (sweep-delay) getter.",
    "category": "sweep",
    "verified": true
  },
  "RMS": {
    "behavior": "Trampoline DLP macro `TRPRST;DISPOSE ONMKRU;IF(ACPPAR)...ELSE DISPOSE ONEOS`: resets traps, disposes the marker-update (ONMKRU) handler, then branches on ACP mode \u2014 running __ACPRCLP + LIMITEST0 when ACP params are active, else disposing the end-of-sweep handler. A measurement/detector setup macro.",
    "category": "measurement",
    "verified": true
  },
  "RQS": {
    "behavior": "Native handler at 0x4F8B4 parses an optional numeric argument (via $6e8/$6090) and defaults to the current mask word at $9EA6.w when none is given, then dispatches through $4E8AE; acts as the GPIB Request-Service (SRQ) mask set/query.",
    "category": "gpib/status",
    "verified": true
  },
  "SADD": {
    "behavior": "Native routine at 0xC470 runs a 7-iteration init loop (i=0..6) copying words from the ROM table at $B30E into RAM words based at ~$FFBA96 (disp -$456A) and zeroing companion words at ~$FFBAA4 (disp -$455C); a table/state reset that ignores the passed argument.",
    "category": "",
    "verified": true
  },
  "SAVEMENU": {
    "behavior": "Native handler at 0x30EAC validates via $306DA then, per the argument's bit0, sets ($30EC2) or clears ($30ECA) bit 12 of the menu/display flag word $B0D6.w and applies the change via $30E00; an on/off menu-save toggle.",
    "category": "menu",
    "verified": true
  },
  "SAVET": {
    "behavior": "Native handler at 0x3D3B0 emits a \"#A\"-prefixed (0x2341) length-tagged binary block over HP-IB \u2014 writes the header, a count, then each data byte via output routines $7108/$73FE/$7068; the companion function at 0x3D3EE parses the same '#','A' header on input. Save/recall-trace block I/O.",
    "category": "trace-io",
    "verified": true
  },
  "SAVRCLTR": {
    "behavior": "Native handler at 0x26512 works on the 16-byte structure at $AC5C: if the arg is '?' it outputs a query via $2639C, else stores via $26416 and mirrors bit0 of $AC6B into $AC5B; then refreshes the user softkey menu from $956A via $92E. Save/recall-trace register handler.",
    "category": "save-recall",
    "verified": true
  },
  "SDEL": {
    "behavior": "Trampoline DLP macro `IF(ACTVF(MKPAUSE));MKPAUSE0;ELSE;MKPAUSE1`: toggles marker pause \u2014 issues MKPAUSE0 (off) when MKPAUSE is the active function, otherwise MKPAUSE1 (on).",
    "category": "marker",
    "verified": true
  },
  "SEDIF": {
    "behavior": "Handler did not resolve through the dispatch tables (pc=0); a tag-0x40 typed command with arg-type 0x16. Name plausibly denotes a 'set difference/delta' or sweep-segment parameter, but no disassembly is available to confirm.",
    "category": "",
    "verified": false
  },
  "SEGDEL": {
    "behavior": "Trampoline DLP macro that, when __V==__X, renumbers the active menu variable __GGTMN (243->242, 246->...) and calls __GTMAP to remap; a segment-delete that renumbers/remaps a segmented (limit-line/sweep-segment) table menu.",
    "category": "",
    "verified": true
  },
  "SELP": {
    "behavior": "Trampoline DLP macro `IF(__PZKP==0){__PZKP=1;}PKZOOM;ELSE MOV PKZOOM,PKZOOM`: if the __PZKP guard flag is clear it sets it and invokes PKZOOM (peak zoom), otherwise a no-op; arms/latches the peak-zoom action.",
    "category": "marker/peak",
    "verified": true
  },
  "SENTER": {
    "behavior": "Native handler at 0x3B330 is a thin wrapper: it pushes the argument and calls the shared parameter-entry routine $3B27E with selector D0=0x0D (13); sibling entries nearby call the same routine with selectors 0x0A/1/9/3.",
    "category": "",
    "verified": true
  },
  "SENTERT": {
    "behavior": "Native handler at 0x179FE is `bsr $179E2;rts`: it clears bit 11 of mode word $B212.w, calls $179CC, and if $BA6B bit2 is clear ORs #$56 into the status word $B1F8.w, then calls the update routine $19C; a mode/state toggle with status-bit set.",
    "category": "",
    "verified": true
  },
  "SER": {
    "behavior": "Trampoline DLP macro `IF(1==__FFTSTF);MOV FFTSTOP,MF;ENDIF;NV`: when the FFT-stop flag __FFTSTF is set, copies FFTSTOP into MF (marker frequency); FFT stop-frequency handling.",
    "category": "fft",
    "verified": true
  },
  "SERSET": {
    "behavior": "Native handler at 0x550D0 parses a user array argument (count/pointer via $2F2) and, when a status bit from $54EF6 is set, iterates the array writing each word via $54EB2 (bounded by the arg count at (8,A6)) and finalizes via $54F6E; loads a block of values into a subsystem.",
    "category": "",
    "verified": true
  },
  "SETPLL": {
    "behavior": "Native handler at 0x11A22 computes a PLL tuning word (sign-dependent path via $1D8/$2B0 plus scale routine $7978), writes it to $B0E8.w, and clamps the value against the min/max bounds passed as arguments; a synthesizer PLL setting.",
    "category": "synth/pll",
    "verified": true
  },
  "SETUPCAT": {
    "behavior": "Trampoline DLP macro installing an ONEOS (end-of-sweep) handler that captures MKF into __GTMKTIME, computes __GTTTM=GD+__GTMKTIME, then pen-up/plot-absolute draws at PA=152; a gated-time ('catalog') marker setup.",
    "category": "",
    "verified": true
  },
  "SFPKEY": {
    "behavior": "Native getter at 0x5F24C returns the stored softkey-definition longword at $94E8.w when the softkey state $94DA.w != -1 (or when helper checks $5ECA2/$5F0C4 pass), else 0; reads the current soft-front-panel-key definition.",
    "category": "",
    "verified": true
  },
  "SHOWOPT": {
    "behavior": "DLP-source macro that, only when ANNOT!=0, lifts the pen, positions to PA=288/161 and draws a CSP annotation text label (then captures __Y=ACPSP into __Z). It renders the option/annotation text overlay on the display.",
    "category": "trampoline",
    "verified": true
  },
  "SIN": {
    "behavior": "DLP-source macro that runs __ACPCC and returns on ACPERR, then copies trace B into trace A (MOV TRB,TRA) and derives a __CZAT value from MSBIT(8,9), re-asserting AT and RL. It is an adjacent-channel-power/trace-math macro (name does not correspond to a trig sine); source is truncated at ~80 chars.",
    "category": "trampoline",
    "verified": true
  },
  "SMOOTH": {
    "behavior": "DLP-source macro whose visible preamble only VARDEFs its working variables (TOIR=-100, __TOIRFLG=0, NDBPNTR=-100, __NDBS=-3, ...) for a trace-smoothing routine; the actual smoothing computation is past the 80-char source truncation.",
    "category": "trampoline",
    "verified": false
  },
  "SNGLS": {
    "behavior": "Single-sweep command: tests sweep-mode word $b0e6.w, clears bits 13-14 of sweep-control word $ab76.w (andi #$9fff), calls arm/status helpers fcn.2bb16/2b8f8/2b916, and sets bit 4 of $ab77.w to select single-sweep mode.",
    "category": "native",
    "verified": true
  },
  "SPEAKER": {
    "behavior": "Takes two 16-bit args (at A6+8 and A6+a) and writes them via generic pointer-store helper fcn.3d2de into two consecutive words of a caller-supplied structure (D0=base, offsets +2/+4); it programs a two-word control block (plausibly the beeper/speaker tone parameters), but the disasm reaches no speaker MMIO directly.",
    "category": "native",
    "verified": true
  },
  "SPZOOM": {
    "behavior": "Init loop (7 iterations, D6=6): copies a 7-word ROM table at PC-relative $b30e into RAM at ~0xFFBA96 (dest (-$456a,A4), A4=index*2) and clears the adjacent word at ~0xFFBAA4; it resets a small state array (span-zoom defaults).",
    "category": "native",
    "verified": true
  },
  "SQR": {
    "behavior": "DLP logic primitive: pops an index via fcn.7c0, fetches word at ROM/RAM table (-$4fa0,A4) (A4=index*2) into D6, bit-tests bit number (arg A6+8) with btst/sne, and pushes the 0/1 boolean result via fcn.77e. It returns a single bit's state, not a square/square-root.",
    "category": "native",
    "verified": true
  },
  "SRCALCRf": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0); by name it is a tracking-generator source ALC (automatic-level-control) reference/mode setting, arg_type 0xFF. Meaning is a best-effort inference, not confirmed by disasm.",
    "category": "unresolved",
    "verified": false
  },
  "SRCNORM": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0); by name it is the tracking-generator source-normalization command (store/subtract a reference trace), arg_type 0xC1. Meaning is a best-effort inference, not confirmed by disasm.",
    "category": "unresolved",
    "verified": false
  },
  "SRCTKPK": {
    "behavior": "Source (tracking-generator) track-peak command: loads the command-token pointer (A6+8,A4), checks (1,A4)==0x3f for the '?' query form, and on either path pushes constant #$7000 and calls fcn.3a308/fcn.32bda to perform the tracking-peak operation/readback.",
    "category": "native",
    "verified": true
  },
  "SRQ": {
    "behavior": "GPIB service-request command: range-checks state word $bfee.w against 0x218d..0x2195 (branches to $4b71e when out of range) then falls through to the SRQ handler at $4b532 which sets/manipulates the service-request status.",
    "category": "native",
    "verified": true
  },
  "STB": {
    "behavior": "Status-byte query: loads command-token ptr (-4,A6,A4), tests (1,A4)==0x3f for '?', and on the query path reads the status longword pair from $ac54.w (movem.l into D0-D1) and formats/outputs it via fcn.5762/7190/70d2.",
    "category": "native",
    "verified": true
  },
  "STBLUg": {
    "behavior": "Handler did not resolve via the dispatch tables (pc=0), arg_type 0xFF; name is opaque (possibly a status-byte or internal 'stable' flag helper). No disasm or doc support \u2014 meaning unknown.",
    "category": "unresolved",
    "verified": false
  },
  "STDEV": {
    "behavior": "DLP-source macro that reads window status (MOV __Q,WINSTAT WINZOOM) and branches on zoom state: if zoomed runs __J0, else runs peak-integrate __PKITG and, depending on the peak table (PKTBL) / marker table (MKTB...), runs __PKDPT \u2014 a marker/peak standard-deviation computation.",
    "category": "trampoline",
    "verified": true
  },
  "STOR": {
    "behavior": "DLP-source macro (complete): if GSTAT(1) is true it sets the time-gate control mode to EDGE, otherwise to LEVEL (GATECTL EDGE/LEVEL). It configures gate-control triggering based on gate status bit 1.",
    "category": "trampoline",
    "verified": true
  },
  "STORBCDL": {
    "behavior": "One entry of a store dispatch table: sets type code D1=-5 (moveq #-5), pops A0/A1 (dest/value) off the stack, loads return address A2=$4068a, pushes A0-A2 and jmp's to the common store routine fcn.3e702; it stores a BCD-long-typed value.",
    "category": "native",
    "verified": true
  },
  "STORDBL": {
    "behavior": "Store (double-type) primitive: reads a status/flag word via fcn.2f498, tests its bits and mode word $b116.w (bit 15) plus $b0ec.w, and on an invalid state reports error code 0x32 via fcn.65e before performing the guarded store.",
    "category": "native",
    "verified": true
  },
  "STORREAL": {
    "behavior": "Takes two word args (A6-2 and A6+8), clamps each to the range 0..15 (clr if <0, cap at 0xf), and stores them as bytes at RAM $ba74.w and $ba75.w; it records a clamped index/coordinate pair (name does not clearly denote a real-number store).",
    "category": "native",
    "verified": true
  },
  "SU": {
    "behavior": "Handler links, pushes buffer pointer $bf66 and the 32-bit value from RAM $bb54, then jsr's fcn 0x3752e (a routine that indexes a word table, masks 0xFFF, computes a size into $a5fa). The SU command semantic is not evident from the call.",
    "category": "",
    "verified": false
  },
  "SUB": {
    "behavior": "Trampoline runs a DLP menu-state macro: cycles menu variable __MN[18] as (__MN[18]%4)+1, calls __PKFMB, then branches on __MN[19]/__MN[20] \u2014 a softkey/menu selection handler.",
    "category": "",
    "verified": true
  },
  "SUM": {
    "behavior": "Trampoline runs the adjacent-channel-power SUM macro: __ACPSUD;__M5;__ACPSUM plus WINITM 17/18/19,0 and __ACPICALC, computing the ACP band-power summation.",
    "category": "",
    "verified": true
  },
  "SUMSQR": {
    "behavior": "Trampoline runs an FFT scaling/sum-of-squares macro: tests __FFTA, MOV __F,MF, then rescales FFTSTOP and ST via DIV by 200 \u2014 FFT-domain math.",
    "category": "",
    "verified": true
  },
  "SWPCPLTh": {
    "behavior": "Unresolved (handler_pc=0, arg 0xFF); by name SWPCPL = sweep couple, selecting auto vs. manual coupling of sweep time to span/RBW/VBW. Not confirmed in disassembly.",
    "category": "sweep",
    "verified": false
  },
  "SYNCMODE": {
    "behavior": "Trampoline runs an interpolation macro: __Q=(2*__B-__A+__C)/2, __R=(2*__C-__D+__B)/2, then sets TOIR to the smaller \u2014 a 3-point parabolic peak interpolation.",
    "category": "",
    "verified": true
  },
  "T7": {
    "behavior": "Handler at 0x00ABDE writes ACRTC command 0x0002 to 0xFFF5FC and 0x0180 to the data port 0xFFF5FE (HD63484 Color Control Register = 2-bpp mode), then rts. T7 and T8 share this one tiny display helper, suggesting dispatch mis-resolution rather than a distinct trace handler.",
    "category": "display",
    "verified": false
  },
  "T8": {
    "behavior": "Same handler_pc (0x00ABDE) as T7: writes ACRTC command 0x0002/data 0x0180 to 0xFFF5FC/0xFFF5FE (HD63484 CCR, 2-bpp), then rts. Two different command names resolving to one routine indicates the dispatch table did not distinguish them.",
    "category": "display",
    "verified": false
  },
  "TDF": {
    "behavior": "Unresolved (handler_pc=0); documented HP command TDF = Trace Data Format, selecting the trace-data transfer encoding (B=binary, M=measurement units, P=parameter/real units, A=ASCII). Not confirmed in disassembly.",
    "category": "",
    "verified": false
  },
  "TEXT": {
    "behavior": "Handler reads a 2-char token argument; the 2nd char 'C'(0x43)/'F'(0x46)/'S'(0x53) selects RAM word $b1ac/$b1aa/$b1b0 and stores a bounded value (0..0xFFF) there \u2014 sets a display text-attribute variable.",
    "category": "display",
    "verified": true
  },
  "THEN": {
    "behavior": "Handler at 0x0406EC tests $b1e0 bit12 AND $b1fe==0x12; if both hold it jsr's low-mem vector 0x4d8 (jmp 0x19a72), else rts \u2014 a conditional dispatch gate. Shared with TRA; the specific command semantic is not resolvable here.",
    "category": "",
    "verified": false
  },
  "TIMEDATEB": {
    "behavior": "Unresolved (handler_pc=0); TIMEDATE sets/enables the on-screen real-time-clock date/time display (TIMEDATE ON/OFF, date/time set; RTC at 0xEF40xx, redraw flag bc67.1). Not confirmed in disassembly.",
    "category": "display",
    "verified": false
  },
  "TITLE": {
    "behavior": "Handler at 0x019700 is a 3-instruction getter returning RAM word $bfee in D0. The normal TITLE (set display title string) behavior is absent, so this handler_pc is likely a mis-resolution to a title/display-state accessor.",
    "category": "display",
    "verified": false
  },
  "TMri": {
    "behavior": "Unresolved (handler_pc=0, arg 0xFF); the name is mangled ('TM'+suffix) and its meaning is uncertain \u2014 plausibly a trigger-mode or timing token. Not confirmed in disassembly.",
    "category": "",
    "verified": false
  },
  "TNG": {
    "behavior": "Handler at 0x040384 builds a status byte at (-1,A6) by testing flags: bit4 $b1f7, bit0 $b0a1, bit8 $b1e0, plus bits of $b1f9/$b073 \u2014 a tuning/status query that packs hardware-state flags into its return value.",
    "category": "status",
    "verified": true
  },
  "TR": {
    "behavior": "Trampoline runs the DLP macro __GTMAP;__GTLMKR; \u2014 regenerates the trace map and the limit/marker overlay (trace-graticule redraw).",
    "category": "display",
    "verified": true
  },
  "TRA": {
    "behavior": "Same handler as THEN (0x0406EC): conditional gate on $b1e0 bit12 && $b1fe==0x12 that jsr's low-mem vector 0x4d8 (\u21920x19a72), else rts. TRA nominally selects trace A, but the shared handler doesn't confirm that.",
    "category": "",
    "verified": false
  },
  "TRALLOC": {
    "behavior": "Trampoline runs a trace-cleanup macro: IF(__CZSU==0) RETURN; else DISPOSE ONEOS, set ACPMK=-1, A1;B4;BLANK TRC \u2014 deallocates/blanks a trace object.",
    "category": "display",
    "verified": true
  },
  "TRB": {
    "behavior": "Shared trace-select handler at 0x406EC: btst #12 of state word $B1E0.w and, if set AND trace-mode word $B1FE.w == 0x12, jsr through low-mem vector $4D8 (= jmp $19A72) into the trace/display routine, else rts. The specific trace (B, from parser arg 0x08) is selected by the caller, not visible in this shared code.",
    "category": "trace",
    "verified": false
  },
  "TRC": {
    "behavior": "Same shared handler as TRB at 0x406EC (arg 0x09 = trace C): guards on $B1E0.w bit 12 and $B1FE.w == 0x12, then jsr $4D8 (jmp $19A72). Trace selection is arg-driven and not resolvable from this shared body.",
    "category": "trace",
    "verified": false
  },
  "TRCEXP": {
    "behavior": "At 0x56C82: calls $5687C then loops $B12A.w times, each pass reading a byte from a buffer at (A4-0x4ED6) and calling $56414 (per-element emit/format), finishing with $56C62 and $56096. Iterates over a B12A-length trace element list to expand/output it.",
    "category": "trace",
    "verified": true
  },
  "TRCLOG": {
    "behavior": "Trampoline (0x696E2) that pushes the DLP source at ROM 0x696DA and calls the DLP scheduler $D18; the macro is 'MENU 91;' which brings up soft-key menu 91.",
    "category": "menu",
    "verified": true
  },
  "TRCMEM": {
    "behavior": "At 0x5B818: indexes a per-trace RAM table via $956C.w*10 (base -0x65EC) and, for arg D0==0xFFFE, fills a 16-byte region at (A4-0x53A4) with 0xFF/0x00 then copies longwords into (A4-0x65AA). Initializes/copies a trace-memory register structure.",
    "category": "trace",
    "verified": true
  },
  "TRD": {
    "behavior": "Same shared handler as TRB/TRC at 0x406EC (arg 0x0A = trace D): guards on $B1E0.w bit 12 and $B1FE.w == 0x12, then jsr $4D8 (jmp $19A72). Which trace is acted on comes from the parser arg, not this shared body.",
    "category": "trace",
    "verified": false
  },
  "TRDEF": {
    "behavior": "At 0x40674 loads D1 = 0x0B (operation selector) and tail-jumps (jmp $3E702) into the shared trace-math/define engine, which dispatches on D1 (stored to A6-0x2E and branched on). TRDEF is trace-op 0x0B (trace define).",
    "category": "trace",
    "verified": true
  },
  "TRDSP": {
    "behavior": "At 0x2D448 configures the trace-display window: loads window struct from $A7D6.w, sets $AB7C/$AB80/$AB84.w=1, $AB82.w=0x15, $A9B2.b=1, clears $AB77.w bit4, sets $AB76.w bits 13/14, then calls display subroutines ($2BB16,$2BA64,$2D37E,$2BD80) and a final DLP call with source ptr 0x2F23C. Sets up/redraws the trace display.",
    "category": "display",
    "verified": true
  },
  "TRGRPH": {
    "behavior": "Trampoline (0x6DAB4) running DLP source at ROM 0x6DA54: 'DISPOSE ONEOS;ONCMD GD {GD=__GTSWDE};ONCMD GD __GTPRGT;ONCMD GATE;GATE OFF;ONCMD GATE __GTPRGT;' \u2014 sets up the gated-sweep time-domain graph by redefining the GD (gate-delay\u2192sweep-delay) and GATE command hooks and disposing the ONEOS handler.",
    "category": "gated-sweep",
    "verified": true
  },
  "TRMATH": {
    "behavior": "Trampoline (0x64BE4) running DLP source at ROM 0x64B06: if FFT is active (__FFTSTF) it sets FFTSTOP=400/(ST*2) and runs the FFT step, otherwise it displays 'You must activate SINGLE FFT or CONTINUS FFT before this function is available.' Trace math/FFT control.",
    "category": "trace-math",
    "verified": true
  },
  "TRPRST": {
    "behavior": "At 0x00ABDE writes 0x0002 to the HD63484 ACRTC command register $F5FC.w and 0x0180 to data register $F5FE.w (write-parameter-register 2 = display control word CCR 0x0180, the 2bpp control), then rts. Reprograms the ACRTC display control.",
    "category": "display",
    "verified": true
  },
  "TRSTAT": {
    "behavior": "Trampoline (0x62BB8) running DLP source at ROM 0x62A92: a MSBIT() flag chain that selects the active trace and does MOV __TRA,TRA / TRB / TRC (or __TRA,0) depending on which trace's view/state bits are set. Resolves the current active-trace status into __TRA.",
    "category": "trace",
    "verified": true
  },
  "TTPXL": {
    "behavior": "Handler at 0x24B6A is 'clr.w $9FD0.w; rts', clearing a trace-plot state variable; the adjacent routine at 0x24B76 converts an amplitude value in D0 to a display pixel row (clamps 0xFC18\u21920, 0xE9BC\u21920x38, else /0x64 with 8-pixel quantization). Trace-to-pixel plotting init/conversion.",
    "category": "trace",
    "verified": true
  },
  "TVLINE6": {
    "behavior": "Unresolved (handler_pc=0, tag 0x30, category tv/catv); did not resolve through the dispatch tables and the name suffix is a name-table over-read. Best-effort: the TV-trigger 'TV LINE' command (selects the TV line number for the video-trigger/CATV option).",
    "category": "tv/catv",
    "verified": false
  },
  "TVSFRMQj": {
    "behavior": "Unresolved (handler_pc=0, tag 0x30, category tv/catv); name is a garbled name-table read. Best-effort: a TV-trigger frame/field command ('TV FRAME'-class) of the video/CATV option; exact behavior not derivable without the handler.",
    "category": "tv/catv",
    "verified": false
  },
  "TVSTNDdk": {
    "behavior": "Unresolved (handler_pc=0, tag 0x30, category tv/catv); garbled name suffix. Best-effort: the 'TV STANDARD' command that selects the TV broadcast standard for the video-trigger/CATV option.",
    "category": "tv/catv",
    "verified": false
  },
  "TVSYNC7l": {
    "behavior": "Unresolved (handler_pc=0, tag 0x30, category tv/catv); garbled name suffix. Best-effort: the 'TV SYNC' command selecting TV sync polarity/type for the video-trigger/CATV option.",
    "category": "tv/catv",
    "verified": false
  },
  "TVTSTAT": {
    "behavior": "At 0x27442 takes value D0; special-cases D0==0xFC19 (query sentinel) and computes a result from trace/TV-buffer longword pointers $BB4A.w and $BB4E.w via low-mem math vectors ($6118/$6106/$982), otherwise clamps and computes a buffer offset. TV/trace status read; exact semantic unclear.",
    "category": "tv/catv",
    "verified": false
  },
  "TVTUNSET": {
    "behavior": "Query handler for the TV-tune value: checks whether the command token's next char is '?' (cmpi.b #$3f,(1,A4)); if so it reads the word at RAM $b24c and formats/outputs it via $73fe/$70d2 (passing buffer $b24e), reporting the stored TV-tuning setting.",
    "category": "",
    "verified": true
  },
  "TVTUNSTD": {
    "behavior": "Takes a longword+word numeric argument (movem.l (8,A6),D0-D1 / (16,A6)) and runs it through conversion routines $397b6 and $5dc2 before dispatching into $369f6 to set a TV-tuning-standard parameter; the $369f6 target is table-dispatched and not resolved in-window.",
    "category": "",
    "verified": false
  },
  "TWNDOW": {
    "behavior": "Trampoline running a DLP macro that, on the ANNOT annotation flag, either clears the FFT window vars (__N,0/__A,0/EM) or initializes the FFT window (__FFTAI, conditionally __FFTCD) \u2014 the time/FFT-window setup command.",
    "category": "",
    "verified": true
  },
  "UDEFPV": {
    "behavior": "Trampoline DLP macro gated on the __PKIF peak flag that clamps the marker-delta-line mode MKDLMODE between 0 and 1 (MXM then MIN) and toggles __PKIF0/__PKIF1 \u2014 manages user-defined peak/marker-delta state.",
    "category": "",
    "verified": true
  },
  "UNTIL": {
    "behavior": "DLP control-flow loop terminator (pairs with REPEAT); resolves only to the shared control-flow fallback at 0x406ec (also used by IF/ELSE/ENDIF/REPEAT/RETURN), whose code merely tests $b1e0 bit12 and $b1fe==0x12 then conditionally jsr $4d8 \u2014 the real UNTIL semantics live inside the DLP interpreter, not this stub.",
    "category": "",
    "verified": false
  },
  "USEROFST": {
    "behavior": "Stores the 32-bit command argument straight into RAM longword $bffa (move.l D0,$bffa.w) \u2014 sets the user amplitude/reference offset value.",
    "category": "",
    "verified": true
  },
  "USERRANGE": {
    "behavior": "Tests bit 11 of the mode flag $b07a.w and branches out of the handler window to $17fb0 or $17dfc; the code that actually applies the user range lies outside the disassembled range and is not resolved here.",
    "category": "",
    "verified": false
  },
  "USTATE": {
    "behavior": "'User state' command; resolves only to the shared 0x406ec control-flow fallback (shared by 15 flow/keyword tokens), so no dedicated handler behavior is captured. Best-effort by name/arg: save or recall of a user instrument-state.",
    "category": "",
    "verified": false
  },
  "UWAIT": {
    "behavior": "Trampoline DLP macro that snapshots the current instrument settings into __CZ* scratch variables \u2014 LEDs via MSFIELD(8,15,16), detector DET, span SP, sweep SS, log-scale LG, atten AT, etc. \u2014 a state-save preamble for a timed wait/measurement sequence.",
    "category": "",
    "verified": true
  },
  "VARDEF": {
    "behavior": "Defines/selects a DLP user variable: normalizes the variable index (subtracting the 0x218e/0x5a bands), bounds-checks it to 0..6, indexes the ROM table at 0x1a242 to store a value into $9fb4, and writes the variable token (+0x218e) into the current-variable pointer $bfee.",
    "category": "",
    "verified": true
  },
  "VARIANCE": {
    "behavior": "Trampoline running the short DLP sequence __PKAM;M2;NV (peak-amplitude / marker-2 / NV) \u2014 a marker-math routine producing a trace variance/statistics readout.",
    "category": "",
    "verified": true
  },
  "VAVG": {
    "behavior": "Did not resolve through the dispatch tables (pc=0); by name and arg_type 0x07 this is most likely the video/trace-averaging control command (VAVG = Video AVeraGe).",
    "category": "",
    "verified": false
  },
  "VBO": {
    "behavior": "Trampoline running the DLP fragment 'KH /GATE|' \u2014 a key/menu macro that invokes the GATE softkey path.",
    "category": "",
    "verified": true
  },
  "VGDAC": {
    "behavior": "Despite the 'DAC' name, this trampoline's DLP macro drives graticule/channel-power annotation: __CHPANLBL, toggles GRAT (GRAT0/GRAT1), clears the channel-power marker (ACPMK=-1) and BLANKs \u2014 a display/annotation macro, not a DAC write.",
    "category": "",
    "verified": true
  },
  "VIEW": {
    "behavior": "Puts a trace into VIEW mode \u2014 tests the trace-active flag $b0e6.w, then calls $3efde with mode value 2 (trace-view) to freeze/display the trace without further writes (or jsr $2bc when already active).",
    "category": "",
    "verified": true
  },
  "VKBDCONFIG": {
    "behavior": "Reads the keyboard-config longword at RAM $bf12, masks the sign bit when negative, and formats/outputs it via $64fa/$62f0/$6016 (or $5dc4) \u2014 a query returning the keyboard-configuration value.",
    "category": "",
    "verified": true
  },
  "VRD": {
    "behavior": "Resolves only to the generic 3-instruction routine at 0xabde (a fallback shared by 13 commands incl. WINON/WINOFF/WARN/WAIT) that writes ACRTC command 0x0002 and param 0x0180 to display regs 0xFFF5FC/0xFFF5FE; this is not VRD's real handler. Best-effort by name: a video/VRAM read.",
    "category": "",
    "verified": false
  },
  "WAIT": {
    "behavior": "GPIB WAIT (hold command execution until pending operations complete); resolves only to the shared 0xabde fallback (the ACRTC 0x0002/0x0180 write shared by 13 commands), so the true WAIT handler is not captured here.",
    "category": "",
    "verified": false
  },
  "WARN": {
    "behavior": "Handler at 0x00abde is a 3-instruction ACRTC helper: writes 0x0002 to the HD63484 command port 0xFFF5FC and 0x0180 to its data port 0xFFF5FE, then rts. The same PC is shared by WARN/WARNC/WARNE/WARNPRO/WINOFF (five semantically-unrelated names), so this is a dispatch-table default, not a resolved per-command handler.",
    "category": "",
    "verified": false
  },
  "WARNC": {
    "behavior": "Points at the same shared PC 0x00abde: writes ACRTC command port 0xFFF5FC=0x0002 and data port 0xFFF5FE=0x0180 then returns. Shared across five unrelated command names, so the mapping is an unresolved dispatch default.",
    "category": "",
    "verified": false
  },
  "WARNE": {
    "behavior": "Same shared handler 0x00abde (ACRTC write of cmd 0x0002 / data 0x0180 to 0xFFF5FC/0xFFF5FE). Shared by five unrelated names, indicating a defaulted/unresolved dispatch rather than a WARNE-specific routine.",
    "category": "",
    "verified": false
  },
  "WARNF": {
    "behavior": "Handler at 0x05ecb6 is a getter: move.w $94de.w,D0; rts \u2014 reads the 16-bit ADC-calibration status word at RAM 0xFFFF94DE (part of the ADC-cal block that also touches $94e0/$94d6/$948a/$94da/$94da) and returns it. The PC is shared by WARNF/WARNH/WARNP/WARNPRN, so the name-to-handler mapping is a dispatch default, not verified.",
    "category": "",
    "verified": false
  },
  "WARNH": {
    "behavior": "Same shared handler 0x05ecb6 reading ADC-cal status word RAM $94de into D0 and returning. Four unrelated WARN* names collapse onto this one PC, so treat the mapping as an unresolved default.",
    "category": "",
    "verified": false
  },
  "WARNP": {
    "behavior": "Same shared handler 0x05ecb6 (returns ADC-cal status word $94de). Shared across WARNF/WARNH/WARNP/WARNPRN \u2014 the disasm is a simple getter but its assignment to WARNP is a dispatch default, not confirmed.",
    "category": "",
    "verified": false
  },
  "WARNPRN": {
    "behavior": "Same shared handler 0x05ecb6 returning RAM word $94de (ADC-cal status). Shared PC across four names, so the WARNPRN mapping is unresolved/defaulted.",
    "category": "",
    "verified": false
  },
  "WARNPRO": {
    "behavior": "Shares the 0x00abde ACRTC helper (writes 0x0002 to 0xFFF5FC and 0x0180 to 0xFFF5FE, then rts). One of five unrelated names on this PC, so it is a dispatch default rather than a WARNPRO-specific handler.",
    "category": "",
    "verified": false
  },
  "WINACTIVE": {
    "behavior": "DLP-source trampoline: sets display attribute DA=__V and, when __S==0, pen-ups and draws TEXT strings at pen positions loaded from the __J/__N and __I/__M coordinate variables \u2014 paints the active-window annotation/label text.",
    "category": "",
    "verified": true
  },
  "WINANNOT": {
    "behavior": "Native handler at 0x05b1a4 bounds-checks the argument D0 to 0..3 (out-of-range returns 0) then returns a 16-bit word from a 4-entry, stride-6 table at RAM ~0xFFFF99FC indexed by D0 \u2014 a window-annotation code lookup.",
    "category": "",
    "verified": true
  },
  "WINCLOSE": {
    "behavior": "DLP trampoline running the source `__GTWINSWAP;NV` \u2014 swaps/closes the active graphics window via the __GTWINSWAP sub, then NV.",
    "category": "",
    "verified": true
  },
  "WINCOUNT": {
    "behavior": "DLP trampoline `IF(GSTAT(1));KB;ELSE;KH /GATE|LENGTH/,ACTVF __GTLMKR,0;ENDIF` \u2014 if gate status GSTAT(1) is set it does KB, otherwise it installs a GATE/LENGTH softkey that activates __GTLMKR (gated-sweep length/marker setup).",
    "category": "",
    "verified": true
  },
  "WINFMT": {
    "behavior": "DLP trampoline `__ACPES;__ACPEM;NV` \u2014 runs the two window-format setup subs __ACPES and __ACPEM (adjacent-channel-power measurement window formatting) followed by NV.",
    "category": "",
    "verified": true
  },
  "WININIT": {
    "behavior": "DLP trampoline computing window/zone init values from sweep parameters: __CZBS = SP*(__K-__J+1)/401 (channel-zone bin size from span) and __CZBN = 1.128*RB (noise bandwidth from RBW), with a detector-dependent adjustment (IF DET==1 then __CZBN += SP/400...).",
    "category": "",
    "verified": true
  },
  "WINITM": {
    "behavior": "DLP trampoline `KH 'FFT STOP|FREQ',ACTVF FFTSTOP` \u2014 defines a softkey labeled 'FFT STOP FREQ' that, when pressed, activates the FFTSTOP command.",
    "category": "",
    "verified": true
  },
  "WINLABEL": {
    "behavior": "Native handler at 0x05b6e2 copies a 4-byte label descriptor from the pointer arg at (0xc,A6), maps its orientation char 'H'(0x48)->1 / 'V'(0x56)->2 / else 0, then indexes the per-window label table at RAM ~0xFFFF9A14 using the current window index $956c \u2014 sets a window's horizontal/vertical label.",
    "category": "",
    "verified": true
  },
  "WINNEXT": {
    "behavior": "Native handler at 0x040002 dispatches on the current command/state word $bb8a: for 0x3b it bset bit7 of $b0e5 (then jsr $418), for 0x3e bit8 and 0x3f bit12 of $b0e4 (jsr $418), for 0x6c it jsr $51a with D0=3 \u2014 a menu/flag state setter keyed on $bb8a.",
    "category": "",
    "verified": true
  },
  "WINOFF": {
    "behavior": "Shares the 0x00abde ACRTC helper (writes 0x0002 to command port 0xFFF5FC and 0x0180 to data port 0xFFF5FE, then rts). One of five unrelated names on this PC, so the WINOFF mapping is a dispatch default, not a confirmed window-off handler.",
    "category": "",
    "verified": false
  },
  "WINON": {
    "behavior": "Writes ACRTC command 0x0002 to $FFF5FC then data 0x0180 to $FFF5FE (the HD63484 register-select + data ports), i.e. programs a display-controller register to turn the window/display on.",
    "category": "",
    "verified": true
  },
  "WINOPEN": {
    "behavior": "Parses its argument for a '#A'-style length-4 binary block header (arg byte0=0x04, byte1=0x23 '#', byte2=0x41 'A'), extracts two window params (bytes 3,4), then copies the window template from RAM $b24c into the destination buffer (or copies the raw arg bytes otherwise) to initialize a window definition.",
    "category": "",
    "verified": true
  },
  "WINSETFMT": {
    "behavior": "Trampoline running DLP source `TEXT~ ~;NV` \u2014 emits a blank fixed-width TEXT field template followed by NV, setting the window's display text format.",
    "category": "",
    "verified": true
  },
  "WINSTAT": {
    "behavior": "Trampoline running DLP `IF __GGTMN,NE,0;IF __GGTMN,NE,500;IF __GGTIN,EQ,1;IF MENU,EQ,13;{MENU=__GGTMN;}` \u2014 sets the active MENU to the gate-menu variable __GGTMN when the gate DLP conditions hold (gate menu not 0/500, gate-in==1, current menu==13).",
    "category": "",
    "verified": true
  },
  "WINSTATE": {
    "behavior": "Trampoline running DLP `IF(HAVE(GATE));VRD __GGTENTR,0;VRD __GGTIN,0;VRD __GGTVB,0;VRD __GGTRB,0;...` \u2014 when the GATE option is present, resets the batch of gate DLP state variables to 0 to initialize the gate window state.",
    "category": "",
    "verified": true
  },
  "WINZOOM": {
    "behavior": "Trampoline running DLP `{DA=__V;};IF(__S==1);{PA=60;PA=180;};TEXT% %;...` \u2014 assigns display var DA=__V and, when __S==1, sets pen-address PA=60/180 and writes a blank TEXT field to position/draw the zoomed window region.",
    "category": "",
    "verified": true
  },
  "WKP": {
    "behavior": "Native: indexes a per-window table via $956c.w (window/menu id x10) at base -$65ec, checks the entry's validity word, and if valid with D0<=0x37 pushes the arg and calls the window field-setters $5a62a/$5a6ca to store the keypress/parameter into the window structure; returns -1 (0xFFFF) on failure.",
    "category": "",
    "verified": true
  },
  "WRNM": {
    "behavior": "Native getter: `move.w $94de.w, D0; rts` \u2014 reads the warning register/number word from RAM $94de into D0 and returns it.",
    "category": "",
    "verified": true
  },
  "WRNO": {
    "behavior": "Native getter sharing the exact same handler as WRNM (pc 0x5ecb6): reads the warning word from RAM $94de into D0 and returns it.",
    "category": "",
    "verified": true
  },
  "XCH": {
    "behavior": "Native: when arg==0 it tests via low-mem $18a and, if a status bit is set, performs a swap through helpers $6ef8/$32c22/$71e4/$70d2; otherwise it clamps the numeric arg to >=2 (default 0x191=401, the trace length), scales it via $7978, and copies a 3-byte record template from ROM $3a134 into a stack buffer. Exact exchanged buffers are table/helper-dispatched and not concretely resolved.",
    "category": "",
    "verified": false
  },
  "XLAT": {
    "behavior": "Native thin wrapper: `link; move.l D0,(A7); jsr $a2a.w; unlk; rts` \u2014 passes its argument in D0 and dispatches to the low-memory API/jump-table routine at $a2a (a translate/lookup call whose target is not resolvable from this stub).",
    "category": "",
    "verified": false
  },
  "YTFDAC": {
    "behavior": "Trampoline running DLP `__ACPSUD;TRC-8000;VIEW TRC;M1;__CHPCG;NV` \u2014 runs an analog/power-supply update, drives the trace to -8000, VIEWs it, sets marker M1 and calls __CHPCG; a YTF (YIG-tuned-filter) DAC alignment sequence acting on the trace and markers.",
    "category": "",
    "verified": true
  },
  "YTFDATAR": {
    "behavior": "Trampoline running DLP `ANNOT0;EM;__PKNSP;__PKNAU;KEYDEF8049,/HD;/;WININIT;WINOPEN __W,0,60,400,140;...` \u2014 disables annotation, erases, runs peak routines, defines softkey 8049 and opens/inits a window; sets up the YTF-data read user-interface/menu window.",
    "category": "",
    "verified": true
  },
  "YTFDATAW": {
    "behavior": "Trampoline running DLP `{FOR(__MN[19]=37;__MN[19]<49&&(!(__MN[20]=MSFIELD(__MN[19],4,5))||MSFIELD(...` \u2014 loops over menu/measurement field indices 37..48 reading MSFIELD values into the __MN[] array to write out the YTF calibration data.",
    "category": "",
    "verified": true
  },
  "ZMK": {
    "behavior": "Native trampoline: pushes its arg block and calls $3d32c, which in turn writes two 16-bit fields (at offsets arg and arg+2) via $3d2de into a marker/zone data structure. Builds a 4-byte zone-marker record; the exact target buffer is not fully resolved.",
    "category": "",
    "verified": false
  },
  "ZMKACT": {
    "behavior": "Native: reads display/zone control word $bc64.w, clears its top two bits (AND 0x3fff) and sets bit15 (OR 0x8000) writing it back, sets the zone-marker-active flag $9b26.w=1, then calls the redraw routine $561e8 \u2014 activates the zone marker.",
    "category": "",
    "verified": true
  },
  "ZMKCP": {
    "behavior": "Native: when arg==0 and the length count $b12a.w >= 8, copies $b12a bytes from source table at (-$4ed6,A4) into a local stack buffer, then scans from index 3 for a ':' (0x3a) delimiter \u2014 parses/copies a colon-delimited zone-marker string of length $b12a.",
    "category": "",
    "verified": true
  },
  "ZMKLEFT": {
    "behavior": "Native clamp helper: returns 0 if D0<0, returns the arg upper bound ($8,A6) if D0 exceeds it, else returns D0 \u2014 clamps a value to the range [0, arg] to compute the zone-marker left/lower boundary.",
    "category": "",
    "verified": true
  },
  "ZMKLPOS": {
    "behavior": "Native marker-left-position calculator: reads marker index $a9a0.w, calls trap $15a and helper fcn.7978, and maps the position into a 2000-point (0x7d0) sweep-coordinate range via a divs, indexing the $b0c8.w/$b0e6.w marker tables and setting $bb06.w=1.",
    "category": "",
    "verified": true
  },
  "ZMKPK": {
    "behavior": "Native arithmetic wrapper: calls helper fcn.88de and returns (result+1)>>1 (a rounded/averaged marker-peak value); no hardware or MMIO access.",
    "category": "",
    "verified": true
  },
  "ZMKPKNL": {
    "behavior": "Resolves to a 3-instruction stub at 0xABDE that writes ACRTC command 0x0002 / data 0x0180 to HD63484 ports 0xFFF5FC/0xFFF5FE (a display control-word write); this shared address does not match a 'peak-next-left' marker op and is almost certainly a dispatch-table mis-resolution.",
    "category": "",
    "verified": false
  },
  "ZMKPKNR": {
    "behavior": "Resolves to the SAME 0xABDE stub as ZMKPKNL, writing ACRTC command 0x0002 / data 0x0180 to 0xFFF5FC/0xFFF5FE; the identical shared address for two distinct marker commands indicates a dispatch mis-resolution rather than the real 'peak-next-right' handler.",
    "category": "",
    "verified": false
  },
  "ZMKRIGHT": {
    "behavior": "Native: indexes a marker table at (A4-0x6108) by arg*2 into $b206.w, sets/clears bit14 of $b204.w depending on whether the arg is <10, then drives MMIO 0xFFF710<-$b206 and 0xFFF712<-$b204 (marker/sweep hardware registers) and invokes traps $232/$238.",
    "category": "",
    "verified": true
  },
  "ZMKRPOS": {
    "behavior": "Unresolved (pc=0); handler not recovered via the dispatch tables. By name, the marker right-edge position counterpart to ZMKLPOS (reports/sets the right marker limit); not confirmed by disassembly.",
    "category": "",
    "verified": false
  },
  "ZMKTYPE": {
    "behavior": "Unresolved (pc=0); handler not recovered. By name, sets or reports the marker type (e.g. normal/delta/span-pair); not confirmed by disassembly.",
    "category": "",
    "verified": false
  },
  "ZTGAOFST": {
    "behavior": "Unresolved (pc=0); handler not recovered. By name, the tracking-generator amplitude/level offset (TG output-power offset); not confirmed by disassembly.",
    "category": "",
    "verified": false
  },
  "__ACPAC": {
    "behavior": "Native ACP handler: fetches a numeric argument list via trap $2f2 (count into -6(A6), pointer into D0), and when count>1 iterates the list adding offset $a(A6) and passing each element through helpers fcn.e30a and fcn.b3f4.",
    "category": "",
    "verified": true
  },
  "__ACPANF": {
    "behavior": "Native: guards on fcn.36166 (ready/valid, bit0) and fcn.3a05a, then reads a byte length from the argument pointer (A4), computes (count+1)>>1, and copies/processes the length-prefixed block into a local -0x2a(A6) buffer; exact ACP field meaning not pinned down.",
    "category": "",
    "verified": true
  },
  "__ACPANLBL": {
    "behavior": "Native: fetches an arg via fcn.3070c, validates range 0..15, then writes a 4-bit field (bits 1-4) of RAM control word $b19e.w = (arg<<1)&0x1e with the surrounding bits preserved, and refreshes via fcn.30b3a.",
    "category": "",
    "verified": true
  },
  "__ACPANNOT": {
    "behavior": "Native: fetches an arg list via trap $2f2; if count>1 it passes the list pointer to fcn.6c02 and returns result+1, otherwise returns 0 \u2014 a thin arg-list dispatcher for the ACP annotation path.",
    "category": "",
    "verified": true
  },
  "__ACPANRES": {
    "behavior": "Native: stores the argument word into RAM $ba58.w, loads $b076.w into D0, and calls fcn.80a0 (the amplitude/measurement recompute), i.e. sets an ACP resolution parameter and triggers a measurement refresh.",
    "category": "",
    "verified": true
  },
  "__ACPANTIC": {
    "behavior": "Native query: a 2-instruction getter `move.w $b19e.w,D0; rts` that returns the ACP annotation control word $b19e.w (the same field __ACPANLBL writes).",
    "category": "",
    "verified": true
  },
  "__ACPAUTO": {
    "behavior": "Native: fetches an arg list via trap $2f2 and, when non-empty, drives helpers fcn.6314/fcn.6280/fcn.643e with immediate 0x64000007 and populates the $b24c.w parameter table \u2014 an ACP auto-setup that programs a parameter block.",
    "category": "",
    "verified": true
  },
  "__ACPAUTOB": {
    "behavior": "Native: fetches a boolean arg via fcn.306da and sets (bit0=1) or clears (bit0=0) bit 11 of RAM control word $b0d6.w, then refreshes via fcn.30e00 \u2014 an ON/OFF toggle.",
    "category": "",
    "verified": true
  },
  "__ACPBW": {
    "behavior": "Native: gated on flags bit3/bit2 of $ab77.w, compares the argument to segment/channel count $a9ae.w and conditionally calls fcn.2d8fe/fcn.2bab8, decrementing $a9ae.w \u2014 manipulates the ACP band/segment list.",
    "category": "",
    "verified": true
  },
  "__ACPBWC": {
    "behavior": "Native: clears $acca.w, then per an internal branch either zeroes $ad0c.w and copies parameter block $b12a into $acca via fcn.6a2e, or sets $ad0c.w=0xFFFF and copies block $b164 \u2014 selecting one of two ACP bandwidth-coupling configurations.",
    "category": "",
    "verified": true
  },
  "__ACPC": {
    "behavior": "Fetches a word-array descriptor via the DLP array helper jsr $2f2.w (pointer into (-$8,A6), element count into (-$a,A6)), then loops sign-extending each word and accumulating them into D6; returns the running sum in D0.",
    "category": "",
    "verified": true
  },
  "__ACPCC": {
    "behavior": "On arg==0 it reads a flag via jsr $18a.w (btst #0) and runs a copy/emit path ($6ef8, $32c22, $71e4, $70d2); otherwise it clamps the arg to >=0x191, transforms it via $7978, then builds a 3-byte code from ROM table $3a134 and appends the arg byte \u2014 constructing/dispatching an ACP command code.",
    "category": "",
    "verified": false
  },
  "__ACPCG": {
    "behavior": "Entry immediately bra's to $af28 (body outside the disassembled window); the reachable code reads status byte $ba87.w (valid range 0..9) and maps it through a jump table into result cell (-$a,A6), returning a small mapped code.",
    "category": "",
    "verified": false
  },
  "__ACPDONE": {
    "behavior": "Calls $5687c, then if RAM count $b12a.w > 0 iterates that many entries, reading a byte table at (A4-$4ed6) and calling $56414 per entry, then finalizes via $56c62 and $56096 (adjacent code sets $9afb bit2) \u2014 flushing/completing the ACP entry list.",
    "category": "",
    "verified": true
  },
  "__ACPDV": {
    "behavior": "Loads the DLP menu/struct pointer at $9562.w and reads offset $50, calls $154.w, then (gated on $afcf.w bit0) walks a descriptor block via PC-relative tables $27a86/$27b32, calling $27b88 and $27f62 on non-null longword fields \u2014 manipulating a DLP menu/struct.",
    "category": "",
    "verified": false
  },
  "__ACPEC": {
    "behavior": "The dispatch PC lands in a moveq jump-table slot (moveq #2,D1) that tail-jumps to the common routine at $3e702; it selects enum/error code 2 in D1 and hands off to that shared ACP dispatcher.",
    "category": "",
    "verified": true
  },
  "__ACPEM": {
    "behavior": "Operates on the ACP entry array (pointer $bb54.w, count $bb52.w, length $bfe6.w): clamps the arg to [1,0x64], reads/writes the index byte $bff1.w, and shifts entries (A2)->(A3) to insert/edit within the buffer.",
    "category": "",
    "verified": true
  },
  "__ACPERR": {
    "behavior": "Calls $6ef8 then for bit index 7..0 reads a bit-mask table at $3f2cc and tests the status word $b072.w; for each set bit it indexes a longword table at $3f29c and calls $7154.w to emit \u2014 reporting ACP error/status flags held in $b072.",
    "category": "",
    "verified": true
  },
  "__ACPES": {
    "behavior": "Thin wrapper that links, saves D0, calls system routine jsr $4cfe.w and returns; the actual effect lives in $4cfe and is not visible within the handler window.",
    "category": "",
    "verified": false
  },
  "__ACPGR": {
    "behavior": "Seeds D0=0 and calls setup routine $2dc60, then forwards three word arguments ((-$8/-$a/-$c relative to A6 / stack $8/$a/$c) to the common routine $2e816 (the sibling variant at $2e926 uses D0=1) \u2014 invoking ACP group/graph operation index 0.",
    "category": "",
    "verified": true
  },
  "__ACPICALC": {
    "behavior": "Fetches an array via $2f2; if non-empty it computes the sum-of-squares via $3e160 then runs a chain of FP-math library calls ($5df0,$64f0,$6016,$5e36,$6060,$6802) \u2014 a statistical calculation (mean/variance/RMS) over the array.",
    "category": "",
    "verified": true
  },
  "__ACPM": {
    "behavior": "Thin wrapper: pushes the longword arg at stack ($8,A6) and calls $3d32c (a single store/access primitive), then tail-returns \u2014 a single-element ACP store/move (companion of the two-element __ACPS).",
    "category": "",
    "verified": true
  },
  "__ACPMK": {
    "behavior": "Pushes constant selector 4 (move.w #$4,-(A7)) and calls $3efde; sibling entries push 3 and 5 into the same routine \u2014 invoking the ACP marker/mask handler $3efde with code 4.",
    "category": "",
    "verified": true
  },
  "__ACPPI": {
    "behavior": "Entry bra's to $3a822 (body outside window); the reachable subroutine indexes the ACP entry array at pointer $bb54.w (mask 0xfff), copies the selected entry into buffer $b24c.w and calls $35e \u2014 reading/writing the [$bb54] entry array and $b24c working buffer.",
    "category": "",
    "verified": false
  },
  "__ACPPWRUP": {
    "behavior": "Fetches two array descriptors via $2f2, processes them through $6bb8.w, then reads a flag via $aea.w and (bit0 set) writes sentinel 0xffff into the first array, finalizing via $9ee.w \u2014 power-up initialization of the ACP arrays.",
    "category": "",
    "verified": true
  },
  "__ACPRCLP": {
    "behavior": "Recall-panel routine: on arg==0xfc19 it takes a special path (math $6118, base pointers $bb4e.w and $bb4a.w), otherwise it validates the arg against a limit ($322), computes a table offset (+0x191, scaled via $6106/$6118) added to base $bb4e.w \u2014 recalling a saved ACP parameter/panel from the [$bb4a/$bb4e] table.",
    "category": "",
    "verified": true
  },
  "__ACPS": {
    "behavior": "Calls $3d32c twice \u2014 first with stack arg ($8,A6) at base address D0, then with ($c,A6) at D0+4 \u2014 storing two consecutive longwords; a two-element ACP store (companion of single-element __ACPM).",
    "category": "",
    "verified": true
  },
  "__ACPSAVP": {
    "behavior": "Save-panel routine: computes the target via $274d4 then uses a computed branch to dispatch to one of several store/emit paths referencing ROM constants $2b4ba/$2b482/$2b4ca/$2b4d0 through $22c/$2da \u2014 saving an ACP panel/parameter (complement of __ACPRCLP).",
    "category": "",
    "verified": false
  },
  "__ACPSKA": {
    "behavior": "Small accessor (link/deref/rts): takes a pointer argument, loads it into A4 and returns the 32-bit longword at *arg (move.l (A4),D0). A generic get-element accessor used by the ACP DLP; no MMIO/cal touch.",
    "category": "",
    "verified": true
  },
  "__ACPSKB": {
    "behavior": "Guard routine: reads config/state byte $bf02.w and compares to 0x5f; unless equal it calls subroutine $2c22 before rts. Conditional invocation gated on the $bf02 state byte.",
    "category": "",
    "verified": true
  },
  "__ACPSP": {
    "behavior": "Copies a length-prefixed (Pascal) string of up to 12 bytes from its pointer argument into the RAM buffer at $ac47.w (byte-by-byte via dbra); if the length byte is <=0 or >=13 it just clears the length byte at $ac47. Stores an ACP label/title string.",
    "category": "",
    "verified": true
  },
  "__ACPSUA": {
    "behavior": "Dispatches on $b07a.w bit 11 and $adc4.w bit 15, then reads the trace amplitude array via the display-array pointer $9552.w (indexed word reads) and converts values through helper $1cec2, using $b0c8 as an index. An ACP-value computation over the trace buffer.",
    "category": "",
    "verified": true
  },
  "__ACPSUD": {
    "behavior": "Parses two arguments via $2f2, reads sweep/frequency state $b0c6.w (span) and $b080.w (center), pushes them with a mode flag and calls compute routine $411a4, and checks arg[1]=='A' (0x41). An ACP band/delta computation.",
    "category": "",
    "verified": true
  },
  "__ACPSUK": {
    "behavior": "Loads ROM template pointer 0x17806 into D0 and calls output helper $7154 (which formats it into the response buffer $bbca via $4326) then terminator $70d2. Emits a fixed query-response string built from ROM template 0x17806.",
    "category": "",
    "verified": true
  },
  "__ACPSUM": {
    "behavior": "Seeds config byte $b05c.w from $9b20.w, then falls to $546f4 which calls helper $6862 before returning. Copies an HP-IB/keyboard address-style config byte and runs a measurement-setup helper.",
    "category": "",
    "verified": true
  },
  "__ACPSUPV": {
    "behavior": "Parses an argument via $6ef8, range-checks it against 0x62/0x63, and validates/indexes the RAM table spanning $a7da.w..$a896.w (using char-compare and helper $439a). An ACP setup-value validation against a table.",
    "category": "",
    "verified": true
  },
  "__ACPSUTV": {
    "behavior": "Parses two arguments via $2f2, compares them, points A4 at RAM buffer $b24c.w when equal, then loops over a count argument iterating a table structure. An ACP table-value fill/read over $b24c.",
    "category": "",
    "verified": true
  },
  "__AMACT": {
    "behavior": "Entry dispatches via $598b6; one branch sets display-mode flag $bc64.w bit 13 and scans/copies the entered string in buffer $9AD6 (length at $9ad6, chars scanned for space 0x20) into a local. An AM 'active' measurement setup that consumes the $9AD6 entry buffer.",
    "category": "",
    "verified": true
  },
  "__AMANOT": {
    "behavior": "Guard/dispatch: range-checks RAM token word $bfee.w against [0x218d,0x2195]; in-range branches to body $4b532, otherwise to $4b71e. The body/data past 0x4b486 is a table (disassembles as zero-fill), so only the gate is confirmed.",
    "category": "",
    "verified": false
  },
  "__AMDISP": {
    "behavior": "Calls $5862a, masks display-mode flags $bc64.w &= 0xf007 (clears bits 3-11), copies $bc28.w -> $bc26.w, and calls display setup $561e8. Configures the AM measurement display mode.",
    "category": "",
    "verified": true
  },
  "__AMLGSCL": {
    "behavior": "AM log-scale set: gets arg via $5de94, sets $bffe.w=0x204, converts the value through $643e and stores it to $94ee.w clamped to [0,0xFFFF], sets valid-flag $94ec.w, then calls $ff74. Writes the log-scale value/flag.",
    "category": "",
    "verified": true
  },
  "__AMLNSCL": {
    "behavior": "Despite the 'linear scale' name, the handler reads MMIO POST/status latches 0xFFF612 and 0xFFF610, NOTs the word, and outputs it plus RAM words $bb2e/$bb30/$bb32 as a comma-separated, terminated response (via $71e4/$70ca/$70d2). It is a diagnostic status query, not a scale setter \u2014 name/behavior mismatch (possible dispatch mis-resolution).",
    "category": "",
    "verified": true
  },
  "__AMMEAS": {
    "behavior": "Arms an AM measurement: writes measurement control block $ab7c/$ab80/$ab84=1, $ab82=0x15, $a9b2=1, clears $ab77.w bit4, sets $ab76.w bits 13/14, sets $ab7e=1, and calls measurement subs ($2bb16/$2ba64/$2bd80/$2c5b2) plus loads DLP source 0x2f23c via $730.",
    "category": "",
    "verified": true
  },
  "__AMNOSIG": {
    "behavior": "If $b0e6.w > 0 it aborts via $2bc (D0=1) and exits; otherwise it parses arguments via $32c22 (x2) and $3e318 and bounds-checks them to [1,0x400]. Validates AM 'no-signal'/measurement parameters.",
    "category": "",
    "verified": true
  },
  "__AMRUN": {
    "behavior": "Thin wrapper: bsr $2d2c2, which draws a rect via $39a (params 0xaaaa,0xaf,0x18f,1), calls $2d21c(D0=4), clears measurement-active flag $ab7e.w, and calls $76c/$62e. Restarts/redraws the AM measurement.",
    "category": "",
    "verified": true
  },
  "__AMSIG": {
    "behavior": "Dispatches on the command token in D0 (0x3421,0x2b0f,0x2b44,0x30e,0x30f,...) and, via helper $19754, sets or clears the corresponding bit in annunciator/flag word $bff6.w (bits 0xa/0x9/0xe etc.), or stores an arg to $9fbc.w. An AM signal/annunciator flag setter.",
    "category": "",
    "verified": true
  },
  "__AMTERM": {
    "behavior": "Native handler at 0x22c9a: calls $223a2, then on $bfee (mode word, compared to 0x2190) and flag $b0c8 selects among ROM string/descriptor constants (0x4786A, 0x7271) and copies 8-byte descriptor pairs into locals to format an amplitude/units terminator label. Exact semantic (which string) not pinned down.",
    "category": "display-format",
    "verified": false
  },
  "__CHPANLBL": {
    "behavior": "Native at 0x5b1d2: takes an argument string pointer; if the first byte is 0 it defaults to the menu register $956a, otherwise string-matches the arg against ROM constants (0x5be84/0x5be8e) via the compare routine jsr $6a52 to select a channel-annotation label id.",
    "category": "annotation",
    "verified": true
  },
  "__CHPANRES": {
    "behavior": "Native at 0x5ba7e: after a guard jsr $18a and a count from __CHPM (bsr $5b16a), tests/sets flag $956e.w and restores the 4-entry, 6-byte-record annotation table at $-6604(A4) by copying saved words from the table at $-6a90(A0) (a 'restore' of the CHP annotation state).",
    "category": "annotation",
    "verified": true
  },
  "__CHPANTIC": {
    "behavior": "Native at 0x5b410: copies a 26-byte (0x1A) record from the arg pointer into a local buffer, then scans the 4-entry annotation table at $-6604(A4) (limit index 3) for a free/next slot \u2014 adds/positions an annotation ('tic') entry.",
    "category": "annotation",
    "verified": true
  },
  "__CHPC": {
    "behavior": "Native at 0x5b380: if menu register $956a is set it deletes that menu (bsr $5b236) and rebuilds the menu via the menu-loader bsr $5acb2, then clears the annotation-table records (clr.w $-6604(A4) and $-65ec(A4)) for indices 1..2 \u2014 a clear/reset of the channel-annotation display.",
    "category": "annotation",
    "verified": true
  },
  "__CHPCG": {
    "behavior": "Native at 0x4d992: if count $9fb4 > 1, opens a string builder (jsr $6ef8) and loops $9fb4-1 times reading longword entries from the table at $-5fba(A4), converting each (jsr $5140) and appending (jsr $7154/$70ca) to build a formatted list/graph string.",
    "category": "display-format",
    "verified": true
  },
  "__CHPERR": {
    "behavior": "Native at 0x5b7c2: if the channel menu $956c has a valid record ($-65ec(A4)!=0) and the arg code D0<=0x37, writes softkey/message text into that menu slot via bsr $5a62a and bsr $5a6ca \u2014 posts an error/message string on the CHP menu.",
    "category": "menu",
    "verified": true
  },
  "__CHPGR": {
    "behavior": "Native getter at 0x3d44e: returns the 32-bit value stored in RAM $bffa.w (move.l $bffa.w,D0; rts) \u2014 reads a global CHP register/pointer.",
    "category": "query",
    "verified": true
  },
  "__CHPM": {
    "behavior": "Native at 0x5b16a: iterates indices 0..3 over the 6-byte-record table at $-6604(A4) and returns in D0 the count of records whose first word is non-zero \u2014 counts active channel-annotation/marker entries.",
    "category": "query",
    "verified": true
  },
  "__CHPS": {
    "behavior": "Native at 0x5b6e2: copies a 4-byte record from the arg pointer at $c(A6) into a local buffer, extracts the first-byte code (ext.w) and branches on it (compares against 0x48 'H' etc.) to select a channel-annotation display state/units.",
    "category": "annotation",
    "verified": true
  },
  "__FFAFFTM": {
    "behavior": "Native at 0x3c18c: reads setting $a35a (negated as a default), bounds-checks the arg record's first byte against 0x1e (30 entries) and on overflow reports an error with ROM string 0x3d0fe (jsr $2da); processes an FFT/trace marker entry within a 30-slot table.",
    "category": "marker",
    "verified": true
  },
  "__FFAPC": {
    "behavior": "Native at 0x3b3cc: invokes the shared FF dispatcher bsr $3b27e with selector 5 (which registers bit 5 in flag word $bef8 and handles the '?' query); on success computes a value (jsr $62a6/$64ba) and stores the 32-bit result to $bf1a.w.",
    "category": "config",
    "verified": true
  },
  "__FFATOC": {
    "behavior": "Native thin wrapper at 0x3b330: pushes the arg and calls the shared FF dispatcher bsr $3b27e with selector 0x0d, which sets bit 0x0d in the FF flag word $bef8 (and answers a trailing '?' as a query) then returns.",
    "category": "config",
    "verified": true
  },
  "__FFCALC": {
    "behavior": "Native getter at 0x3d2c8: even-aligns the arg pointer (bclr #0 of its low byte) and returns the 16-bit word read from that address (move.w (A4),D0; rts) \u2014 a fetch of a word at the given RAM address.",
    "category": "query",
    "verified": true
  },
  "__FFCFFTM": {
    "behavior": "Native at 0x2d5c4: iterates an FFT-marker index table at $-5652(A4) (word-indexed), and for each non-empty slot loads the 8-byte record from $-5624(A0) (index*8) and processes rows with a 0x28 (40) stride \u2014 walks/computes the FFT marker records.",
    "category": "marker",
    "verified": true
  },
  "__FFCLIP": {
    "behavior": "Native at 0xcf8c: sets clipping-enable flags in RAM $b213 \u2014 arg==1 sets bit 3 (else clears it), arg==2 sets bit 5 (else clears it); toggles two clip-mode bits.",
    "category": "config",
    "verified": true
  },
  "__FFFSM": {
    "behavior": "Native at 0x3f63e: builds a formatted status string (jsr $6ef8 open, template ptr 0x40604 via jsr $7154, close via jsr $70ca) using index $b234 to look up a byte in the table pointed to by $a80 \u2014 formats an FF state/mode message.",
    "category": "display-format",
    "verified": true
  },
  "__FFLCLPK": {
    "behavior": "Native at 0x271c0: runs a local-peak search loop (bsr $26ef4/$26a2a/$26b5a) that, gated on flag $ac76 and $acc9 bit 4, captures the peak position from $ac78 and accumulates $ac7c into a running total while stepping a 0x20 index \u2014 a local peak-find over sweep/marker RAM.",
    "category": "marker",
    "verified": true
  },
  "__FFMUHKC": {
    "behavior": "Reads state words $b0e6 and $b0ec: if $b0e6>0 dispatches D0=1 via jsr $2bc, else if $b0ec==0x31 dispatches D0=2 via $2bc; the default path calls $3b27e with D0=6, i.e. set/query bit 6 of the $bef8 flag register (with '?'-query support).",
    "category": "state-flag",
    "verified": true
  },
  "__FFRLA": {
    "behavior": "Toggles annotation state (tests $ab7f.0, clears $ab77.4) then redraws a display element id 0x191 twice at +$a9ac and -$a9ac through the display objects at $9556/$955e (jsr $6b9e), finishing with $2bc44 and $2c5b2 \u2014 a symmetric limit/reference-line redraw driven by amplitude value $a9ac.",
    "category": "display-redraw",
    "verified": true
  },
  "__FFSFFTM": {
    "behavior": "Pure getter: if $ab77 bit4 is set it returns bits [6:5] of $ab76 (lsr #5, mask 3 \u2192 0..3), otherwise returns 4, in D0. Queries a small mode field.",
    "category": "query-getter",
    "verified": true
  },
  "__FFSID": {
    "behavior": "String-compares the argument against constant $41d5c ($6a52); on match, if any of $b0b0/$b0aa/$b0b6/$b0bc is nonzero and $bef8 bit10 is set it sets bit1 of $a5d5, then emits code 0x4c via jsr $184.",
    "category": "parser-setter",
    "verified": true
  },
  "__FFSIM": {
    "behavior": "Sets $b20e bit13 (busy/guard) and jsr $178, sets $b072 bit9 (a display/label-enable flag), issues command 0x74 via jsr $4c0, then clears $b20e bit13.",
    "category": "command-dispatch",
    "verified": true
  },
  "__FFSSHKC": {
    "behavior": "Guarded by $a380: copies the argument string (byte count clamped to 0xc via $7978) into RAM buffer $a386, storing the length at $a382 and length+1 at $a385, and toggles the $a380 busy flag around the copy.",
    "category": "string-store",
    "verified": true
  },
  "__FFSTOC": {
    "behavior": "Calls $3b27e with selector 2 (query bit 2 of the $bef8 flag register); if that bit is set it sets bit15 of $befa.",
    "category": "state-flag",
    "verified": true
  },
  "__FFSTPA": {
    "behavior": "Reads the hardkey-menu register $b1ee; when $b1ee < -1 it computes menu index (-b1ee-2)*5 and emits the 5 softkey-label strings for that menu from the longword table at $3c6ac via jsr $7154, each followed by separator 0x1c ($708a).",
    "category": "menu-output",
    "verified": true
  },
  "__FFTAC": {
    "behavior": "Argument parser with query support: if arg byte[1]=='?' it emits the value $a35a via $3a308 (context 0x6000); otherwise it validates the leading token byte against limit 0x1e (30) before further processing. $a35a holds the queried state.",
    "category": "parser",
    "verified": true
  },
  "__FFTAI": {
    "behavior": "Thin wrapper: pushes the constant token/string pointer $3d108 and calls the shared __FFTAC parser at $3a374 with D0=0.",
    "category": "parser-wrapper",
    "verified": true
  },
  "__FFTAMC": {
    "behavior": "Trampoline that loads selector D1=1 and jmps to the shared numeric parameter set/query routine at $3e702 (which parses the numeric arg via $2f2). Which physical parameter selector 1 controls is not resolvable from this stub.",
    "category": "table-dispatch",
    "verified": false
  },
  "__FFTAMD": {
    "behavior": "Indexes a word table in high RAM (effective ~$b060 + index*2) after fetching a parameter via $7c0, tests the bit selected by arg word ($8,A6) with btst (sne), and emits the resulting 0/1 via jsr $77e.",
    "category": "query-getter",
    "verified": true
  },
  "__FFTCD": {
    "behavior": "Emits the HP/488.2 arbitrary-block header '#A' (0x2341) via $7108, then the length word ($8,A6), then outputs that many data bytes ($73fe/$7068) \u2014 formats a binary definite-length block response.",
    "category": "block-output",
    "verified": true
  },
  "__FFTCLAC": {
    "behavior": "Trampoline that loads selector D1=0x0c and jmps to the shared numeric parameter set/query routine at $3e702 (same family as __FFTAMC). The specific parameter controlled by selector 12 is not resolvable from this stub.",
    "category": "table-dispatch",
    "verified": false
  },
  "__FFTCO": {
    "behavior": "Keyword parser: matches the argument against several constant strings ($e520/$e526/$e52a via $6a52) and stores the supplied value into distinct config cells \u2014 $baa0 (with value transform via $b38e/$c30a +0x5000), $ba96, or $ba98 \u2014 depending on which keyword matched.",
    "category": "parser-setter",
    "verified": true
  },
  "__FFTDEFS": {
    "behavior": "Emits the literal characters 'HP' (0x4850) via $7108, then the value from $bfee.w via $71e4, then a message terminator ($708a/$70d2) \u2014 outputs an 'HP'-prefixed response string carrying $bfee.",
    "category": "response-output",
    "verified": true
  },
  "__FFTEXIT": {
    "behavior": "Pure getter: reads the word at $bfee.w and returns it in D0 (an exit/status/current-command code register).",
    "category": "query-getter",
    "verified": true
  },
  "__FFTFC": {
    "behavior": "Allocates a ~0x112-byte stack buffer, clears $a990.b, and iterates 12-byte records (fetched via $6bb0, processed via $72a) comparing the words at frame offsets -0x114/-0x116 and referencing table $b24c; the exact subsystem it walks is not clear from the entry code.",
    "category": "record-iterator",
    "verified": false
  },
  "__FFTI": {
    "behavior": "Sets the 2-bit FFT-mode field in RAM state word $bc64 (masks 0x3FFF, ORs 0x4000 \u2192 field=01), clears state word $9b26, then calls the FFT redraw/setup routine $561e8.",
    "category": "",
    "verified": true
  },
  "__FFTKEYS": {
    "behavior": "FFT softkey-menu handler: errors out (jsr $2bc) when the front-panel-busy flag $b0e6>0, otherwise clears long $bf0a, reads keyboard/state byte $bf02 and cal-scratch RAM $2fc003, and dispatches the FFT menu/softkey ('_'=0x5f) via $54d78/$7128/$754c.",
    "category": "",
    "verified": true
  },
  "__FFTMI": {
    "behavior": "Stores the command argument word into RAM word $9e50; if the parsed numeric arg is zero it issues internal command/menu index 0x37 via jsr $42a.",
    "category": "",
    "verified": true
  },
  "__FFTONIP": {
    "behavior": "Gated by helper $5de94; when enabled it calls low-mem API routine $ff74 and toggles FFT sub-mode processing via $5df8c/$5dfd2 (selector args 0/1, checking $94ec), otherwise disables them (selector 4).",
    "category": "",
    "verified": true
  },
  "__FFTPAAC": {
    "behavior": "Stores the argument word into RAM state word $ba54, then loads $b076 and dispatches an internal command via jsr $1af8.",
    "category": "",
    "verified": true
  },
  "__FFTPAMC": {
    "behavior": "Enumerated 5-choice parameter using the keyword table at ROM $40694: on '?' query it reads the packed setting $b0aa and outputs the current choice ($7154/$70d2); on set it parses the keyword (jsr $39762) and updates $b0aa via $40dc2.",
    "category": "",
    "verified": true
  },
  "__FFTPAMK": {
    "behavior": "Trivial setter \u2014 stores the command argument word into RAM state word $9b24.",
    "category": "",
    "verified": true
  },
  "__FFTPAMM": {
    "behavior": "Enumerated 4-choice parameter analogous to FFTPAMC using the keyword table at ROM $406a8: '?' query outputs the current choice from $b0aa; set parses the keyword (jsr $39762), errors ('O' via jsr $184) on choice 3, else updates $b0aa via $40dc2.",
    "category": "",
    "verified": true
  },
  "__FFTSAC": {
    "behavior": "Address accessor: word-aligns a caller-supplied pointer (bclr #0 on its low byte) and stores the 16-bit value passed at (8,A6) into that address (A4).",
    "category": "",
    "verified": true
  },
  "__FFTSC": {
    "behavior": "Computes a statistic over a trace/FFT buffer: errors if $b0e6>0, else fetches buffer+count via $32c22, counts and sums the samples exceeding threshold $b080, and returns the over-threshold average via jsr $61fe.",
    "category": "",
    "verified": true
  },
  "__FFTSTAC": {
    "behavior": "10-way enumerated dispatch: parses the keyword against a 10-entry offset table at $20f0e (jsr $862), each case pushing the arg and calling $20ea0 with a distinct bit-selector (0x000/0x800/0x100/\u2026).",
    "category": "",
    "verified": true
  },
  "__FFTVDI": {
    "behavior": "Sets the 2-bit FFT-mode field in RAM state word $bc64 to 10 (masks 0x3FFF, ORs 0x8000), clears $9b26, then calls the FFT redraw routine $561e8.",
    "category": "",
    "verified": true
  },
  "__GGTSWSW": {
    "behavior": "DLP macro that sets up adjacent-channel-power (ACP) sweep parameters \u2014 defining the CHANNEL SPACING active function (ACPSP, default 25 kHz) and channel-bandwidth (ACPBW) when they are unset.",
    "category": "",
    "verified": true
  },
  "__GTCLPAR": {
    "behavior": "DLP macro: if the FFT-stop flag __FFTSTF==1 it copies FFTSTOP into MF (marker/measurement frequency), then terminates with NV.",
    "category": "",
    "verified": true
  },
  "__GTCLRP": {
    "behavior": "Runs a short DLP snippet 'KH '% AM|ON' (invokes classic command KH with a literal string); the fragment's exact meaning is unclear from the (possibly truncated) source.",
    "category": "",
    "verified": false
  },
  "__GTCLUP": {
    "behavior": "DLP macro that runs DLPINIT then DLPTERM then NV \u2014 initializes and immediately terminates the downloadable-program state (cleanup/reset).",
    "category": "",
    "verified": true
  },
  "__GTCRBW": {
    "behavior": "Conditional DLP macro keyed on the ANNOT (annotation) flag: if ANNOT==0 it clears __N,0 / __A,0 and runs EM, else if __N==0 it runs __FFTAI and conditionally __FFTCD \u2014 configuring FFT annotation/display state.",
    "category": "",
    "verified": true
  },
  "__GTCST": {
    "behavior": "DLP macro that VRD (variable-read/restores) a series of saved __CZ* state variables (__CZBLEDS, __CZDET, __CZSP, __CZSS, __CZLG, __CZRB, __CZVB\u2026), restoring the channel/measurement state set.",
    "category": "",
    "verified": true
  },
  "__GTCVBW": {
    "behavior": "Trampoline running a DLP macro that (re)defines the measurement's softkeys: KEYCMD39 is the SCALE LOG/LIN toggle that blocks LIN with error \"LIN Scale can not be used for this meas.\", KEYCMD74 is the SWEEP CONT/SGL toggle (ACPCONTM/ACPSNGLM keyed on MSBIT(9,10)), plus KEYDEF8041/8044. It configures the ACP measurement's scale and sweep-mode soft keys.",
    "category": "",
    "verified": true
  },
  "__GTDADJ": {
    "behavior": "Trampoline whose DLP macro draws the adjacent-channel boundary bars: zeroes DA, conditionally sets pen from MSBIT(18,10), then does pen-up/pen-down vertical strokes at graticule columns __CZJCA-1 and __CZJCB-1 (channel-A/B edge indices) between rows 1 and 50. It paints the ACP channel-limit marker lines.",
    "category": "",
    "verified": true
  },
  "__GTDMKRF": {
    "behavior": "Trampoline; the DLP macro refreshes the ACP density/PSD annotation (returns early if ANNOT==0): writes \"CSP\" (=ACPSP span) and \"CBW\" (=ACPBW channel BW) labels via __ACPANF, then displays channel power (DSPLYA CHPWR) and power-spectral-density (DSPLYPSD CHPSD) at fixed pen positions. It paints the density-marker result text.",
    "category": "",
    "verified": true
  },
  "__GTDMKRSTF": {
    "behavior": "Trampoline whose DLP macro recomputes the averaged density-graph trace: sets __K=__CZBWTE, calls __ACPBWC, __W=__CZBNFDU, applies TRC-8000 offset then PAVGGRAPH,TRC,TRA,__CZBWTE,__W (power-average TRC into TRA) and clears the density marker (DRTYMK CLR,C). It restores/rebuilds the ACP average-graph state.",
    "category": "",
    "verified": true
  },
  "__GTEXIT": {
    "behavior": "Trampoline; despite the name it paints the ACP results annotation (returns if ANNOT==0): \"CSP\"/\"CBW\" span/BW labels via __ACPANF, then \" Pwr:\" (DSPLYA ACPPWRTX), \" Low:\" (ACPLOWER dB) and \" Up: \" (ACPUPPER dB) \u2014 the adjacent-channel lower/upper power results block, with a compare of ACPLOWER vs ACPUPPER controlling blanking.",
    "category": "",
    "verified": true
  },
  "__GTGCTL": {
    "behavior": "Trampoline whose DLP macro integrates adjacent-channel power: clamps integration indices __J/__K into [1,401] for the lower channel (__CZILA..__CZILB), runs __ACPBWC/__ACPPI to get ACPLOWER=__R-ACPPWRTX, repeats for the upper channel (__CZIUA..__CZIUB) to get ACPUPPER, then MXM ACPMAX. It computes the lower/upper ACP ratios relative to carrier.",
    "category": "",
    "verified": true
  },
  "__GTGDRV": {
    "behavior": "Trampoline; the DLP macro is pure variable declaration/init for the window/gate driver: VARDEF __WN_ACT=0, __WN_IDO=-1, __WN_IDA=-1, __WN_VAR_1..5=0, then NV. It initialises the DLP window-management state variables.",
    "category": "",
    "verified": true
  },
  "__GTGSCLF": {
    "behavior": "Trampoline whose DLP macro, when HAVE(GATE), variable-declares/reads the full gated-sweep state set (__GGTENTR, __GGTIN, __GGTVB, __GGTRB, __GGTSTM, __GGTRE, __GGTPWID, __GGTPRI, gate delay __GGTTMPD=GD, gate length __GGTTMPL=GL, __GGTRCTL=1) and then calls __GTMAKEVAR/__GTMAKEREM/__GGTSWSW/__GTSWDEF/__GTDMKRF/__GTLMKRF to rebuild gate menus and markers. It performs full gated-sweep variable/menu initialisation.",
    "category": "",
    "verified": true
  },
  "__GTGSCLSTF": {
    "behavior": "Trampoline; the short DLP macro is a gate on/off state restore \u2014 IF(HAVE(GATE)) then IF(GSTAT(0)) GATE ON else GATE OFF. It re-applies the saved gate enable state from status bit GSTAT(0).",
    "category": "",
    "verified": true
  },
  "__GTGTOFF": {
    "behavior": "Trampoline whose DLP macro executes KH /SWEEP|DELAY/, ACTVF __GTSWDE,0 \u2014 sets the SWEEP DELAY softkey's active/highlight flag via __GTSWDE. It reconfigures/turns off the sweep-delay (gate) soft key.",
    "category": "",
    "verified": true
  },
  "__GTGTON": {
    "behavior": "Trampoline; the DLP macro sets the GATE CTL EDGE/LVL softkey based on gate status: IF(GSTAT(2)) KEYLBL 544,/GATE CTL|EDGE LVL/ else KH /GATE CTL|EDGE LVL/,0,GSTAT(1). It updates the gate-control soft key label/highlight for edge-vs-level triggering.",
    "category": "",
    "verified": true
  },
  "__GTGTRI": {
    "behavior": "Trampoline whose DLP macro toggles the FFT measurement state: if __FFTA!=0 it clears __FFTA, saves FFTSTAT into __FFTSTF and sets __FFTA=1; else if (__FFTDIF==0 && FFTSTAT!=0) it zeroes FFTSTAT. It enables/disables the FFT (gated) processing state.",
    "category": "",
    "verified": true
  },
  "__GTINITVAR": {
    "behavior": "Trampoline; despite the name the DLP macro drives a SIGNAL-CLIPPED overload annotation: sets pen to (5,190), and IF(1==__O) writes TEXT 'SIGNAL CLIPPED' with __A=1, else writes blanks to erase it with __A=0. It shows/clears the ADC-overload warning based on flag __O.",
    "category": "",
    "verified": true
  },
  "__GTINP": {
    "behavior": "Trampoline whose DLP macro acquires and shows the ACP trace: __ACPSUD (setup), TRC-8000 offset, VIEW TRC, marker M1, __ACPCG (compute graph), NV. It performs the ACP input/acquire-and-display step.",
    "category": "",
    "verified": true
  },
  "__GTKSW": {
    "behavior": "Trampoline; the DLP macro snapshots the current instrument settings into __CZ* shadow variables: __CZBLEDS=MSFIELD(8,15,16), __CZDET=DET, __CZSP=SP, __CZSS=SS, __CZLG=LG, __CZAT=AT, __CZRB=RB, __CZVB=VB, __CZST=ST. It saves detector/span/scale/atten/RBW/VBW/sweep-time state before the measurement changes them.",
    "category": "",
    "verified": true
  },
  "__GTLADJ": {
    "behavior": "Trampoline whose DLP macro sets up the Occupied-Bandwidth measurement: __ACPSUD/__M5/__ACPSUM, WINITM 17/18/19,0 (init measurement windows), __ACPICALC, __OBWANLBL label, defines KEYCMD1288 as the % POWER key (OBWPCT), then __ACPSKA. It configures OBW windows, softkeys and labels.",
    "category": "",
    "verified": true
  },
  "__GTLMKRF": {
    "behavior": "Trampoline; the DLP macro computes the occupied-bandwidth result: MOV OBWBW, PWRBW TRA, OBWPCT (bandwidth holding OBWPCT% of trace power), derives band edges OBWLOWER/OBWUPPER from marker fields MSFIELD(41)/MSFIELD(38) and per-point span __CZSPTE=SP/400, center error OBWFERR, then integrates power (__ACPPI\u2192OBWPWR) and calls __OBWANRES. It calculates the OBW frequency edges and power.",
    "category": "",
    "verified": true
  },
  "__GTLMKRSTF": {
    "behavior": "Trampoline whose DLP macro paints the OBW result annotation (returns if ANNOT==0): \"(OBWPCT%)\", \"CSP\" span, \" OBW: \" (OBWBW), \" \u00f9Fc: \" (OBWFERR center-freq error) via __ACPANF, and \" Pwr:\" (DSPLYA OBWPWR). It renders the occupied-bandwidth results text block.",
    "category": "",
    "verified": true
  },
  "__GTMAA": {
    "behavior": "DLP trampoline (pushes len=115 src @062a0a, calls scheduler $d18.w) that initializes the Peak-Zoom scratch registers: sets DLP local variables __I..__P all to 10, __S=0, __T=0, __U=24, and __X=1.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAB": {
    "behavior": "Trampoline running a Peak-Zoom keypress guard: IF(__PZKP==0){__PZKP=1;}PKZOOM; ELSE MOV PKZOOM,PKZOOM; i.e. on first press it sets the flag and runs PKZOOM, otherwise it re-echoes the PKZOOM active-function value.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAC": {
    "behavior": "Trampoline that defines the PKZOOM active function when unset (ACTDEF PKZOOM,'FINAL SPAN',1E6,ABSHZ,...) with a 1 MHz default and an embedded __PZEXE branch gated on __PZADIF, then displays variables __PZADIF/__PZKP/PKZMOK via VRD.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAD": {
    "behavior": "Trampoline for the 827-byte Peak-Zoom acquisition engine: TS/MKPK HI/__PZGETFRA, compares marker freq/amp against limits __A/__N and ref-level RL, iteratively lowers RL (MKRL) and re-sweeps, narrows span (SP/4) and recenters (CF/MKCF/__PZZOOM/__PZCHKIT), and reports 'Signal not found' when the PEAKS count __K==0.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAE": {
    "behavior": "Trampoline continuing Peak-Zoom final narrowing: repeats MKRL/TS until MKA<=RL, goes CONTS with MKTRACK ON to track the peak, clamps span to 350 kHz, applies 8596-specific handling above 2 GHz, and ends in marker noise/signal-search modes (MKN, MKSS).",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAF": {
    "behavior": "Trampoline for Auto-FFT teardown: DISPOSE of ONDELAY/ONEOS and softkeys 61/67/297/131/8044/8041, restores DET/ANNOT/DRTYMK, blanks TRC/TRB, and zeroes all FFT state vars (FFTPCTAMR, __FFTSTF, __FFTA, __FFTDIF, FFTPCTAM, FFTSTAT, FFTCLIP, FFTSTOP).",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAG": {
    "behavior": "Trampoline that installs the Auto-FFT softkey menu: runs __FFTPAMK, then KEYDEFs key 131 (ANALOG+ ON/OFF -> __FFAPC), 61/67 (MARKER->MID SCRN -> FFTMM), 8044 (__FFSSHKC) and 8041 (__FFMUHKC).",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAH": {
    "behavior": "Trampoline that redraws the FFT graticule/annotation: toggles GRAT off/on when __M==1, clears window items (WINITM 7..10) and text via PUPA/TEXT blanks, and when annotation is on draws the 'FFT START 0 Hz' / 'FFT STOP' labels via PA/TEXT.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAI": {
    "behavior": "Trampoline for one-time Auto-FFT init: IF(0==__FFTDIF){__FFTDEFS; DLPINIT __FFTEXIT; __FFTDIF=1;} \u2014 runs the FFT variable definitions, registers the __FFTEXIT handler, and sets the 'defined' flag so it only runs once.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAJ": {
    "behavior": "Trampoline defining the 'MARKER / AUTO FFT' softkey header via KH, made conditionally active by the guard IF((1==__M)&&(1==__FFTDIF)).",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAK": {
    "behavior": "Short Auto-FFT activation trampoline: runs FFT video-display-init __FFTVDI, sets flag __M=1, then runs the FFT auto-compute macro __FFTAC.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAKECOM": {
    "behavior": "Trampoline that blanks trace C (BLANK TRC), applies a FLATTOP window to it (TWNDOW TRC,FLATTOP), and stores the windowed trace sum into __T (MOV __T,SUM TRC) \u2014 a windowed power-sum computation.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAKEREM": {
    "behavior": "Trampoline dispatching on the gate flag __GTPFLG: +1 -> __GTWID (gate width), -1 -> __GTPRIZ; a small gate-menu remake selector.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAKEVAR": {
    "behavior": "Trampoline for the core single Auto-FFT computation: __FFTMI, sets trace states (S2/A1/A2) per MSBIT(9,*) status bits, TRDSP TRA OFF, TS, FFT TRB,TRA,TRC, VIEW TRB, conditional marker setup (MKACTV/MKTRACE/MKREAD FFT) gated on MSFIELD(37,*), registers ONEOS'__FFTCO;', and sets __FFTSTF=1 / FFTSTAT.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAKWINA": {
    "behavior": "Trampoline defining softkey KEYCMD 1289: toggles the ACP graph-marker flag (IF(ACPMK>0){ACPMK=-1;}ELSE{ACPMK=1;}) and redefines the 'GRPH MKR ON OFF' softkey label with its on/off state.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAKWINB": {
    "behavior": "Trampoline for 846-byte ACP channel-window geometry: computes span-per-bin __CZSPTE=SP/400, half-bandwidth-in-bins __CZIHBS, center-span-in-bins __CZICS, and the integer trace-bin boundaries __CZILA/ILB/ICA/ICB/IUA/IUB (channel A) plus __CZJ* (channel B) clamped to [1,401], and the channel bin-width __CZBWTE.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAO": {
    "behavior": "Trampoline that sets reference level RL from marker amplitude MKA: MPY RL,MKA,1.059 when AUNITS==3, MPY by 1.122 when AUNITS==4 (voltage/power unit scaling), else ADD RL,MKA,.5 \u2014 the Peak-Zoom marker-to-ref-level step.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMAP": {
    "behavior": "Trampoline for the continuous Auto-FFT recompute path (companion to __GTMAKEVAR): __FFTMI, selects trace states A1/A2/A3/S2 and TRDSP per MSBIT(9,*) bits, registers ONEOS'__FFTCO;', either recomputes (FFT TRB,TRA,TRC; VIEW TRB) or just TS depending on __FFTSTF, updates markers (MKTRACE/MKREAD FFT), and sets __FFTSTF=1 / FFTSTAT.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__GTMNA": {
    "behavior": "Trampoline (pushes ROM DLP source 0x60504, calls scheduler $d18) that tears down the zoom-marker window: if in MENU 202 returns to MENU 0, and if a window is active (__WN_ACT) it disables/copies the zoom marker (ZMKCP/ZMKACT/ZMK OFF), re-inits the window (WININIT), disposes DLP object 8049 and clears __WN_ACT.",
    "category": "",
    "verified": true
  },
  "__GTMNB": {
    "behavior": "Trampoline running DLP source 0x606A4 that updates the zoom window: queries the active window (WINACTIVE?), temporarily turns off video averaging (VAVG OFF), finds the next zoom-marker peak (ZMKPK NL), runs the pan/zoom sub __WN_P_ZOOM, then restores VAVG ON.",
    "category": "",
    "verified": true
  },
  "__GTMND": {
    "behavior": "Trampoline running DLP source 0x60952 \u2014 the TOI (third-order-intercept) run dispatcher: if the personality is enabled (__SOENBL) and MODE==0 (spectrum) it runs the TOI measurement __TOIRUN when TOI is set, else re-inits the DLP (DLPINIT/DLPTERM); in any other mode it warns (WRNM).",
    "category": "",
    "verified": true
  },
  "__GTMNE": {
    "behavior": "Trampoline running DLP source 0x60A5C that performs the TOI peak-find: PEAKS on trace __TRC locates the two fundamentals and two IM3 products, sorts them by position (__E..__H), validates count/spacing/excursion into status __T, then converts the four amplitudes (CTA), picks lin/log units, and calls __TOIDISP.",
    "category": "",
    "verified": true
  },
  "__GTMNF": {
    "behavior": "Trampoline running DLP source 0x60D28 that computes the third-order intercept value: takes LOG of the four amplitudes __A-__D, forms two intercept estimates __Q/__R, stores the smaller as TOIR, EXPs it back to linear and applies lin/log scaling (__TOLNSCL/__TOLGSCL).",
    "category": "",
    "verified": true
  },
  "__GTMNH": {
    "behavior": "Trampoline running DLP source 0x60E76 that converts the four TOI trace-point amplitudes __TRA[__F/__G/__E/__H] from raw trace units into screen dB values (formula (__TRA-8000+800*LG)/(4*LG)), storing them to __B/__C/__A/__D.",
    "category": "",
    "verified": true
  },
  "__GTMNI": {
    "behavior": "Trampoline running DLP source 0x60F74 that lays out the on-screen TOI annotation: sets the display attribute (DA), blanks old text lines, re-enables the graticule (GRAT ON) and positions the pen (PA) to write the TOI result labels.",
    "category": "",
    "verified": true
  },
  "__GTMNJ": {
    "behavior": "Trampoline running DLP source 0x613DE \u2014 the TOI annotation toggle: if ANNOT is on it redraws the TOI display (__TOIDISP), otherwise clears the display (CLRDSP) and redraws the graticule if GRAT is enabled.",
    "category": "",
    "verified": true
  },
  "__GTMNK": {
    "behavior": "Trampoline running DLP source 0x61484 \u2014 the N-dB-down bandwidth run dispatcher: if the personality is enabled (__SOENBL) it runs the measurement __NDBRUN when NDBPNT is set, else re-inits the DLP (DLPINIT/DLPTERM).",
    "category": "",
    "verified": true
  },
  "__GTMNL": {
    "behavior": "Trampoline running DLP source 0x6157A that performs the N-dB-down measurement body: PEAKS on trace __TRB, sets status __T (no-signal/warn), selects lin/log scaling (__NDLNSCL/__NDLGSCL), flags if bandwidth points hit the trace edges, then calls __NDBDISP.",
    "category": "",
    "verified": true
  },
  "__GTMNM": {
    "behavior": "Trampoline running DLP source 0x617C6 that builds the N-dB threshold trace: from the __TRB peak it computes the (NDB-MKPX) threshold offset, builds a rectangular threshold trace and MXMs it against __TRA, finds the two crossing positions (PKPOS via MIRROR) and computes NDBPNTR as the N-dB bandwidth in frequency-span units.",
    "category": "",
    "verified": true
  },
  "__GTMNN": {
    "behavior": "Trampoline running DLP source 0x6198E that lays out the on-screen N-dB-down annotation: sets the display attribute (DA), blanks old text lines, re-enables the graticule and positions the pen (PA) to write the N-dB bandwidth result labels.",
    "category": "",
    "verified": true
  },
  "__GTMNO": {
    "behavior": "Trampoline running DLP source 0x61E08 \u2014 the N-dB annotation toggle (mirror of __GTMNJ): if ANNOT is on it redraws the N-dB display (__NDBDISP), otherwise clears the display and redraws the graticule if GRAT is on.",
    "category": "",
    "verified": true
  },
  "__GTMNQ": {
    "behavior": "Trampoline running DLP source 0x61EB4 \u2014 the NDB parameter entry/clamp: forces NDB negative, clamps it between -1 and -80 dB (MIN/MXM), stores __NDBS, and if a measurement is running re-measures (__NDBMEAS) and enables the appropriate trace views (VIEW TRA/TRB/TRC) per the MSBIT status bits.",
    "category": "",
    "verified": true
  },
  "__GTMNS": {
    "behavior": "Trampoline running DLP source 0x6200E \u2014 the %AM-depth measurement setup: DLPINIT __AMTERM, sets __AMRFLG, ensures MENU 50, runs __SOONRUN, hooks ANNOT to __AMANOT and installs __AMMEAS as the end-of-sweep (ONEOS) handler so it re-measures each sweep.",
    "category": "",
    "verified": true
  },
  "__GTMNV": {
    "behavior": "Trampoline running DLP source 0x621F6 that computes percent AM depth: forms the two sideband-to-carrier ratios __Q/__R (200*__TRA[sideband]/__TRA[carrier]), stores the larger as the result PCTAMR, and scales the three amplitudes by /40.",
    "category": "",
    "verified": true
  },
  "__GTMNX": {
    "behavior": "Trampoline running DLP source 0x623F4 \u2014 the %AM display dispatch: if ANNOT is on it draws the signal-found AM result (__AMSIG) when status __T==0, otherwise the no-signal message (__AMNOSIG).",
    "category": "",
    "verified": true
  },
  "__GTMNY": {
    "behavior": "Trampoline running DLP source 0x62628 that draws the %AM result/error text: blanks old text lines, writes the ' AM:' label, and on error status writes 'Require 3 signals > PEAK EXCURSION above THRESHOLD' (__T==1) or 'Signals do not fit expected % AM pattern' (__T==2), setting PCTAMR=-100 on failure.",
    "category": "",
    "verified": true
  },
  "__GTMNZ": {
    "behavior": "DLP trampoline (pushes token 0x40, calls scheduler $d18) running the macro `DISPOSE ONEOS;ONCMD ANNOT, ;{__AMRFLG=0;};{PCTAMR=-100;};CLRDSP;NV`. It exits/cleans up a measurement mode: disposes the one-shot end-of-sweep hook, disarms the ANNOT annotation command, clears the AM-marker flag __AMRFLG and PCTAMR=-100, then clears the display (CLRDSP).",
    "category": "",
    "verified": true
  },
  "__GTNEXT": {
    "behavior": "DLP trampoline running the ACP (adjacent-channel-power) draw macro: `__ACPANLBL;IF(GRAT);GRAT0;GRAT1;ELSE GRAT0;ENDIF;IF(ACPGR==-1);{ACPMK=-1;};BLANK TRC;__ACPSKA;__ACPC;ELSE __ACPSKB;ACPGRAPH;ENDIF`. It repaints the ACP annotation labels and graticule, and either blanks trace TRC / draws the ACP skirt-A view (__ACPSKA/__ACPC) or the ACP graph view (__ACPSKB/ACPGRAPH) depending on ACPGR.",
    "category": "",
    "verified": true
  },
  "__GTONHK": {
    "behavior": "DLP trampoline running `IF ACPSP,LT,1E3;MOV ACPSP,1E3;ENDIF;IF ACPSP,GT,9999E5;MOV ACPSP,9999E5;ENDIF;__ACPAUTOB;{SS=ACPSP;};UDEFPV ACPSP,ACPSP;NV`. It clamps the ACP channel spacing/span variable ACPSP to [1 kHz, 999.9 MHz], runs ACP auto-setup __ACPAUTOB, copies ACPSP into span SS, and re-declares ACPSP as a user parameter.",
    "category": "",
    "verified": true
  },
  "__GTPRGT": {
    "behavior": "DLP trampoline for the ACP measurement-with-graph execution: runs __ACPCC (compute) and returns on ACPERR, saves attenuation state (__CZAT=MSBIT(8,9)), drops ref level RL by 30 dB, takes a sweep (TS), does trace math on TRA/TRB (MXM/MIN/SUB/ADD, VIEW TRC), restores RL and AT AUTO, then draws the ACP graph (__ACPCG or __ACPAC/__ACPANRES) and clears the delta marker.",
    "category": "",
    "verified": true
  },
  "__GTPRIZ": {
    "behavior": "DLP trampoline running an FFT-mode setup macro: if __FFTA!=0 it saves marker freq MF into __F, clears __FFTA, derives FFTSTOP from sweep-time ST (DIV ST,200,FFTSTOP and back), restores MF and runs __FFSTPA when __N!=0; otherwise it zeroes FFTSTOP when __FFTDIF==0 and FFTSTOP!=0. Manages the FFT stop-frequency / FFT-active state variables.",
    "category": "",
    "verified": true
  },
  "__GTPTRK": {
    "behavior": "DLP trampoline running `MOV __GGTMN,242;HD;__GTMAP;__GTWINTRK;NV`. It selects gate menu number __GGTMN=242, holds the display (HD), then runs the gate-map (__GTMAP) and gate-window-track (__GTWINTRK) routines to enter the gated-sweep window-tracking view.",
    "category": "",
    "verified": true
  },
  "__GTREDG": {
    "behavior": "DLP trampoline for the gated/FFT SIGNAL ID procedure: requires SINGLE or CONTINUS FFT active (else prompts the user), positions the marker (MKTRACE TRB/MKREAD FFT/MKACTV with an AMSG prompt), then adjusts FFTSTOP and increments sweep-time ST in 20us steps, validates the readout (emits 'INVALID SIGNAL ID READOUT' when MF is near FFTSTOP), and toggles the graticule via ONDELAY. Implements gated-FFT signal identification.",
    "category": "",
    "verified": true
  },
  "__GTREMA": {
    "behavior": "DLP trampoline running `IF(__GTCPLR==0);MOV GDRVRBW,1;ELSE;MOV GDRVRBW,0;ENDIF`. It sets the gate-drive RES-BW coupling flag GDRVRBW from the gate RBW-coupling state __GTCPLR (coupled=1, manual=0).",
    "category": "",
    "verified": true
  },
  "__GTREMB": {
    "behavior": "DLP trampoline running `IF(__GTCPLV==0);MOV GDRVVBW,1;ELSE;MOV GDRVVBW,0;ENDIF`. Identical to __GTREMA but for video bandwidth: it sets the gate-drive VID-BW coupling flag GDRVVBW from the gate VBW-coupling state __GTCPLV.",
    "category": "",
    "verified": true
  },
  "__GTREMC": {
    "behavior": "DLP trampoline that sets label Y-positions __A/__B by orientation __Z, then if pulse period __GGTPRI>0 sets the gate-drive sweep-time coupling flag GDRVST from __GTCPLS, else draws the 'PULSE PERIOD (PRI) MUST BE ENTERED... See Pulse Param menu' text prompt at position-dependent PA coordinates. Handles gate sweep-time coupling / missing-PRI warning.",
    "category": "",
    "verified": true
  },
  "__GTREMD": {
    "behavior": "DLP trampoline running `MOV __GGTMN,242;__GTWINTRK`. It selects gate menu __GGTMN=242 and runs the gate-window-track routine __GTWINTRK (a shorter variant of __GTPTRK without the HD/__GTMAP steps).",
    "category": "",
    "verified": true
  },
  "__GTREME": {
    "behavior": "DLP trampoline running `IF(GSTAT(1));KB;ELSE;KH /GATE|DELAY/,ACTVF __GTDMKR,0;ENDIF;NV`. If gate status GSTAT(1) is set it beeps/backs out (KB); otherwise it highlights the 'GATE DELAY' softkey (KH) and activates the gate-delay marker function __GTDMKR.",
    "category": "",
    "verified": true
  },
  "__GTREMF": {
    "behavior": "DLP trampoline running `IF(GSTAT(1));KB;ELSE;KH /GATE|LENGTH/,ACTVF __GTLMKR,0;ENDIF`. Same pattern as __GTREME but for gate length: highlights the 'GATE LENGTH' softkey and activates the gate-length marker function __GTLMKR.",
    "category": "",
    "verified": true
  },
  "__GTREMG": {
    "behavior": "DLP trampoline running `KH /GATE|ON  OFF/,0,GSTAT 0;NV`. It highlights the 'GATE ON OFF' softkey and sets the time-gate status GSTAT to 0 (gate off).",
    "category": "",
    "verified": true
  },
  "__GTREMH": {
    "behavior": "DLP trampoline running `IF (__GTTMF==1);DISPOSE ONEOS;MKOFF;{__GTTMF=0;};IF (__Z==1);ELSE;CLRBOX 1,211,300,221,0;ENDIF;ENDIF`. If the gate time-marker flag __GTTMF is set, it disposes the one-shot end-of-sweep hook, turns markers off (MKOFF), clears the flag, and erases the 'GATE TRIG TO MKR' text box.",
    "category": "",
    "verified": true
  },
  "__GTREMI": {
    "behavior": "DLP trampoline running a block of VRD variable-default reads that initialize the gate personality's local variables __A..__Z (e.g. __E=DA, __F=DA, __L=6, __O=2, __P=398, __Q=ST, __S=ST, __V=-1, __W=GD, __X=-1, __Y=-1, __Z=0). It is the gate-measurement variable-initialization/reset routine.",
    "category": "",
    "verified": true
  },
  "__GTREMJ": {
    "behavior": "DLP trampoline that toggles the gate time-marker flag __GTTMF: when turning it on it runs window track/swap (__GTWINTRK/__GTWINSWAP), draws 'GATE TRIG TO MKR:' with the DA value, sets and updates a marker on trace TRA (__GTUPMK); when turning off it disposes the hook, clears the box, re-tracks the window and turns markers off. Implements the GATE TRIG TO MKR toggle/display.",
    "category": "",
    "verified": true
  },
  "__GTREMK": {
    "behavior": "DLP trampoline running the gate reset-all macro: clears a display box (CLRBOX by __Z), zeroes the gate globals __GGTRE/__GGTPWID/__GGTPRI/__GTTTM and all gate-drive coupling variables (GDRVREFE/PWID/PRI/ST/RBW/VBW), then runs __GTSHPP. It clears/resets the entire gated-sweep measurement state.",
    "category": "",
    "verified": true
  },
  "__GTREML": {
    "behavior": "DLP trampoline (token 0x2b at src 0x6a914) that reconfigures the gated/pulse-measurement drive: sets __GGTRE=__GTTTM (ABS), clears the on-screen box (CLRBOX 1,211,300,221), HD/MKOFF ALL/DISPOSE ONEOS, rehooks ONCMD ST/GD to __GTUNCS/__GTPRGT, sets menu 246, runs __GTSHPP, and loads the gate-drive registers GDRVST/GDRVRBW/GDRVREFE/GDRVPWID/GDRVPRI from the __GGT* pulse params.",
    "category": "",
    "verified": true
  },
  "__GTREMM": {
    "behavior": "DLP trampoline (src 0x6ac7e) implementing the interactive 'measure the PRI' helper: menu 248, __GTPTRK peak-track, prints TEXT prompts ('Position the delta marker\u2026the pulse or pattern period (PRI)\u2026Press DONE'), then computes PRI from the delta marker via __T=(__GTTTM-__GTSWDE)/(ST/401) with MKN/MKP/MKTRACE TRA/MKD and stores __GGTPRI=MKF on end-of-sweep.",
    "category": "",
    "verified": true
  },
  "__GTRUNX": {
    "behavior": "DLP trampoline (src 0x67f64) that draws the ACP channel-boundary tick lines: sets DA=0 and does a series of pen PU/PD (PA) strokes at the channel-zone edge coordinates __CZJLA/__CZJLB (lower), __CZJCA/__CZJCB (center), __CZJUA/__CZJUB (upper) to render the vertical channel limit markers on the graticule.",
    "category": "",
    "verified": true
  },
  "__GTSHPP": {
    "behavior": "DLP trampoline (src 0x659e4) whose entire source is 'FFTSNGLS;' \u2014 it issues the FFT single-sweep command.",
    "category": "",
    "verified": true
  },
  "__GTSPGD": {
    "behavior": "DLP trampoline (src 0x673c6) that sets up the ACP/CHP/OBW power-graph display: returns if __CZSU==0, sets ACPMK=-1, A1/B4/BLANK TRC, then per mode __M installs the graph/graticule and the appropriate end-of-sweep handler (__M1\u2192__ACPC, __M3\u2192GRAT/label, __M4\u2192CHP __CHPC, __M5\u2192OBW __OBWC).",
    "category": "",
    "verified": true
  },
  "__GTSWADJ": {
    "behavior": "DLP trampoline (src 0x68542) that adjusts the ACP sweep marker/on-marker-update handler: if ACPMK>0 it activates marker 1 on TRC and (mode<4) installs an ONMKRU that computes ACPMKA=MKA+ACPPWRTX and displays it (DSPLYA) with a save/restore of TRC[201] and delta MKD=ACPSP; else disposes ONMKRU and redraws the graticule.",
    "category": "",
    "verified": true
  },
  "__GTSWDEF": {
    "behavior": "DLP trampoline (src 0x68688) defining the CHP (channel-power) measurement: __ACPSUD, mode __M4, __ACPSUM setup, __ACPICALC integrate, __CHPANTIC/__CHPANLBL/__CHPERR annotation+error, and defines softkey 1288 (KEYCMD1288) as a PWRGRAPH ON/OFF toggle on CHPGR.",
    "category": "",
    "verified": true
  },
  "__GTSWDESTF": {
    "behavior": "DLP trampoline (src 0x6799c) that recomputes ACP integration when parameters change: if ACPBW/ACPSP/SP/RB/ACPPAR differ from cached __F/__G/__H/__C/__B it re-runs __ACPICALC/annotation/graticule/labels/error and re-caches them, then clamps the integration indices __J,__K to [1..401] (from __CZILA/__CZIUB) and runs __ACPBWC/__ACPPI, storing ACPPWRTX=__R.",
    "category": "",
    "verified": true
  },
  "__GTTDRW": {
    "behavior": "DLP trampoline (token 0x2b, src 0x6595a) whose source is 'IF (1==__FFTSTF);DIV FFTSTOP,MF,0.50;ENDIF;' \u2014 when the FFT-stop flag is set it sets FFTSTOP = MF/0.50 (marker-freq scaled).",
    "category": "",
    "verified": true
  },
  "__GTTRKNXT": {
    "behavior": "DLP trampoline (src 0x699e4) whose source is 'MKOFF ALL;DISPOSE ONEOS;__GTGSCL;' \u2014 turns off all markers, disposes the end-of-sweep handler, and runs __GTGSCL (gated-scale) to advance to the next track step.",
    "category": "",
    "verified": true
  },
  "__GTTTMKR": {
    "behavior": "DLP trampoline (src 0x64b06) that sets the FFT stop bin from sweep time: if __FFTSTF==1 it computes FFTSTOP=400/(ST*2) (bracketed by __FFTA=0/1), optionally __FFSTPA when __N!=0, and queries FFTSTOP; otherwise HD and errors with AMSG 'You must activate SINGLE FFT\u2026'.",
    "category": "",
    "verified": true
  },
  "__GTUNCB": {
    "behavior": "DLP trampoline (src 0x676ba) that recomputes/annotates ACP: '__ACPCC;IF(ACPERR)RETURN;IF(ACPGR>0)__ACPCG ELSE __ACPAC;{DA=__E;};__ACPANRES;' \u2014 runs the ACP channel calc, returns on error, then either the graph (__ACPCG) or numeric-annotate (__ACPAC) path and restores annotation.",
    "category": "",
    "verified": true
  },
  "__GTUNCS": {
    "behavior": "DLP trampoline (src 0x6751e) that performs ACP setup in mode 1: __ACPSUD, __M1, __ACPSUM, __ACPICALC, __ACPANTIC/__ACPANLBL/__ACPERR annotation+error, and defines softkey 1288 as an ACPGRAPH ON/OFF toggle on ACPGR.",
    "category": "",
    "verified": true
  },
  "__GTUNCV": {
    "behavior": "DLP trampoline (src 0x67750) identical to __GTUNCS but with mode __M3: runs ACP setup (__ACPSUD/__ACPSUM/__ACPICALC/annotation/error) and the same ACPGRAPH softkey-1288 toggle, i.e. the mode-3 ACP measurement setup.",
    "category": "",
    "verified": true
  },
  "__GTUPCP": {
    "behavior": "DLP trampoline (src 0x65f1a) that initializes the ACP measurement DLP once (if __CZSU==0): DLPINIT with done-handler __ACPDONE, __ACPSUK setup, hooks ONCMD ANNOT\u2192__ACPANNOT, PA, and marks setup-done via __CZSU1.",
    "category": "",
    "verified": true
  },
  "__GTUPMK": {
    "behavior": "DLP trampoline (src 0x64dee) whose source is 'IF (1==__FFTDIF);DLPINIT;DLPTERM;ENDIF;' \u2014 when the FFT-diff flag is set it re-initializes and terminates the DLP (DLPINIT/DLPTERM) to update the marker/FFT state.",
    "category": "",
    "verified": true
  },
  "__GTVCST": {
    "behavior": "DLP trampoline (token 0xd1, src 0x6bb44) that sets sweep time from the pulse PRI: computes __T=401*__GGTPRI, and if >100 draws the warning TEXT 'PRI TOO LONG. TRACE DROPOUTS MAY OCCUR', then sets sweep time ST=__T, rehooks ONCMD ST\u2192__GTUNCS, and caches __GTTEMS=ST.",
    "category": "",
    "verified": true
  },
  "__GTVDFS": {
    "behavior": "DLP trampoline (token 0x6e, src 0x66596) that restores the ACP video/default measurement state: T1, TRPRST trace-preset, DET0 detector, ST AUTO, forces log scale (LG10, or LG13 in mode __M3), sets step size SS=ACPSP, and runs __ACPAUTOB auto-bandwidth.",
    "category": "",
    "verified": true
  },
  "__GTWID": {
    "behavior": "DLP macro: if graphics vars __V and __N are both non-zero it positions the pen (PA=__U;PA=__V), draws TEXT(' ') and sets DA=__B, then computes __J=PEAKS(__TRA,TRB,...) \u2014 i.e. it peak-scans the trace arrays and lays down a graticule/window text marker.",
    "category": "trampoline",
    "verified": true
  },
  "__GTWINSET": {
    "behavior": "DLP macro that resets measurement-window state: TRPRST (trace preset), DISPOSE ONMKRU (drop the marker-update handler), then IF(ACPPAR) runs __ACPRCLP;LIMITEST0 else DISPOSE ONEOS \u2014 configures the ACP/window measurement and tears down its end-of-sweep/marker service routines.",
    "category": "trampoline",
    "verified": true
  },
  "__GTWINSWAP": {
    "behavior": "Short DLP macro 'MOV __GGTMN,245;HD;__GTWINTRK;NV': sets the graphics-menu variable __GGTMN to 245, holds, invokes __GTWINTRK to re-track the active window, and updates the display.",
    "category": "trampoline",
    "verified": true
  },
  "__GTWINTRK": {
    "behavior": "DLP macro 'KH /T WINDOW|SWP TIME/,ACTVF __GTGSCL,0;NV': installs the 'T WINDOW / SWP TIME' softkey label and makes __GTGSCL the active function (with initial value 0), then updates the display.",
    "category": "trampoline",
    "verified": true
  },
  "__GTZOOM": {
    "behavior": "DLP macro that clamps the occupied-bandwidth-percent variable OBWPCT into the range [1, 99.99] (MOV OBWPCT,1 / MOV OBWPCT,99.99) before an update \u2014 the zoom/percent guard for the OBW window measurement.",
    "category": "trampoline",
    "verified": true
  },
  "__MN": {
    "behavior": "Menu-select handler: tests bit 12 of RAM flag $b1e0 and, if the current menu id at $b1fe equals 0x12 (its arg_type), calls the low-memory menu/A7-API dispatch routine at $4d8.w; otherwise returns without action.",
    "category": "native",
    "verified": true
  },
  "__NDBACT": {
    "behavior": "Queries whether the N-dB measurement is active/ready: calls status sub $5eca2, tests cal-valid bit0 of $94e1 and bit13 of $befa, reads sweep-status MMIO $FFF300 and tests bit 11 (sweep-complete), and returns a 0/1/2 state code in the A6 frame.",
    "category": "native",
    "verified": true
  },
  "__NDBANOT": {
    "behavior": "Draws the N-dB measurement annotations: in a loop it computes screen Y=0xC8-idx*0xA, sets the pen via jsr $154, draws marker/line primitives (jsr $5dc2/$75a), loads a per-entry longword from the table at (A4*4 - $5FBA) and formats/prints the value with jsr $62b2/$6016.",
    "category": "native",
    "verified": true
  },
  "__NDBDISP": {
    "behavior": "Accessor that loads and returns the 32-bit RAM variable at $a99a (an NDB display-state/pointer word); the resolved handler is a clean link/move.l $a99a/unlk/rts getter, not a screen-drawing action.",
    "category": "native",
    "verified": true
  },
  "__NDBMEAS": {
    "behavior": "Parses the command's keyword argument string (byte via pointer in D0/A4) through the token-lookup helper jsr $7978.w using table id 0x18, then switches on the result via the 16-entry jump table at 0x39b10 (mis-disassembled as ori.b #$XX,D3) to select an NDB measurement submode.",
    "category": "native",
    "verified": true
  },
  "__NDBNOSIG": {
    "behavior": "Configures the NDB 'no-signal' parameter block: divides the incoming value by 10 up to 3 times to derive a scale exponent, packs that exponent into the low 2 bits of $b0f0.w, and stores two parsed values (via jsr $7978.w) into $b0f2.w and $b0f4.w.",
    "category": "native",
    "verified": true
  },
  "__NDBRUN": {
    "behavior": "Starts/re-inits the NDB measurement run: masks interrupts (ori #$700,SR) and initializes measurement buffers in cal-scratch RAM at 0x2FC136 (offsets +$CA and +$7E2, up to 0x2FFFFF) via helper $399FA, then reads the pointer at $a7d6 and status word $bb52.",
    "category": "native",
    "verified": true
  },
  "__NDBSIG": {
    "behavior": "Constant-return stub: the resolved handler at 0x3f8a8 is 'moveq #1,D0; rts', i.e. it always returns 1 (signal-present = true).",
    "category": "native",
    "verified": true
  },
  "__NDBTERM": {
    "behavior": "Renders the NDB measurement result/terminal text: branches on state word $9fb4 (>=2 vs <2) to pick a ROM string (0x4a3b6 / 0x4a3ca), then positions the pen (jsr $154) and prints the fixed ROM strings at 0x4a3da/0x4a3f0 (jsr $100).",
    "category": "native",
    "verified": true
  },
  "__NDBUPDATE": {
    "behavior": "Updates an NDB display/measurement parameter by forwarding to helper $f092 with a mode tuple chosen from the arg at ($a,A6): value<2 -> f092(0,arg,val), ==2 -> f092(2,0,val), ==3 -> f092(2,1,val).",
    "category": "native",
    "verified": true
  },
  "__NDLGSCL": {
    "behavior": "Sets the NDB measurement to LOG scale: inspects the argument string bytes (first char / a space 0x20 at offset 1) and calls scale-setter $3adac with the ROM parameter table 0x3d108 (or falls through to $3afc8) \u2014 the log-scale counterpart of __NDLNSCL.",
    "category": "native",
    "verified": true
  },
  "__NDLNSCL": {
    "behavior": "Sets the NDB measurement to LINEAR scale by calling scale-setter $3adac with argument 0; its companion sub $3afc8 parses a count and copies bytes into the scale buffers at $a360/$a370.",
    "category": "native",
    "verified": true
  },
  "__OBWANERR": {
    "behavior": "Resets the occupied-bandwidth annotation/separator buffer byte $acca to 0 and calls redraw routine $26282; sibling routines build $acca by mapping arg chars B/C/S to separators 0x20/0x2c/0x3b (space/comma/semicolon).",
    "category": "native",
    "verified": true
  },
  "__OBWANLBL": {
    "behavior": "Native handler at 0x4F2DE: takes the OBW active-trace selector from D0 (or from RAM $9EA6.w when D0==0xFFFF), validates it is in 0..3, then indexes a per-trace word table (base displacement -0x616A, i.e. high-RAM near 0xFF9E96) by idx*4, reads the entry, and returns its absolute value in D0. Reads an OBW parameter value for the selected trace; no MMIO/DAC access.",
    "category": "",
    "verified": true
  },
  "__OBWANRES": {
    "behavior": "Native handler at 0x24B6A is a two-instruction body that clears RAM word $9FD0.w (an OBW state/result cell) and returns. A reset of one OBW status word only.",
    "category": "",
    "verified": true
  },
  "__OBWC": {
    "behavior": "Native handler at 0x4FFD6: parses its argument (jsr $6B8 arg-fetch, $6E8/$6090 numeric parse) to a trace index into $9EA6.w (or uses the stored active trace), validates 0..3, checks per-trace flag bit7 at table -0x61AE, then matches the argument keyword bytes 'N' then 'L'/'R' (0x4E,0x4C/0x52). Parses an OBW 'NL'/'NR' keyword argument gated on the trace-enabled flag.",
    "category": "",
    "verified": true
  },
  "__OBWM": {
    "behavior": "Native handler at 0x4F8B4: resolves the OBW trace selector ($9EA6.w or parsed arg), and unless the arg is a '?' query, parses a numeric value pair via helper $4E8AE (returns two longwords in D2/D3). Sets/reads an OBW measurement value pair for the selected trace.",
    "category": "",
    "verified": true
  },
  "__OBWPCT": {
    "behavior": "Native handler at 0x223B6 computes an OBW percent/count value: helper $223A2 reads config bit4 of RAM $B213.w to select constant 0x28(40) vs 3, then computes 2000-x, shifts left 8 and masks 0xFFF into D0; an adjacent nested leaf writes nibble-packed data to the A7 analog I/O bus (0xFFF728 select / 0xFFF72A data). The exact percent semantics and the f728 helper's caller are inferred, not resolved.",
    "category": "",
    "verified": false
  },
  "__OBWS": {
    "behavior": "Native handler at 0x4ED26: unless the arg is a '?' query, parses a number ($6E8/$6090) validated to the range -1..3 and stores it as the OBW active-trace selector in $9EA6.w, then returns $9EA6.w. This is the OBW source/trace select register used across the OBW family.",
    "category": "",
    "verified": true
  },
  "__PKAM": {
    "behavior": "Trampoline running DLP source that registers ONCMD hooks for the START (ST) and SPAN (SP) commands: if the marker-value equals the reference it preserves start into __T when span==0 else zeros span, and sets start (ST) from __GTGSCL. A marker/span (auto-center / zero-span) soft-key macro.",
    "category": "",
    "verified": true
  },
  "__PKAMT": {
    "behavior": "Trampoline whose DLP source, when FFT mode is active (FFTSTAT), copies FFTSTOP into marker-freq MF, otherwise sets MF=FB and FB=MKF. A marker-to-frequency handoff that branches on FFT vs normal sweep state.",
    "category": "",
    "verified": true
  },
  "__PKBMD": {
    "behavior": "Trampoline: if menu-state __MN[18] equals the marker-active flag it turns markers off (MKOFF), else it invokes __PKAM and __PKMAF; ends with NV (no re-verify). A marker band/mode toggle soft-key.",
    "category": "",
    "verified": true
  },
  "__PKBPD": {
    "behavior": "Trampoline that reads menu-state bits/fields (MSBIT(55,13), MSFIELD(26,..)/(27,..)) and, when span==0, adjusts __X and computes __Y from a state field offset by 4000 and scaled by FMGAIN. A band-power / marker field-computation macro.",
    "category": "",
    "verified": true
  },
  "__PKCR": {
    "behavior": "Trampoline: when marker-value equals reference it runs __GTMAP and HD, sets __V=__Y, runs window-track __GTWINTRK, and hooks the GL (graph limit-line) command to load __GGTTMPL. A cursor/graph-window redraw and GL command-hook macro.",
    "category": "",
    "verified": true
  },
  "__PKCRE": {
    "behavior": "Trampoline that zeros graph control __GGTRCTL and, if user-routine GDRVUTIL is undefined, defines it via ACTDEF bound to &__GTREMA and stores it. Sets up a graphics drive-utility user function.",
    "category": "",
    "verified": true
  },
  "__PKCUG": {
    "behavior": "Trampoline: stores the peak result PKRES[__N]=__X, converts point index __X to a frequency __F=(FA+SP*(__X-1)/400)/1E6 MHz (start-freq FA plus span SP over 400 trace points), displays it with DSPLY and updates the cursor amplitude via CTA. A peak/cursor readout-to-frequency macro.",
    "category": "",
    "verified": true
  },
  "__PKDF": {
    "behavior": "Trampoline that disposes the on-marker-update handler (DISPOSE ONMKRU), runs __PKIF1, zeros the marker table (MOV MKTBL,0), runs __PKIF0 and __PKDTW. A marker-table clear / delete-function soft-key.",
    "category": "",
    "verified": true
  },
  "__PKDFA": {
    "behavior": "Trampoline: if a frequency counter is present (HAVE(CNT)) and the marker frequency-counter is active (ACTVF(MKFCR)) and the menu bit MSBIT(32,9) is set, it turns the counter off (MKFCR0, HD), otherwise re-asserts MKFCR. A marker frequency-counter enable/disable soft-key.",
    "category": "",
    "verified": true
  },
  "__PKDMD": {
    "behavior": "Trampoline that toggles marker pause: if MKPAUSE is active it clears it (MKPAUSE0) else sets it (MKPAUSE1), ending with NV. A marker-pause toggle soft-key.",
    "category": "",
    "verified": true
  },
  "__PKDMT": {
    "behavior": "Trampoline that loads menu field __MN[22]=MSFIELD(37,15,2) and, when it is zero, emits a soft-key label 'MK TRACE|...' via KH. A marker-trace menu/soft-key-label setup macro.",
    "category": "",
    "verified": true
  },
  "__PKDPD": {
    "behavior": "Trampoline running a FOR loop over menu fields (__MN[19]=37..48) reading MSFIELD(__MN[19],4,5) to locate the active soft-key/menu slot. A menu-scan / soft-key dispatch macro.",
    "category": "",
    "verified": true
  },
  "__PKDPT": {
    "behavior": "DLP trampoline (pushes source id, jsr $d18 scheduler): if not already in the __PKIF guarded state, it enters it (__PKIF1) and clamps the peak/delta-line mode variable PKDLMODE to the range [-1,1] via MXM/MIN, then leaves the guard (__PKIF0). Toggles/limits a peak delta-line display mode.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKDT": {
    "behavior": "DLP trampoline: if not in the __PKIF guard, when there is no peak table (PKTBL) it does a bare DLPINIT;DLPTERM bracket, else it DLPINITs with __PKPTO, turns all markers off (MKOFF ALL) and moves __TRB.. \u2014 i.e. it (re)builds or tears down the peak-table display context.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKDTW": {
    "behavior": "DLP trampoline that writes the display text label \" Inactive \" (padded) via TEXT, terminated by NV. Paints the 'Inactive' annotation for a peak/gate field.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKENA": {
    "behavior": "DLP trampoline: sets DA=0, clears a box region (CLRBOX -40,-1,400,60) then draws a rectangle outline with the vector pen (PU/PA/PD to the corner coordinates). Draws the enable/annotation border box on the display.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKEND": {
    "behavior": "DLP trampoline: displays numeric value __A with format 8.2 (offset by marker-table flag MKTBL) and appends units text \u2014 'dBDL' when in marker delta-line mode (MKTBL&&MKDLMODE), else a __C-conditional unit. Formats a marker amplitude readout with units.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKENF": {
    "behavior": "Unresolved (handler_pc=0, not reached via the dispatch tables). By name and by its only caller __PKENT ('MOV __F,__A;__PKENF'), it is most likely the peak/entry frequency-readout companion to the amplitude routine __PKEND, formatting/displaying the __F frequency register \u2014 but this is inferred, not confirmed from disassembly.",
    "category": "unresolved",
    "verified": false
  },
  "__PKENT": {
    "behavior": "DLP trampoline: copies the accumulator __A into the frequency register __F (MOV __F,__A) then invokes __PKENF to render it (NV terminator). Loads and displays the peak-marker/entry frequency value.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKFMB": {
    "behavior": "DLP trampoline: computes menu-state var __MN[21] = MSFIELD(37,11,4)%5 (forcing 1 when span=0 and result 0), then guarded on __MN[21] defines a softkey via KH,/M... \u2014 a peak/marker menu-field builder driven by a measurement status field.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKFNA": {
    "behavior": "DLP trampoline: EM (erase/empty), disables annotation (ANNOT0), DISPOSE 8049 (frees a display object), re-inits the window (WININIT), re-enables annotation (ANNOT1) and registers ONCMD hooks for ANNOT and WINZOOM. Reinitializes the display window plus its annotation/zoom command handlers.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKIP": {
    "behavior": "DLP trampoline (the factory power-up personality routine whose corruption caused the Opt-027 reboot loop): if __GGTMN==0 it registers an active-function via ACTDEF __GGTMN with a 500 STEP and an action block that sets MN=__GGTMN. Installs the gate-menu active function.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKITG": {
    "behavior": "DLP trampoline: conditional units/label helper \u2014 when __C is 19 or 22 and __N==2 it emits a TEXT label (source truncated). Selects a marker readout unit-text string based on the __C/__N mode variables.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKITW": {
    "behavior": "DLP trampoline: tests measurement-status bits MSBIT(9,x) and prints the active-trace letter \u2014 TEXT('A') if bit7/bit0 set, else TEXT('B') if bit8/bit3, etc. Renders which trace (A/B/...) is active from the status word.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMA": {
    "behavior": "DLP trampoline: clears the __Z flag, turns the graticule off (GRAT OFF), sets trace-mode __T to -1 or -2 depending on the gate time-mode flag __GTTMF, then continues into __GT... \u2014 sets up a gated-measurement display mode (graticule off, trace-select).",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMAF": {
    "behavior": "DLP trampoline: MOV GDRVRBW,0 (clears the gate-drive resolution-bandwidth coupling variable) terminated by NV. Zeroes the RBW gate-drive coupling state.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMAO": {
    "behavior": "DLP trampoline: an empty DLPINIT;DLPTERM bracket (NV) \u2014 an init/terminate pair with no body, effectively flushing/resetting the DLP display context.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMBD": {
    "behavior": "DLP trampoline: guarded on menu var __MN[18]==1 it defines a softkey via KH,/SELECT|... (source truncated). Builds a 'SELECT' softkey for a marker-band/menu field.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMD": {
    "behavior": "DLP trampoline: registers a set of ONCMD command-intercept handlers \u2014 ST\u2192__GTUNCS, and GD/GL/GATE\u2192__GTPRGT (plus more, truncated). Installs the gate/sweep command handlers for the gated-measurement personality.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMFA": {
    "behavior": "DLP trampoline: when gate-control __GGTRCTL==1, sets the coupling variable __GTCPLV to 0 or 1 depending on GDRVVBW (video-BW gate drive) then calls __GTCVBW. Manages VBW coupling in gated-measurement mode.",
    "category": "dlp-trampoline",
    "verified": true
  },
  "__PKMFB": {
    "behavior": "16-byte DLP trampoline (jsr $d18 scheduler) that runs the DLP source `__GTWINSWAP;NV` \u2014 invokes the gate-utility window-swap subroutine, toggling which gate-utility display window is active, then returns.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMKMN": {
    "behavior": "DLP trampoline running the gate-utility init/banner script: `IF(__GTVRDS==0)` then pen-up (PU), position-absolute (PA 8,170/8,160) and draw the `INITIALIZING GATE UTILITY.` text on the graticule the first time the utility is drawn.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMM": {
    "behavior": "DLP trampoline `__PKAM;M3;IF(__MN[18]==2);__MN[18]1;IF(MN==9);MN9;ENDIF;ENDIF;NV` \u2014 calls sub __PKAM, sets marker mode M3, and normalizes gate-utility menu state (resets menu var __MN[18] from 2 to 1 and re-selects menu 9 when active).",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMMT": {
    "behavior": "DLP trampoline `IF(HAVE(CNT));IF(MSBIT(8,13));MC0;ELSE;MC1;ENDIF;ELSE;WRNO;ENDIF;` \u2014 if the frequency-counter option is present, toggles marker-count (MC0/MC1) based on status bit (8,13); otherwise issues WRNO (feature-not-available warning).",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMN": {
    "behavior": "DLP trampoline building the gate-utility window layout: `WINOPEN __X,0,120,400,90` opens window __X at x=0,y=120 sized 400x90, `WINITM -1` clears items, and successive `WINANNOT` calls define the annotation fields.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMRE": {
    "behavior": "DLP trampoline `IF(__GTLMKR==0);ACTDEF __GTLMKR,/GATE LENGTH/,__GGTTMPL,SEC,&__GTLMKRSTF;&;ENDIF` \u2014 lazily defines the GATE LENGTH active function (units SEC, step routine __GTLMKRSTF) the first time it is needed.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMTA": {
    "behavior": "DLP trampoline for gate-utility layout/coupling: `IF(__GGTPRI>0){__GTCPLS=1;}` sets the coupling flag, then branches on window vars __V/__X and flag __Z to set on-screen coordinate temporaries __A/__B (e.g. 180,170).",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMTE": {
    "behavior": "DLP trampoline `__GTMAP;__GTDMKR;` \u2014 sequentially calls the gate-map subroutine and the gate delay-marker subroutine to redraw the gate map and delay marker.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKMTO": {
    "behavior": "DLP trampoline `__PKAM;__PKMAF;` \u2014 chains two other gate-utility package subroutines (__PKAM then __PKMAF); the exact effect lives in those subs, but this record just sequences them.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKNAU": {
    "behavior": "Unresolved DLP-internal routine (handler_pc=0, dispatch tables did not resolve it); by naming it is another `__PK\u2026` subroutine of the factory GATE UTILITY DLP, but no disassembly or source is available to confirm its action.",
    "category": "dlp-gate-utility",
    "verified": false
  },
  "__PKNMR": {
    "behavior": "DLP trampoline `IF __GTDMKR,LT,1E-6;MOV __GTDMKR,1E-6;...IF __GTDMKR,GT,65E-3;MOV __GTDMKR,65E-3` \u2014 clamps the gate delay-marker value __GTDMKR to the valid range 1 us .. 65 ms.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKNMT": {
    "behavior": "DLP trampoline `IF(__GTSWDE==0);ACTDEF __GTSWDE,/SWEEP DELAY/,GD,SEC,&__GTSWDESTF;&;ENDIF;NV` \u2014 lazily defines the SWEEP DELAY active function (units SEC, step routine __GTSWDESTF) the first time it is needed.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKNSP": {
    "behavior": "Unresolved DLP-internal routine (handler_pc=0, not resolved via dispatch tables); by naming pattern a `__PK\u2026` subroutine of the GATE UTILITY DLP, but no handler or source is available to confirm behavior.",
    "category": "dlp-gate-utility",
    "verified": false
  },
  "__PKOCA": {
    "behavior": "Unresolved DLP-internal routine (handler_pc=0, dispatch tables did not resolve it); a `__PK\u2026`-family gate-utility subroutine by name only, with no disassembly or DLP source to verify.",
    "category": "dlp-gate-utility",
    "verified": false
  },
  "__PKPDL": {
    "behavior": "DLP trampoline `M2;MKTYPE PSN;E1;M3;MKMIN;NV` \u2014 configures markers for the gate measurement: select marker 2, set marker type to position (PSN), E1, select marker 3, then marker-to-minimum (MKMIN).",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKPK": {
    "behavior": "DLP trampoline `IF (__V==__Y);__GTWINSWAP;ENDIF;NV` \u2014 conditionally swaps the gate-utility window (calls __GTWINSWAP) when window variable __V equals __Y.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKPME": {
    "behavior": "DLP trampoline `IF(__Z==1);CLRBOX 1,1,399,199,0;ENDIF;IF (__V==__X);WINACTIVE __X;{__GTSPF=0;}` \u2014 optionally clears the full display box, then activates window __X and clears the sweep-pending flag __GTSPF.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKPS": {
    "behavior": "DLP trampoline `MOV __GTDUM,ZMK OFF,__X;{__GTGDIS=1;};NV` \u2014 moves the ZMK-OFF value of window __X into the dummy var __GTDUM and sets the gate-display-enable flag __GTGDIS=1.",
    "category": "dlp-gate-utility",
    "verified": true
  },
  "__PKPTA": {
    "behavior": "DLP trampoline running a peak/plot-menu macro that clamps menu variable __MN[18] to the range 1..4, calls __PKFMB, and (when an FFT measurement is active and __MN[19] is clear) moves a marker value.",
    "category": "",
    "verified": true
  },
  "__PKPTO": {
    "behavior": "DLP trampoline whose short macro '__PKAM;MN12;E1;' invokes __PKAM, selects softkey menu 12, then executes function key E1.",
    "category": "",
    "verified": true
  },
  "__PKSM": {
    "behavior": "DLP trampoline: if the sweep-time active-function variable __GTGSCL is 0 it ACTDEFs it as active function 'SWEEP TIME' with a 10E-3 SEC default (and STF step handler), i.e. sets up the sweep-time menu entry.",
    "category": "",
    "verified": true
  },
  "__PKSME": {
    "behavior": "DLP trampoline graph/window macro: when __V==__X it maps the graph (__GTMAP), holds, sets __V=__Y, runs window tracking (__GTWINTRK) and installs ONCMD GL handlers tying GL to __GGTTMPL.",
    "category": "",
    "verified": true
  },
  "__PKTC": {
    "behavior": "DLP trampoline: if graph control __GGTRCTL==1, toggles the gate drive by calling __GTGTOFF when GDRVGT==0 else __GTGTON \u2014 gate on/off control for the gated-sweep graph.",
    "category": "",
    "verified": true
  },
  "__PKTL": {
    "behavior": "DLP trampoline: if graph control __GGTRCTL==1, copies GDRVPRI into __GGTPRI, takes its absolute value (ABS), and UDEFPVs GDRVPRI \u2014 updating the graph's lower/priority drive parameter.",
    "category": "",
    "verified": true
  },
  "__PKTM": {
    "behavior": "DLP trampoline: if graph control __GGTRCTL==1, conditionally swaps the window (__GTWINSWAP when __V==__Y), then moves GDRVSWP (sweep drive) into __GTGSCL and UDEFPVs it \u2014 updating the graph sweep-time drive.",
    "category": "",
    "verified": true
  },
  "__PKTP": {
    "behavior": "DLP trampoline: if graph control __GGTRCTL==1, copies GDRVREFE (ref-level drive) into __GGTRE, takes ABS, and UDEFPVs GDRVREFE \u2014 updating the graph top/reference drive parameter.",
    "category": "",
    "verified": true
  },
  "__PZCHKIT": {
    "behavior": "Native predicate at 0x209D4: btst #15,$FFADC4 and if set returns -1 in D0, otherwise returns the word at $FFB0C8 \u2014 a status/check accessor reading the $ADC4 flag word.",
    "category": "",
    "verified": true
  },
  "__PZEXE": {
    "behavior": "Native handler at 0x31724: calls low-mem service $136 with D0=-3, and if the returned word != 0xFFFF it sets ($FFBAF8=0xFF) or clears ($FFBAF8=0) the byte flag $FFBAF8 based on bit0 of its input argument.",
    "category": "",
    "verified": true
  },
  "__PZGETFRA": {
    "behavior": "Native handler at 0x20AE6: loads table/string pointer 0x20DC6 into D0 and tail-calls the shared '?'-query parameter processor at 0x209E6 (which checks for the '?' query char, tests $FFB0A0 bit11 and emits from either 0x20DC2 or 0x20DC6).",
    "category": "",
    "verified": true
  },
  "__PZKEYEXE": {
    "behavior": "Native handler at 0x2F4C0: a 0..3 subcommand dispatcher on D0 that sets/clears bit15 of the key-state register $FFB116 and bit4 of $FFBFF7, gated on hardware key/status reads via helpers $2F498/$5414/$54B8 \u2014 front-panel key execution state machine.",
    "category": "",
    "verified": true
  },
  "__PZKEYMNU": {
    "behavior": "Native handler at 0x2FB18: programs the key-menu state \u2014 $FFB118=1, $FFB11A=0x1CCD/0x1CCC (per $FFBFF6 bit10), $FFB11C=7, key register $FFB116=0x603E/0xE03E, writes command 0xEC to keyboard/PIT port $FFF801, and sets timing/count cells $FFB102=1000, $FFB0FE=1, $FFB0FC=1.",
    "category": "",
    "verified": true
  },
  "__PZREMCMDS": {
    "behavior": "Native handler at 0x544F4: copies the remote/interface state byte $FFB05D into $FFB05C and switch-dispatches on it to remote-command handlers ($544B0/$543D8), managing $FFBBBC and clearing bit5 of $FFBEFD \u2014 processes queued remote (HP-IB) commands.",
    "category": "",
    "verified": true
  },
  "__PZZOOM": {
    "behavior": "Native handler at 0x54FA2: reads a pair of longword args at (A6+8), normalizes them via number-helper $609C, then uses bit-field extractor $69B4 (fields 0xE/0xF/0xC...) to assemble packed parameter values written back through $54EB2 \u2014 builds/programs a zoom-window parameter set.",
    "category": "",
    "verified": true
  },
  "__SOONIP": {
    "behavior": "Native handler at 0x17C9A: rejects the query form ('?' at arg[1]), copies its keyword argument into a local buffer, looks it up to an index via parser $6090, range-checks 0..0x31 (49), and dispatches through $17A64 \u2014 a keyword sub-command parser/dispatcher.",
    "category": "",
    "verified": true
  },
  "__SOONRUN": {
    "behavior": "Native handler at 0x3A74C: initializes a count at 0xFFAD, then loops decrementing it (while > -1000 and while helper $32BDA(D0,0x2000) stays positive) before calling $3A0E2 and $3A642 with the arg-string pointer at (A6+8) \u2014 a range/search-and-emit loop whose exact subsystem is not identifiable from this fragment.",
    "category": "",
    "verified": false
  },
  "__SOTRACE": {
    "behavior": "Native handler at 0x4E7BC: stores its 32-bit argument into state cell $FFBB12, loads the mode word $FFB076, and calls display/state-update helper $1AF8 \u2014 sets a trace-related state variable and triggers a redraw.",
    "category": "",
    "verified": true
  },
  "__TOIACT": {
    "behavior": "Handler at 0x179FE tail-calls fcn.179E2, which clears bit 11 of state word $b212.w, tests $ba6b.w bit 2, and conditionally ORs 0x56 into the display-mode/scale status word $b1f8.w before jsr $19c. Activates the TOI (third-order-intercept) measurement mode.",
    "category": "",
    "verified": true
  },
  "__TOIANOT": {
    "behavior": "Handler at 0x5F276 pushes 0xFFFF plus its D0 arg and calls fcn.5E9C4, which clears/updates the TOI measurement state word $94d6.w (in the 0x948e measurement-state block) according to the arg (0/-1/other). Sets the TOI annotation state.",
    "category": "",
    "verified": true
  },
  "__TOIDISP": {
    "behavior": "Handler at 0x3F84A takes arg (8,A6), calls fcn.3D83E (which when $b234.w==5 runs HP-IB/display output routines $64f0/$62e4/$62b2), then $5dc2/$ad8. Formats and emits a TOI display/result value via the output path; exact quantity is name-inferred.",
    "category": "",
    "verified": false
  },
  "__TOIMEAS": {
    "behavior": "Handler at 0x17FB4 is a jump-table state machine: jsr $154 range-checks an index, indexes a 7-entry PC-relative table at 0x17FBC (offsets 0x62..0xa6) and jsr $100 to the selected step, also invoking DLP sources at 0x193b0/0x193b6. Drives the sequential TOI measurement.",
    "category": "",
    "verified": true
  },
  "__TOINOSIG": {
    "behavior": "Handler at 0x5F28C calls fcn.5ECA2, parses a numeric arg via jsr $2f2, then clamps the value at (8,A6) to a maximum of 0x3E80 (16000) and a minimum of 1. A parameter-setter for the TOI no-signal amplitude threshold.",
    "category": "",
    "verified": true
  },
  "__TOIRUN": {
    "behavior": "Handler at 0x6FE6 clears run/marker byte $bf01.w, reads mode byte $9b20.w (sign-extended), and on the taken path sets flag $beff.w = 0xFF. Starts/continues the TOI measurement run.",
    "category": "",
    "verified": true
  },
  "__TOISIG": {
    "behavior": "Handler at 0x40384 builds a flags byte in a local by testing hardware/status bits: $b1f7.w bit4, $b0a1.w bit0, $b1e0.w bit8, and $b1f9.w bits1/2/4 (qualified by $b073.w bits 2/3/5). Aggregates signal-present status into a returned bitfield.",
    "category": "",
    "verified": true
  },
  "__TOITERM": {
    "behavior": "Handler at 0x5ECC4 calls fcn.5ECBC, clears measurement-state words $94e0.w and $94d6.w, then via fcn.5ECA2/5E33A conditionally finalizes. Terminates the TOI measurement and resets its state.",
    "category": "",
    "verified": true
  },
  "__TOLGSCL": {
    "behavior": "Handler at 0xBA7C clamps two args each to 0..15, stores them to $ba74.w/$ba75.w, packs them into a word 0x7000|(a<<4)|b and sends it via fcn.C30A. Sets the log-scale (two 4-bit fields) display/scale parameter.",
    "category": "",
    "verified": true
  },
  "__TOLGUNITS": {
    "behavior": "Handler at 0x3EDE4 matches an argument against a 7-entry table at 0x3EDEC (offsets 0x44..0x64) via fcn.39762, then parses two numeric values through jsr $2f2 at (8,A6)/(-4,A6). Selects the log-amplitude units and their operands.",
    "category": "",
    "verified": true
  },
  "__TOLNSCL": {
    "behavior": "Handler at 0x3972E is a 3-instruction accessor: link, move.w $b20a.w,D0, unlk/rts \u2014 it returns the current linear-scale value held in RAM word $b20a.w.",
    "category": "",
    "verified": true
  },
  "__TOLNUNITS": {
    "behavior": "Handler at 0x7CD6 masks bits 0x30 of status word $b1f6.w and returns 1 (seq) iff both bits are set, else 0. A boolean query for whether linear-units mode is selected.",
    "category": "",
    "verified": true
  },
  "__VCOM": {
    "behavior": "Unresolved (handler_pc=0): a DLP-internal startup 'global routine' dispatched through the DLP record region (slot 1145, record-offset 0x479 \u2192 recPtr 0x71AFB), token 0x158\u21920x5FBDE per docs. It is the one startup global with a real body, setting up the common/video DLP environment; not resolvable via the C dispatch tables.",
    "category": "",
    "verified": false
  },
  "__WN_EXIT": {
    "behavior": "Handler at 0x29BE4 reads window-state byte $abfa.w and returns 1 (seq) iff it equals 5, else 0. A boolean predicate testing the window 'exit' condition.",
    "category": "",
    "verified": true
  },
  "__WN_ON": {
    "behavior": "Handler at 0x4C530 parses an argument list via jsr $2f2, then while a count at (-e,A6) > 0 iterates computing element addresses and calling fcn.4BDE6 per element. Enables/configures windows from the parsed list.",
    "category": "",
    "verified": true
  },
  "__WN_PK_NL": {
    "behavior": "Handler at 0x37D70 walks a window peak/marker table using cursor $a5d8.w vs count $a5d6.w and base pointer $a5dc.w (bounds $a5e4/$a5e8), searching via fcn.3174C/$439a. Finds the next peak entry to the left in the window peak array.",
    "category": "",
    "verified": true
  },
  "__WN_PK_NR": {
    "behavior": "Handler at 0x57E86 parses 3 coordinate params plus a keyword via fcn.57B80, then dispatches on the keyword's first letter \u2014 'T'/'t' selects plane mask 0x5555 (text/graticule plane), 'A'/'a' selects 0xAAAA (trace plane) \u2014 and calls draw routine jsr $39a. Paints into a window using a plane-select code (name suggests peak-next-right but the disasm is a plane-select paint).",
    "category": "",
    "verified": true
  },
  "__WN_P_ZOOM": {
    "behavior": "Handler at 0x37FC4 calls fcn.37ED6 to compute a value, ORs it with command tag 0xB000, and emits it via jsr $646; neighbouring code maintains a window flag bitmap at (-0x5a34,A4) with dirty flag $a5d5.w. Issues the window point-zoom command.",
    "category": "",
    "verified": true
  },
  "__WN_VARDEF": {
    "behavior": "DLP-internal ('__'-prefixed) command whose handler did not resolve through the dispatch tables (pc=0); by name it appears to be a WiNdow VARiable DEFinition \u2014 declaring/binding a named variable within the DLP window/graphics context \u2014 but no disassembly confirms this.",
    "category": "dlp-internal",
    "verified": false
  },
  "or": {
    "behavior": "Native handler at 0x434e6 that LINKs a frame, validates a 0x168-byte CalRAM block via helper $61ec, then word-copies calibration/state tables from CalRAM scratch (0x2fc016, 0x2fcb42, 0x2fc91a, 0x2fca1c) into low working-RAM buffers ($9eb6, $a01e, $a096, $a198); it is a state/table restore from cal RAM, not a bitwise-OR operation.",
    "category": "state-restore",
    "verified": true
  }
}